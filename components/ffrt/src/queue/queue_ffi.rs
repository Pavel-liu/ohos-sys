// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::deprecated_semver)]
use crate::type_def::*;

#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl ffrt_queue_type_t {
    /// Serial queue.
    pub const ffrt_queue_serial: ffrt_queue_type_t = ffrt_queue_type_t(0);
    /// Concurrent queue.
    pub const ffrt_queue_concurrent: ffrt_queue_type_t = ffrt_queue_type_t(1);
    /// Invalid queue.
    pub const ffrt_queue_max: ffrt_queue_type_t = ffrt_queue_type_t(2);
}
#[repr(transparent)]
/// Enumerates the queue types.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct ffrt_queue_type_t(pub ::core::ffi::c_uint);
/// Defines the queue handle, which identifies different queues.
///
///
/// Available since API-level: 10
pub type ffrt_queue_t = *mut ::core::ffi::c_void;
extern "C" {
    /// Initializes a queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Indicates a pointer to the queue attribute.
    ///
    /// # Returns
    ///
    /// * Returns <b>0</b> if the queue attribute is initialized;
    /// returns <b>-1</b> otherwise.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_init(attr: *mut ffrt_queue_attr_t) -> ::core::ffi::c_int;
    /// Destroys a queue attribute, the user needs to invoke this interface.
    ///
    /// # Arguments
    ///
    /// * `attr` - Indicates a pointer to the queue attribute.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_destroy(attr: *mut ffrt_queue_attr_t);
    /// Sets the QoS for a queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Indicates a pointer to the queue attribute.
    ///
    /// * `qos` - Indicates the QoS.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_set_qos(attr: *mut ffrt_queue_attr_t, qos: ffrt_qos_t);
    /// Gets the QoS of a queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Indicates a pointer to the queue attribute.
    ///
    /// # Returns
    ///
    /// * Returns the QoS.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_get_qos(attr: *const ffrt_queue_attr_t) -> ffrt_qos_t;
    /// Sets the execution timeout of a serial queue attribute.
    ///
    /// The lower limit of timeout value is 1 ms, if the value is less than 1 ms, it will be set to 1 ms.
    ///
    /// # Arguments
    ///
    /// * `attr` - Serial queue attribute pointer.
    ///
    /// * `timeout_us` - Serial queue task execution timeout.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_set_timeout(attr: *mut ffrt_queue_attr_t, timeout_us: u64);
    /// Gets the execution timeout of a serial queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Serial queue attribute pointer.
    ///
    /// # Returns
    ///
    /// * Returns the serial queue task execution timeout.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_get_timeout(attr: *const ffrt_queue_attr_t) -> u64;
    /// Sets the timeout callback function of a serial queue attribute.
    ///
    /// Do not call `exit` in `f` - this may cause unexpected behavior.
    /// # Arguments
    ///
    /// * `attr` - Serial queue attribute pointer.
    ///
    /// * `f` - Serial queue timeout callback function.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_set_callback(
        attr: *mut ffrt_queue_attr_t,
        f: *mut ffrt_function_header_t,
    );
    /// Gets the timeout callback function of a serial queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Serial queue attribute pointer.
    ///
    /// # Returns
    ///
    /// * Returns the serial queue task timeout callback function.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_attr_get_callback(
        attr: *const ffrt_queue_attr_t,
    ) -> *mut ffrt_function_header_t;
    /// Sets the queue max concurrency of a queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Queue attribute pointer.
    ///
    /// * `max_concurrency` - queue max_concurrency.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn ffrt_queue_attr_set_max_concurrency(
        attr: *mut ffrt_queue_attr_t,
        max_concurrency: ::core::ffi::c_int,
    );
    /// Gets the queue max concurrency of a queue attribute.
    ///
    /// # Arguments
    ///
    /// * `attr` - Queue attribute pointer.
    ///
    /// # Returns
    ///
    /// * Returns the queue max concurrency.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn ffrt_queue_attr_get_max_concurrency(
        attr: *const ffrt_queue_attr_t,
    ) -> ::core::ffi::c_int;
    /// Sets the execution mode of a queue attribute.
    ///
    /// This interface specifies whether tasks in the queue are executed in coroutine mode or thread mode.
    /// By default, tasks are executed in coroutine mode.
    /// Set <b>mode</b> to <b>true</b> to enable thread-based execution.
    ///
    /// # Arguments
    ///
    /// * `attr` - Queue attribute pointer.
    ///
    /// * `mode` - Indicates whether to enable thread-based execution mode.
    /// - <b>true</b>: Tasks are executed as native threads (thread mode).
    /// - <b>false</b>: Tasks are executed as coroutines (default).
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn ffrt_queue_attr_set_thread_mode(attr: *mut ffrt_queue_attr_t, mode: bool);
    /// Gets the execution mode of a queue attribute.
    ///
    /// This interface returns whether tasks in the queue are configured to run in thread-based execution mode (thread mode).
    ///
    /// # Arguments
    ///
    /// * `attr` - Queue attribute pointer.
    ///
    /// # Returns
    ///
    /// * Returns <b>true</b> if tasks are executed as native threads (thread mode);
    /// returns <b>false</b> if tasks are executed as coroutines (default).
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn ffrt_queue_attr_get_thread_mode(attr: *const ffrt_queue_attr_t) -> bool;
    /// Creates a queue.
    ///
    /// # Arguments
    ///
    /// * `type` - Indicates the queue type.
    ///
    /// * `name` - Indicates a pointer to the queue name.
    ///
    /// * `attr` - Indicates a pointer to the queue attribute.
    ///
    /// # Returns
    ///
    /// * Returns a non-null queue handle if the queue is created;
    /// returns a null pointer otherwise.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_create(
        type_: ffrt_queue_type_t,
        name: *const ::core::ffi::c_char,
        attr: *const ffrt_queue_attr_t,
    ) -> ffrt_queue_t;
    /// Destroys a queue, the user needs to invoke this interface.
    ///
    /// # Arguments
    ///
    /// * `queue` - Indicates a queue handle.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_destroy(queue: ffrt_queue_t);
    /// Submits a task to a queue.
    ///
    /// # Arguments
    ///
    /// * `queue` - Indicates a queue handle.
    ///
    /// * `f` - Indicates a pointer to the task executor.
    ///
    /// * `attr` - Indicates a pointer to the task attribute.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_submit(
        queue: ffrt_queue_t,
        f: *mut ffrt_function_header_t,
        attr: *const ffrt_task_attr_t,
    );
    /// Submits a task to the queue, and obtains a task handle.
    ///
    /// # Arguments
    ///
    /// * `queue` - Indicates a queue handle.
    ///
    /// * `f` - Indicates a pointer to the task executor.
    ///
    /// * `attr` - Indicates a pointer to the task attribute.
    ///
    /// # Returns
    ///
    /// * Returns a non-null task handle if the task is submitted;
    /// returns a null pointer otherwise.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_submit_h(
        queue: ffrt_queue_t,
        f: *mut ffrt_function_header_t,
        attr: *const ffrt_task_attr_t,
    ) -> ffrt_task_handle_t;
    /// Submits a task to a queue, simplified from of the ffrt_queue_submit interface.
    ///
    /// This interface wraps the provided task function and its argument into a task wrapper designed
    /// for queue submission (ffrt_function_kind_queue). The task destroy callback (after_func), which
    /// would normally handle any post-execution cleanup, is automatically set to NULL in this wrapper,
    /// thus omitting any additional cleanup actions. The resulting task wrapper is then submitted to
    /// the specified queue via the ffrt_queue_submit interface.
    ///
    /// # Arguments
    ///
    /// * `queue` - Indicates a queue handle.
    ///
    /// * `func` - Indicates a task function to be executed.
    ///
    /// * `arg` - Indicates a pointer to the argument or closure data that will be passed to the task function.
    ///
    /// * `attr` - Indicates a pointer to the task attribute.
    /// [`ffrt_queue_submit`]
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn ffrt_queue_submit_f(
        queue: ffrt_queue_t,
        func: ffrt_function_t,
        arg: *mut ::core::ffi::c_void,
        attr: *const ffrt_task_attr_t,
    );
    /// Submits a task to a queue, and obtains a task handle, simplified from the ffrt_queue_submit_h interface.
    ///
    /// This interface wraps the provided task function and its argument into a task wrapper designed
    /// for queue submission (ffrt_function_kind_queue). The task destroy callback (after_func), which
    /// would normally handle any post-execution cleanup, is automatically set to NULL in this wrapper,
    /// thus omitting any additional cleanup actions. The resulting task wrapper is then submitted to
    /// the specified queue via the ffrt_queue_submit_h interface.
    ///
    /// # Arguments
    ///
    /// * `queue` - Indicates a queue handle.
    ///
    /// * `func` - Indicates a task function to be executed.
    ///
    /// * `arg` - Indicates a pointer to the argument or closure data that will be passed to the task function.
    ///
    /// * `attr` - Indicates a pointer to the task attribute.
    ///
    /// # Returns
    ///
    /// * Returns a non-null task handle if the task is submitted;
    /// returns a null pointer otherwise.
    /// [`ffrt_queue_submit_h`]
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn ffrt_queue_submit_h_f(
        queue: ffrt_queue_t,
        func: ffrt_function_t,
        arg: *mut ::core::ffi::c_void,
        attr: *const ffrt_task_attr_t,
    ) -> ffrt_task_handle_t;
    /// Waits until a task in the queue is complete.
    ///
    /// # Arguments
    ///
    /// * `handle` - Indicates a task handle.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_wait(handle: ffrt_task_handle_t);
    /// Cancels a task in the queue.
    ///
    /// # Arguments
    ///
    /// * `handle` - Indicates a task handle.
    ///
    /// # Returns
    ///
    /// * Returns <b>0</b> if the task is canceled;
    /// returns <b>-1</b> otherwise.
    ///
    /// Available since API-level: 10
    pub fn ffrt_queue_cancel(handle: ffrt_task_handle_t) -> ::core::ffi::c_int;
    /// Gets the application main thread queue.
    ///
    ///
    /// # Returns
    ///
    /// * Returns application main thread queue.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn ffrt_get_main_queue() -> ffrt_queue_t;
    /// Gets the application worker(ArkTs) thread queue.
    ///
    ///
    /// # Returns
    ///
    /// * Returns application worker(ArkTs) thread queue.
    ///
    /// **Deprecated** since 18
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    #[deprecated(since = "18")]
    pub fn ffrt_get_current_queue() -> ffrt_queue_t;
}
