// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]

/// Defines the handles of models.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[repr(C)]
pub struct OH_NNModel {
    _unused: [u8; 0],
}
/// Defines the compilation handle.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[repr(C)]
pub struct OH_NNCompilation {
    _unused: [u8; 0],
}
/// Defines the executor handle.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[repr(C)]
pub struct OH_NNExecutor {
    _unused: [u8; 0],
}
/// Defines the quantization parameter handle.
///
///
/// Available since API-level: 11
///
/// Version: 1.0
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
pub struct NN_QuantParam {
    _unused: [u8; 0],
}
/// Defines the tensor descriptor handle.
///
///
/// Available since API-level: 11
///
/// Version: 1.0
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
pub struct NN_TensorDesc {
    _unused: [u8; 0],
}
/// Defines the tensor handle.
///
///
/// Available since API-level: 11
///
/// Version: 1.0
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
pub struct NN_Tensor {
    _unused: [u8; 0],
}
impl OH_NN_PerformanceMode {
    /// No performance mode preference
    pub const OH_NN_PERFORMANCE_NONE: OH_NN_PerformanceMode = OH_NN_PerformanceMode(0);
    /// Low power consumption mode
    pub const OH_NN_PERFORMANCE_LOW: OH_NN_PerformanceMode = OH_NN_PerformanceMode(1);
    /// Medium performance mode
    pub const OH_NN_PERFORMANCE_MEDIUM: OH_NN_PerformanceMode = OH_NN_PerformanceMode(2);
    /// High performance mode
    pub const OH_NN_PERFORMANCE_HIGH: OH_NN_PerformanceMode = OH_NN_PerformanceMode(3);
    /// Ultimate performance mode
    pub const OH_NN_PERFORMANCE_EXTREME: OH_NN_PerformanceMode = OH_NN_PerformanceMode(4);
}
#[repr(transparent)]
/// Defines the hardware performance mode.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_PerformanceMode(pub ::core::ffi::c_uint);
impl OH_NN_Priority {
    /// No priority preference
    pub const OH_NN_PRIORITY_NONE: OH_NN_Priority = OH_NN_Priority(0);
    /// Low priority
    pub const OH_NN_PRIORITY_LOW: OH_NN_Priority = OH_NN_Priority(1);
    /// Medium priority
    pub const OH_NN_PRIORITY_MEDIUM: OH_NN_Priority = OH_NN_Priority(2);
    /// High priority
    pub const OH_NN_PRIORITY_HIGH: OH_NN_Priority = OH_NN_Priority(3);
}
#[repr(transparent)]
/// Defines the model inference task priority.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_Priority(pub ::core::ffi::c_uint);
impl OH_NN_ReturnCode {
    /// The operation is successful.
    pub const OH_NN_SUCCESS: OH_NN_ReturnCode = OH_NN_ReturnCode(0);
    /// The operation failed.
    pub const OH_NN_FAILED: OH_NN_ReturnCode = OH_NN_ReturnCode(1);
    /// Invalid parameter.
    pub const OH_NN_INVALID_PARAMETER: OH_NN_ReturnCode = OH_NN_ReturnCode(2);
    /// Memory-related error, for example, insufficient memory, memory data copy failure,
    /// or memory application failure.
    pub const OH_NN_MEMORY_ERROR: OH_NN_ReturnCode = OH_NN_ReturnCode(3);
    /// Invalid operation.
    pub const OH_NN_OPERATION_FORBIDDEN: OH_NN_ReturnCode = OH_NN_ReturnCode(4);
    /// Null pointer exception
    pub const OH_NN_NULL_PTR: OH_NN_ReturnCode = OH_NN_ReturnCode(5);
    /// Invalid file.
    pub const OH_NN_INVALID_FILE: OH_NN_ReturnCode = OH_NN_ReturnCode(6);
    /// A hardware error occurs, for example, HDL service crash.
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NN_UNAVAILABLE_DEVICE`]
    #[deprecated(since = "11", note = "Use instead: OH_NN_UNAVAILABLE_DEVICE")]
    pub const OH_NN_UNAVALIDABLE_DEVICE: OH_NN_ReturnCode = OH_NN_ReturnCode(7);
    /// Invalid path.
    pub const OH_NN_INVALID_PATH: OH_NN_ReturnCode = OH_NN_ReturnCode(8);
    /// Timeout.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_TIMEOUT: OH_NN_ReturnCode = OH_NN_ReturnCode(9);
    /// Unsupported.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_UNSUPPORTED: OH_NN_ReturnCode = OH_NN_ReturnCode(10);
    /// Connection Exception.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_CONNECTION_EXCEPTION: OH_NN_ReturnCode = OH_NN_ReturnCode(11);
    /// Save cache exception.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_SAVE_CACHE_EXCEPTION: OH_NN_ReturnCode = OH_NN_ReturnCode(12);
    /// Dynamic shape.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_DYNAMIC_SHAPE: OH_NN_ReturnCode = OH_NN_ReturnCode(13);
    /// A hardware error occurs, for example, HDL service crash.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_UNAVAILABLE_DEVICE: OH_NN_ReturnCode = OH_NN_ReturnCode(14);
}
#[repr(transparent)]
/// Defines error codes.
///
///
/// Available since API-level: 9
///
/// Version: 2.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_ReturnCode(pub ::core::ffi::c_uint);
/// Defines the callback function handle for the post-process when the asynchronous execution has been done.
///
/// Use <b>userData</b> to identify the asynchronous execution you want to get.
/// It is the argument <b>userData</b> passed to [`OH_NNExecutor_RunAsync`].
///
///
/// Use <b>errCode</b> of type [`OH_NN_ReturnCode`] to get the error code returned by the asynchronous execution.
///
///
/// The <b>outputTensor</b> and <b>outputCount</b> are the inference results, which is the same as ones passed to
/// [`OH_NNExecutor_RunAsync`].
///
///
/// # Arguments
///
/// * `userData` - Asynchronous execution identifier, which is the argument <b>userData</b> passed to
/// [`OH_NNExecutor_RunAsync`].
///
/// * `errCode` - Error code [`OH_NN_ReturnCode`] returned by the asynchronous execution.
///
/// * `outputTensor` - An array of output tensors [`NN_Tensor`] of the model, which is the same as the argument
/// <b>outputTensor</b> passed to [`OH_NNExecutor_RunAsync`].
///
/// * `outputCount` - Output tensor count, which is the same as the argument <b>outputCount</b> passed to
/// [`OH_NNExecutor_RunAsync`].
///
/// Available since API-level: 11
///
/// Version: 1.0
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type NN_OnRunDone = ::core::option::Option<
    unsafe extern "C" fn(
        userData: *mut ::core::ffi::c_void,
        errCode: OH_NN_ReturnCode,
        outputTensor: *mut *mut ::core::ffi::c_void,
        outputCount: i32,
    ),
>;
/// Defines the callback function handle for the post-process when the device driver service is dead during
/// asynchronous execution.
///
/// You should recompile the model if this callback function is called.
///
///
/// Use <b>userData</b> to identify the asynchronous execution you want to get.
/// It is the argument <b>userData</b> passed to [`OH_NNExecutor_RunAsync`].
///
///
/// # Arguments
///
/// * `userData` - Asynchronous execution identifier, which is the argument <b>userData</b> passed to
/// [`OH_NNExecutor_RunAsync`].
///
/// Available since API-level: 11
///
/// Version: 1.0
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type NN_OnServiceDied =
    ::core::option::Option<unsafe extern "C" fn(userData: *mut ::core::ffi::c_void)>;
impl OH_NN_FuseType {
    /// The fusion activation function is not specified.
    pub const OH_NN_FUSED_NONE: OH_NN_FuseType = OH_NN_FuseType(0);
    /// Fusion relu activation function
    pub const OH_NN_FUSED_RELU: OH_NN_FuseType = OH_NN_FuseType(1);
    /// Fusion relu6 activation function
    pub const OH_NN_FUSED_RELU6: OH_NN_FuseType = OH_NN_FuseType(2);
}
#[repr(transparent)]
/// Defines activation function types in the fusion operator.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_FuseType(pub i8);
impl OH_NN_Format {
    /// The tensor does not have a specific layout type (such as scalar or vector).
    pub const OH_NN_FORMAT_NONE: OH_NN_Format = OH_NN_Format(0);
    /// The tensor arranges data in NCHW format.
    pub const OH_NN_FORMAT_NCHW: OH_NN_Format = OH_NN_Format(1);
    /// The tensor arranges data in NHWC format.
    pub const OH_NN_FORMAT_NHWC: OH_NN_Format = OH_NN_Format(2);
    /// The tensor arranges data in ND format.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub const OH_NN_FORMAT_ND: OH_NN_Format = OH_NN_Format(3);
}
#[repr(transparent)]
/// Defines the layout type of tensor data.
///
///
/// Available since API-level: 9
///
/// Version: 2.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_Format(pub ::core::ffi::c_uint);
impl OH_NN_DeviceType {
    /// Devices that are not CPU, GPU, or dedicated accelerator
    pub const OH_NN_OTHERS: OH_NN_DeviceType = OH_NN_DeviceType(0);
    /// CPU device
    pub const OH_NN_CPU: OH_NN_DeviceType = OH_NN_DeviceType(1);
    /// GPU device
    pub const OH_NN_GPU: OH_NN_DeviceType = OH_NN_DeviceType(2);
    /// Dedicated hardware accelerator
    pub const OH_NN_ACCELERATOR: OH_NN_DeviceType = OH_NN_DeviceType(3);
}
#[repr(transparent)]
/// Defines device types.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_DeviceType(pub ::core::ffi::c_uint);
impl OH_NN_DataType {
    /// Unknown type
    pub const OH_NN_UNKNOWN: OH_NN_DataType = OH_NN_DataType(0);
    /// bool
    pub const OH_NN_BOOL: OH_NN_DataType = OH_NN_DataType(1);
    /// int8
    pub const OH_NN_INT8: OH_NN_DataType = OH_NN_DataType(2);
    /// int16
    pub const OH_NN_INT16: OH_NN_DataType = OH_NN_DataType(3);
    /// int32
    pub const OH_NN_INT32: OH_NN_DataType = OH_NN_DataType(4);
    /// int64
    pub const OH_NN_INT64: OH_NN_DataType = OH_NN_DataType(5);
    /// uint8
    pub const OH_NN_UINT8: OH_NN_DataType = OH_NN_DataType(6);
    /// uint16
    pub const OH_NN_UINT16: OH_NN_DataType = OH_NN_DataType(7);
    /// uint32
    pub const OH_NN_UINT32: OH_NN_DataType = OH_NN_DataType(8);
    /// uint64
    pub const OH_NN_UINT64: OH_NN_DataType = OH_NN_DataType(9);
    /// float16
    pub const OH_NN_FLOAT16: OH_NN_DataType = OH_NN_DataType(10);
    /// float32
    pub const OH_NN_FLOAT32: OH_NN_DataType = OH_NN_DataType(11);
    /// float64
    pub const OH_NN_FLOAT64: OH_NN_DataType = OH_NN_DataType(12);
}
#[repr(transparent)]
/// Defines tensor data types.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_DataType(pub ::core::ffi::c_uint);
impl OH_NN_OperationType {
    /// Returns the tensor of the sum of the elements corresponding to two input tensors.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: first input tensor, of the Boolean or number type.
    /// * <b>input2</b>: second input tensor, whose data type must be the same as that of the first tensor.
    ///
    /// Parameters:
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: sum of <b>input1</b> and <b>input2</b>.
    /// The data shape is the same as that of the input after broadcasting,
    /// and the data type is the same as that of the input with a higher precision.
    pub const OH_NN_OPS_ADD: OH_NN_OperationType = OH_NN_OperationType(1);
    /// Apply 2D average pooling to the input tensor, which now must be in NHWC format.
    /// The int8 quantization input is supported.
    ///
    /// If the input contains the <b>padMode</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: tensor.
    ///
    /// Parameters:
    ///
    /// * <b>kernelSize</b> indicates the kernel size used to obtain the average value.
    /// It is an int array [kernelHeight, kernelWidth].
    /// The first number indicates the kernel height, and the second number indicates the kernel width.
    /// * <b>strides</b> indicates the distance of kernel moving. The value is an int array
    /// [strideHeight, strideWidth]. The first number indicates the moving step in height,
    /// and the second number indicates the moving step in width.
    /// * <b>padMode</b>: padding mode, which is optional. The value is of the int type and can be <b>0</b> (same)
    /// or <b>1</b> (valid). The nearest neighbor value is used for padding.
    /// <b>0</b> (same): The height and width of the output are the same as those of the input.
    /// The total and padding quantity is calculated horizontally and vertically and
    /// evenly distributed to the top, bottom, left, right if possible.
    /// Otherwise, the last additional padding will be completed from the bottom and right.
    /// <b>1</b> (valid): The possible maximum height and width of the output will be returned in case of no
    /// padding. Excessive pixels will be discarded.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>global</b> Whether to do global pooling.
    /// * <b>roundMode</b> Boundary handling method. When the pool cannot completely cover the input feature map,
    /// the output feature map is rounded up, 0 means round down, 1 means round up.
    ///
    /// If the input contains the <b>padList</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: tensor.
    ///
    /// Parameters:
    ///
    /// * <b>kernelSize</b> indicates the kernel size used to obtain the average value.
    /// It is an int array [kernelHeight, kernelWidth].
    /// The first number indicates the kernel height, and the second number indicates the kernel width.
    /// * <b>strides</b> indicates the distance of kernel moving. The value is an int array
    /// [strideHeight, strideWidth]. The first number indicates the moving step in height,
    /// and the second number indicates the moving step in width.
    /// * <b>padList</b>: padding around <b>input</b>. It is an int array [top, bottom, left, right],
    /// and the nearest neighbor values are used for padding.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>global</b> Whether to do global pooling.
    /// * <b>roundMode</b> Boundary handling method. When the pool cannot completely cover the input feature map,
    /// the output feature map is rounded up, 0 means round down, 1 means round up.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: average pooling result of the input.
    pub const OH_NN_OPS_AVG_POOL: OH_NN_OperationType = OH_NN_OperationType(2);
    /// Performs batch normalization on the input tensors. Apply a transformation to keep the average output
    /// close to 0 and the output standard deviation close to 1.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor of shape [N, ..., C].
    /// The <i>n</i>th dimension is the number of channels.
    /// * <b>scale</b>: 1D tensor of the scaling factor used to scale the first normalized tensor.
    /// * <b>offset</b>: 1D tensor used to move to the first normalized tensor.
    /// * <b>mean</b>: 1D tensor of the overall mean value. It is used only for inference.
    /// In case of training, this parameter must be left empty.
    /// * <b>variance</b>: 1D tensor used for the overall variance. It is used only for inference.
    /// In case of training, this parameter must be left empty.
    ///
    /// Parameters:
    ///
    /// * <b>epsilon</b>: fixed small additional value.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional output tensor whose shape
    /// and data type are the same as those of the input.
    pub const OH_NN_OPS_BATCH_NORM: OH_NN_OperationType = OH_NN_OperationType(3);
    /// Divides the batch dimension of a 4D tensor into small blocks by <b>blockShape</b>,
    /// and interleaves these blocks back into the spatial dimension.
    ///
    /// Parameters:
    ///
    /// * <b>input</b>: input tensor. The dimension will be divided into small blocks,
    /// and these blocks will be interleaved into the spatial dimension.
    ///
    /// Outputs:
    ///
    /// * <b>blockSize</b>: size of each block to be interleaved into the spatial dimension.
    /// The value is an array [heightBlock, widthBlock].
    /// * <b>crops</b>: elements truncated from the spatial dimension of the output. The value is a 2D array
    /// [[crop0Start, crop0End], [crop1Start, crop1End]] with the shape of (2, 2).
    ///
    ///
    /// Outputs:
    ///
    /// * <b>output</b>. Assume that the shape of <b>input</b> is (n,h,w,c) and
    /// the shape of <b>output</b> is (n',h',w',c'):
    /// n' = n / (blockShape[0] * blockShape[1])
    /// h' = h * blockShape[0] - crops[0][0] - crops[0][1]
    /// w' = w * blockShape[1] - crops[1][0] - crops[1][1]
    /// c'= c
    pub const OH_NN_OPS_BATCH_TO_SPACE_ND: OH_NN_OperationType = OH_NN_OperationType(4);
    /// Offsets the data in each dimension of the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor, which can have two to five dimensions.
    /// * <b>bias</b>: offset of the number of input dimensions.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: sum of the input tensor and the bias in each dimension.
    pub const OH_NN_OPS_BIAS_ADD: OH_NN_OperationType = OH_NN_OperationType(5);
    /// Converts the data type in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>type</b>: converted data type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: converted tensor.
    pub const OH_NN_OPS_CAST: OH_NN_OperationType = OH_NN_OperationType(6);
    /// Connects tensors in a specified dimension.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>N</i> input tensors.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension for connecting tensors.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: result of connecting <i>N</i> tensors along the axis.
    pub const OH_NN_OPS_CONCAT: OH_NN_OperationType = OH_NN_OperationType(7);
    /// 2D convolutional layer.
    ///
    /// If the input contains the <b>padMode</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, inChannel/group] format.
    /// The value of <b>inChannel</b> must be exactly divided by the value of <b>group</b>.
    ///
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    ///
    /// * <b>padMode</b>: padding mode of <b>input</b>.
    /// The value is of the int type and can be <b>0</b> (same) or <b>1</b> (valid).
    /// <b>0</b> (same): The height and width of the output are the same as those of the input.
    /// The total padding quantity is calculated horizontally and vertically
    /// and evenly distributed to the top, bottom, left, and right if possible.
    /// Otherwise, the last additional padding will be completed from the bottom and right.
    ///
    /// <b>1</b> (valid): The possible maximum height and width of the output will be returned
    /// in case of no padding. The excessive pixels will be discarded.
    /// * <b>group</b>: number of groups in which the input is divided by <b>inChannel</b>. The value is of the
    /// int type. If <b>group</b> is <b>1</b>, it is a conventional convolution. If <b>group</b> is greater
    /// than <b>1</b> and less than or equal to <b>inChannel</b>, it is a group convolution.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// If the input contains the <b>padList</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, inChannel/group] format.
    /// The value of <b>inChannel</b> must be exactly divided by the value of <b>group</b>.
    ///
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    /// * <b>padList</b>: padding around <b>input</b>. It is an int array [top, bottom, left, right].
    /// * <b>group</b>: number of groups in which the input is divided by <b>inChannel</b>.
    /// The value is of the int type. If <b>group</b> is <b>1</b>, it is a conventional convolution.
    /// If <b>group</b> is <b>inChannel</b>, it is depthwiseConv2d. In this case, group==inChannel==outChannel.
    /// If <b>group</b> is greater than <b>1</b> and less than <b>inChannel</b>, it is a group convolution.
    /// In this case, outChannel==group.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: convolution computing result.
    pub const OH_NN_OPS_CONV2D: OH_NN_OperationType = OH_NN_OperationType(8);
    /// 2D convolution transposition.
    ///
    /// If the input contains the <b>padMode</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, inChannel/group] format.
    /// The value of <b>inChannel</b> must be exactly divided by the value of <b>group</b>.
    ///
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    ///
    /// Parameters:
    ///
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    /// * <b>padMode</b>: padding mode of <b>input</b>. The value is of the int type and can be <b>0</b> (same) or
    /// <b>1</b> (valid). <b>0</b> (same): The height and width of the output are the same as those of the input.
    /// The total padding quantity is calculated horizontally and vertically and evenly distributed to the top,
    /// bottom, left, and right if possible.
    /// Otherwise, the last additional padding will be completed from the bottom and right.
    /// <b>1</b> (valid): The possible maximum height and width of the output will be returned in case of
    /// no padding. The excessive pixels will be discarded.
    /// * <b>group</b>: number of groups in which the input is divided by <b>inChannel</b>. The value is of the int
    /// type. If <b>group</b> is <b>1</b>, it is a conventional convolution. If <b>group</b> is greater than
    /// <b>1</b> and less than or equal to <b>inChannel</b>, it is a group convolution.
    /// * <b>outputPads</b>: padding along the height and width of the output tensor. The value is an int or a tuple.
    /// It can be a single integer to specify the same value for all spatial dimensions. The amount of output
    /// padding along a dimension must be less than the stride along this dimension.
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// If the input contains the <b>padList</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, inChannel/group] format.
    /// The value of <b>inChannel</b> must be exactly divided by the value of <b>group</b>.
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    /// * <b>padList</b>: padding around <b>input</b>. It is an int array [top, bottom, left, right].
    /// * <b>group</b>: number of groups in which the input is divided by <b>inChannel</b>. The value is of the int
    /// type. If <b>group</b> is <b>1</b>, it is a conventional convolution. If <b>group</b> is greater than
    /// <b>1</b> and less than or equal to <b>inChannel</b>, it is a group convolution.
    /// * <b>outputPads</b>: padding along the height and width of the output tensor. The value is an int or a tuple.
    /// It can be a single integer to specify the same value for all spatial dimensions. The amount of output
    /// padding along a dimension must be less than the stride along this dimension.
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: computing result after convolution and transposition.
    pub const OH_NN_OPS_CONV2D_TRANSPOSE: OH_NN_OperationType = OH_NN_OperationType(9);
    /// 2D depthwise separable convolution.
    ///
    /// If the input contains the <b>padMode</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, 1] format.
    /// <b>outChannel</b> is equal to <b>channelMultiplier</b> multiplied by <b>inChannel</b>.
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    /// * <b>padMode</b>: padding mode of <b>input</b>.
    /// The value is of the int type and can be <b>0</b> (same) or <b>1</b> (valid).
    /// <b>0</b> (same): The height and width of the output are the same as those of the input. The total padding
    /// quantity is calculated horizontally and vertically and evenly distributed to the top, bottom, left, and
    /// right if possible. Otherwise, the last additional padding will be completed from the bottom and right.
    ///
    /// <b>1</b> (valid): The possible maximum height and width of the output will be returned in case of no
    /// padding. The excessive pixels will be discarded.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// If the input contains the <b>padList</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>weight</b>: convolution weight in [outChannel, kernelHeight, kernelWidth, 1] format.
    /// <b>outChannel</b> is equal to <b>channelMultiplier</b> multiplied by <b>inChannel</b>.
    /// * <b>bias</b>: bias of the convolution. It is an array with a length of <b>[outChannel]</b>.
    /// In quantization scenarios, the <b>bias</b> parameter does not require quantization parameters.
    /// The quantization version requires data input of the <b>OH_NN_INT32</b> type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>stride</b>: movement stride of the convolution kernel in height and width.
    /// It is an int array [strideHeight, strideWidth].
    /// * <b>dilation</b>: dilation size of the convolution kernel in height and width.
    /// It is an int array [dilationHeight, dilationWidth]. The value must be greater than
    /// or equal to <b>1</b> and cannot exceed the height and width of <b>input</b>.
    /// * <b>padList</b>: padding around <b>input</b>. It is an int array [top, bottom, left, right].
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: convolution computing result.
    pub const OH_NN_OPS_DEPTHWISE_CONV2D_NATIVE: OH_NN_OperationType = OH_NN_OperationType(10);
    /// Divides two input scalars or tensors.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: first input, which is a number, a bool, or a tensor whose data type is number or Boolean.
    /// * <b>input2</b>: second input, which must meet the following requirements:
    /// If the first input is a tensor, the second input can be a real number, a Boolean value, or a tensor whose
    /// data type is real number or Boolean value. If the first input is a real number or Boolean value,
    /// the second input must be a tensor whose data type is real number or Boolean value.
    ///
    /// Parameters:
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: result of dividing <b>input1</b> by <b>input2</b>.
    pub const OH_NN_OPS_DIV: OH_NN_OperationType = OH_NN_OperationType(11);
    /// Sets parameters to perform product (dot product), sum (addition and subtraction),
    /// or max (larger value) on the input.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: first input tensor.
    /// * <b>input2</b>: second input tensor.
    ///
    /// Parameters:
    ///
    /// * <b>mode</b>: operation mode. The value is an enumerated value.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: computing result, which has the same data type and shape of <b>input1</b>.
    pub const OH_NN_OPS_ELTWISE: OH_NN_OperationType = OH_NN_OperationType(12);
    /// Adds an additional dimension to a tensor in the given dimension.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>axis</b>: index of the dimension to be added.
    /// The value is of the int32_t type and must be a constant in the range [-dim-1, dim].
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: tensor after dimension expansion.
    pub const OH_NN_OPS_EXPAND_DIMS: OH_NN_OperationType = OH_NN_OperationType(13);
    /// Creates a tensor of the specified dimensions and fills it with a scalar.
    ///
    /// Inputs:
    ///
    /// * <b>value</b>: scalar used to fill the tensor.
    /// * <b>shape</b>: dimensions of the tensor to be created.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: generated tensor, which has the same data type as <b>value</b>.
    /// The tensor shape is specified by the <b>shape</b> parameter.
    pub const OH_NN_OPS_FILL: OH_NN_OperationType = OH_NN_OperationType(14);
    /// Full connection. The entire input is used as the feature map for feature extraction.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: full-connection input tensor.
    /// * <b>weight</b>: weight tensor for a full connection.
    /// * <b>bias</b>: full-connection bias. In quantization scenarios, no quantized parameter is required
    /// for this parameter. If quantization is required, the data must be of the OH_NN_INT32 type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>hasBias</b> Whether to use the bias.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: computed tensor.
    ///
    /// If the input contains the <b>axis</b> parameter or <b>useAxis</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: full-connection input tensor.
    /// * <b>weight</b>: weight tensor for a full connection.
    /// * <b>bias</b>: full-connection bias. In quantization scenarios, no quantized parameter is required
    /// for this parameter. If quantization is required, the data must be of the OH_NN_INT32 type.
    /// The actual quantization parameters are determined by <b>input</b> and <b>weight</b>.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: axis in which the full connection is applied. The specified axis and its following axes are
    /// converted into a 1D tensor for applying the full connection.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>useAxis</b> Whether to use the axis.
    /// * <b>hasBias</b> Whether to use the bias.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: computed tensor.
    pub const OH_NN_OPS_FULL_CONNECTION: OH_NN_OperationType = OH_NN_OperationType(15);
    /// Returns the slice of the input tensor based on the specified index and axis.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: tensor to be sliced.
    /// * <b>inputIndices</b>: indices of the specified input on the axis. The value is an array of the int type
    /// and must be in the range [0,input.shape[axis]).
    /// * <b>axis</b>: axis on which <b>input</b> is sliced. The value is an array with one element of the int32_t type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: sliced tensor.
    pub const OH_NN_OPS_GATHER: OH_NN_OperationType = OH_NN_OperationType(16);
    /// Calculate the <b>Hswish</b> activation value of the input.
    ///
    /// Inputs:
    ///
    /// * An <i>n</i>-dimensional input tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional <b>Hswish</b> activation value.
    /// The data type is the same as that of <b>shape</b> and <b>input</b>.
    pub const OH_NN_OPS_HSWISH: OH_NN_OperationType = OH_NN_OperationType(17);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i]<=input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>, can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>, can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used,
    /// the quantization parameters of the output cannot be omitted.
    /// However, values of the quantization parameters do not affect the result.
    pub const OH_NN_OPS_LESS_EQUAL: OH_NN_OperationType = OH_NN_OperationType(18);
    /// Calculate the inner product of <b>input1</b> and <b>input2</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: <i>n</i>-dimensional input tensor.
    /// * <b>input2</b>: <i>n</i>-dimensional input tensor.
    ///
    /// Parameters:
    ///
    /// * <b>TransposeX</b>: Boolean value indicating whether to transpose <b>input1</b>.
    /// * <b>TransposeY</b>: Boolean value indicating whether to transpose <b>input2</b>.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: inner product obtained after calculation. In case of type!=NN_UNKNOWN, the output data type is
    /// determined by <b>type</b>. In case of type==NN_UNKNOWN, the output data type depends on the data type
    /// converted during computing of <b>inputX</b> and <b>inputY</b>.
    pub const OH_NN_OPS_MATMUL: OH_NN_OperationType = OH_NN_OperationType(19);
    /// Calculates the maximum of <b>input1</b> and <b>input2</b> element-wise. The inputs of <b>input1</b>
    ///
    /// and <b>input2</b> comply with the implicit type conversion rules to make the data types consistent.
    /// The inputs must be two tensors or one tensor and one scalar.
    /// When the inputs are two tensors, their data types cannot be both OH_NN_BOOL.
    /// Their shapes can be broadcast to the same size.
    /// When the inputs are one tensor and one scalar, the scalar must be a constant.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: <i>n</i>-dimensional input tensor of the real number or OH_NN_BOOL type.
    /// * <b>input2</b>: <i>n</i>-dimensional input tensor of the real number or OH_NN_BOOL type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional output tensor. The <b>shape</b> and data type of
    /// <b>output</b> are the same as those of the two inputs with a higher precision.
    pub const OH_NN_OPS_MAXIMUM: OH_NN_OperationType = OH_NN_OperationType(20);
    /// Applies 2D maximum pooling to the input tensor.
    ///
    /// If the input contains the <b>padMode</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: tensor.
    ///
    /// Parameters:
    ///
    /// * <b>kernelSize</b>: kernel size used to obtain the maximum. It is an int array [kernelHeight, kernelWidth].
    /// The first number indicates the kernel height, and the second number indicates the kernel width.
    /// * <b>strides</b> indicates the distance of kernel moving. The value is an int array
    /// [strideHeight, strideWidth]. The first number indicates the moving step in height,
    /// and the second number indicates the moving step in width.
    /// * <b>padMode</b>: padding mode, which is optional. The value is of the int type and can be <b>0</b> (same)
    /// or <b>1</b> (valid). The nearest neighbor value is used for padding.
    /// <b>0</b> (same): The height and width of the output are the same as those of the input. The total padding
    /// quantity is calculated horizontally and vertically and evenly distributed to the top, bottom, left, and
    /// right if possible. Otherwise, the last additional padding will be completed from the bottom and right.
    /// <b>1</b> (valid): The possible maximum height and width of the output will be returned in case of
    /// no padding. The excessive pixels will be discarded.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>global</b> Whether to do global pooling.
    /// * <b>roundMode</b> Boundary handling method. When the pool cannot completely cover the input feature map,
    /// the output feature map is rounded up, 0 means round down, 1 means round up.
    ///
    /// If the input contains the <b>padList</b> parameter:
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: tensor.
    ///
    /// Parameters:
    ///
    /// * <b>kernelSize</b>: kernel size used to obtain the maximum. It is an int array [kernelHeight, kernelWidth].
    /// The first number indicates the kernel height, and the second number indicates the kernel width.
    /// * <b>strides</b> indicates the distance of kernel moving. The value is an int array
    /// [strideHeight, strideWidth]. The first number indicates the moving step in height,
    /// and the second number indicates the moving step in width.
    /// * <b>padList</b>: padding around <b>input</b>. It is an int array [top, bottom, left, right],
    /// and the nearest neighbor values are used for padding.
    /// * <b>activationType</b> is an integer constant which is contained in <b>FuseType</b>.
    /// The specified activation function is called before output.
    /// * <b>global</b> Whether to do global pooling.
    /// * <b>roundMode</b> Boundary handling method. When the pool cannot completely cover the input feature map,
    /// the output feature map is rounded up, 0 means round down, 1 means round up.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: tensor obtained after maximum pooling is applied to the input.
    pub const OH_NN_OPS_MAX_POOL: OH_NN_OperationType = OH_NN_OperationType(21);
    /// Multiplies elements in the same positions of <b>input1</b> and <b>input2</b> to obtain the output.
    /// If <b>input1</b> and <b>input2</b> have different shapes, expand them to the same shape
    /// through broadcast and then perform multiplication.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: <i>n</i>-dimensional tensor.
    /// * <b>input2</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Product of each element of <b>input1</b> and <b>input2</b>.
    pub const OH_NN_OPS_MUL: OH_NN_OperationType = OH_NN_OperationType(22);
    /// Generates a one-hot tensor based on the positions specified by <b>indices</b>. The positions specified by
    /// <b>indices</b> are determined by <b>onValue</b>, and other positions are determined by <b>offValue</b>.
    ///
    /// Inputs:
    ///
    /// * <b>indices</b>: <i>n</i>-dimensional tensor. Each element in <b>indices</b> determines the position of
    /// <b>onValue</b> in each one-hot vector.
    /// * <b>depth</b>: integer scalar that determines the depth of the one-hot vector. The value of <b>depth</b>
    /// must be greater than <b>0</b>.
    /// * <b>onValue</b>: scalar that specifies a valid value in the one-hot vector.
    /// * <b>offValue</b>: scalar that specifies the values of other posistions in the one-hot vector except
    /// the valid value.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: integer scalar that specifies the dimension for inserting the one-hot. Assume that the shape
    /// of <b>indices</b> is [N, C], and the value of <b>depth</b> is D.
    /// When <b>axis</b> is <b>0</b>, the shape of the output is [D, N, C].
    /// When <b>axis</b> is <b>-1</b>, the shape of the output is [N, C, D].
    /// When <b>axis</b> is <b>1</b>, the shape of the output is [N, D, C].
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: (<i>n</i>+1)-dimensional tensor if <b>indices</b> is an <i>n</i>-dimensional tensor.
    /// The output shape is determined by <b>indices</b> and <b>axis</b>.
    pub const OH_NN_OPS_ONE_HOT: OH_NN_OperationType = OH_NN_OperationType(23);
    /// Pads <b>inputX</b> in the specified dimensions.
    ///
    /// Inputs:
    ///
    /// * <b>inputX</b>: <i>n</i>-dimensional tensor in [BatchSize, ...] format.
    /// * <b>paddings</b>: 2D tensor that specifies the length to pad in each dimension. The shape is [n, 2].
    /// For example, <b>paddings[i][0]</b> indicates the number of paddings to be added preceding
    /// <b>inputX</b> in the <i>i</i>th dimension.
    /// <b>paddings[i][1]</b> indicates the number of paddings to be added following <b>inputX</b>
    /// in the <i>i</i>th dimension.
    ///
    /// Parameters:
    ///
    /// * <b>constantValue</b>: value to be added to the pad operation.
    /// The value is a constant with the same data type as <b>inputX</b>.
    /// * <b>paddingMode</b>: Padding mode.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor after padding, with the same dimensions and data type as
    /// <b>inputX</b>. The shape is determined by <b>inputX</b> and <b>paddings</b>.
    /// output.shape[i] = input.shape[i] + paddings[i][0]+paddings[i][1]
    pub const OH_NN_OPS_PAD: OH_NN_OperationType = OH_NN_OperationType(24);
    /// Calculates the <b>y</b> power of each element in <b>input</b>.
    /// The inputs must be two tensors or one tensor and one scalar.
    /// When the inputs are two tensors, their data types cannot be both OH_NN_BOOL, and their shapes must be the same.
    /// When the inputs are one tensor and one scalar, the scalar must be a constant.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>y</b>: real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    ///
    /// Parameters:
    /// * <b>scale</b>: A OH_NN_FLOAT32 scalar that represents the factor of the scale blend.
    /// * <b>shift</b>: A OH_NN_FLOAT32 scalar that represents the bias of the scale blend.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: tensor, whose shape is determined by the shape of <b>input</b> and <b>y</b> after broadcasting.
    pub const OH_NN_OPS_POW: OH_NN_OperationType = OH_NN_OperationType(25);
    /// Scales a tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    /// * <b>scale</b>: scaling tensor.
    /// * <b>bias</b>: bias tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimensions to be scaled.
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: scaled <i>n</i>-dimensional tensor, whose data type is the same as that of <b>input</b> and
    /// shape is determined by <b>axis</b>.
    pub const OH_NN_OPS_SCALE: OH_NN_OperationType = OH_NN_OperationType(26);
    /// Calculates the shape of the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: integer array representing the dimensions of the input tensor.
    pub const OH_NN_OPS_SHAPE: OH_NN_OperationType = OH_NN_OperationType(27);
    /// Applies the <b>sigmoid</b> operation to the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: result of the <b>sigmoid</b> operation. It is an <i>n</i>-dimensional tensor
    /// with the same data type and shape as <b>input</b>.
    pub const OH_NN_OPS_SIGMOID: OH_NN_OperationType = OH_NN_OperationType(28);
    /// Slices a tensor of the specified size from the input in each dimension.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    /// * <b>begin</b>: start of the slice, which is an array of integers greater than or equal to 0.
    /// * <b>size</b>: slice length, which is an array of integers greater than or equal to 0.
    /// Assume that a dimension is <b>i</b> and 1<=size[i]<=input.shape[i]-begin[i].
    ///
    /// Parameters:
    ///
    /// * <b>axes</b>: Dimensions on which the tensor is sliced.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor obtained by slicing.
    /// The <b>TensorType</b>, shape, and size of the output are the same as those of the input.
    pub const OH_NN_OPS_SLICE: OH_NN_OperationType = OH_NN_OperationType(29);
    /// Applies the <b>softmax</b> operation to the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension in which the <b>softmax</b> operation is performed.
    /// The value is of the int64 type. It is an integer in the range [-n, n).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: result of the <b>softmax</b> operation. It is an <i>n</i>-dimensional tensor with
    /// the same data type and shape as <b>input</b>.
    pub const OH_NN_OPS_SOFTMAX: OH_NN_OperationType = OH_NN_OperationType(30);
    /// Divides a 4D tensor into small blocks and combines these blocks in the original batch.
    /// The number of blocks is <b>blockShape[0]</b> multiplied by <b>blockShape[1]</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: 4D tensor.
    ///
    /// Parameters:
    ///
    /// * <b>blockShape</b>: a pair of integers. Each of them is greater than or equal to <b>1</b>.
    /// * <b>paddings</b>: a pair of arrays. Each of them consists of two integers. The four integers that from
    /// <b>paddings</b> must be greater than or equal to <b>0</b>. <b>paddings[0][0]</b> and <b>paddings[0][1]</b>
    /// specify the number of paddings in the third dimension, and <b>paddings[1][0]</b> and <b>paddings[1][1]</b>
    /// specify the number of paddings in the fourth dimension.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: 4D tensor with the same data type as <b>input</b>. The shape is determined by <b>input</b>,
    /// <b>blockShape</b>, and <b>paddings</b>. Assume that the input shape is [n,c,h,w], then:
    /// output.shape[0] = n * blockShape[0] * blockShape[1]
    /// output.shape[1] = c
    /// output.shape[2] = (h + paddings[0][0] + paddings[0][1]) / blockShape[0]
    /// output.shape[3] = (w + paddings[1][0] + paddings[1][1]) / blockShape[1]
    /// (h + paddings[0][0] + paddings[0][1]) and (w + paddings[1][0] + paddings[1][1]) is exactly divisible by
    /// (h + paddings[0][0] + paddings[0][1]) and (w + paddings[1][0] + paddings[1][1]).
    pub const OH_NN_OPS_SPACE_TO_BATCH_ND: OH_NN_OperationType = OH_NN_OperationType(31);
    /// Splits the input into multiple tensors along the axis dimension.
    /// The number of tensors is specified by <b>outputNum</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>outputNum</b>: number of output tensors. The data type is long.
    /// * <b>sizeSplits</b>: size of each tensor split from the input. The value is a 1D tensor of the int type. If
    /// <b>sizeSplits</b> is empty, the input will be evenly split into tensors of the same size. In this case,
    /// <b>input.shape[axis]</b> can be exactly divisible by <b>outputNum</b>.
    /// If <b>sizeSplits</b> is not empty, the sum of all its elements must be equal to <b>input.shape[axis]</b>.
    /// * <b>axis</b>: splitting dimension of the int type.
    ///
    /// Outputs:
    ///
    /// * <b>outputs</b>: array of <i>n</i>-dimensional tensors, with the same data type and dimensions.
    /// The data type of each tensor is the same as that of <b>input</b>.
    pub const OH_NN_OPS_SPLIT: OH_NN_OperationType = OH_NN_OperationType(32);
    /// Calculates the square root of a tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: square root of the input.
    /// It is an <i>n</i>-dimensional tensor with the same data type and shape as <b>input</b>.
    pub const OH_NN_OPS_SQRT: OH_NN_OperationType = OH_NN_OperationType(33);
    /// Calculates the square of the difference between two tensors. The <b>SquaredDifference</b> operator supports
    /// tensor and tensor subtraction. If two tensors have different <b>TensorTypes</b>, the Sub operator
    /// converts the low-precision tensor to a high-precision one. If two tensors have different shapes,
    /// the two tensors can be extended to tensors with the same shape through broadcast.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: minuend, which is a tensor of the OH_NN_FLOAT16, OH_NN_FLOAT32, OH_NN_INT32,
    /// or OH_NN_BOOL type.
    /// * <b>input2</b>: subtrahend, which is a tensor of the OH_NN_FLOAT16, OH_NN_FLOAT32, OH_NN_INT32,
    /// or OH_NN_BOOL type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: square of the difference between two inputs. The output shape is determined
    /// by<b>input1</b> and <b>input2</b>. If they have the same shape, the output tensor has the same
    /// shape as them. If they have different shapes, perform the broadcast operation on
    /// <b>input1</b> and <b>input2</b> and perform subtraction.
    /// <b>TensorType</b> of the output is the same as that of the input tensor with higher precision.
    pub const OH_NN_OPS_SQUARED_DIFFERENCE: OH_NN_OperationType = OH_NN_OperationType(34);
    /// Removes the dimension with a length of 1 from the specified axis. The int8 quantization input is supported.
    /// Assume that the input shape is [2, 1, 1, 2, 2] and axis is [0,1], the output shape is [2, 1, 2, 2],
    /// which means the dimension whose length is 0 between dimensions 0 and dimension 1 is removed.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension to be removed.
    /// The value is of int64_t type and can be an integer in the range [-n, n) or an array.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: output tensor.
    pub const OH_NN_OPS_SQUEEZE: OH_NN_OperationType = OH_NN_OperationType(35);
    /// Stacks multiple tensors along the specified axis. If each tensor has <i>n</i> dimensions before stacking,
    /// the output tensor will have <i>n</i>+1 dimensions.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input for stacking, which can contain multiple <i>n</i>-dimensional tensors.
    /// Each of them must have the same shape and type.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension for tensor stacking, which is an integer. The value range is [-(n+1),(n+1)),
    /// which means a negative number is allowed.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: stacking result of the input along the axis dimension.
    /// The value is an <i>n</i>+1-dimensional tensor and has the same <b>TensorType</b> as the input.
    pub const OH_NN_OPS_STACK: OH_NN_OperationType = OH_NN_OperationType(36);
    /// Slices a tensor with the specified stride.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    /// * <b>begin</b>: start of slicing, which is a 1D tensor. The length of <b>begin</b> is <i>n</i>.
    /// <b>begin[i]</b> specifies the start of slicing in the <i>i</i>th dimension.
    /// * <b>end</b>: end of slicing, which is a 1D tensor. The length of <b>end</b> is <i>n</i>.
    /// <b>end[i]</b> specifies the end of slicing in the <i>i</i>th dimension.
    /// * <b>strides</b>: slicing stride, which is a 1D tensor. The length of <b>strides</b> is <i>n</i>.
    /// strides[i] specifies the stride at which the tensor is sliced in the <i>i</i>th dimension.
    ///
    /// Parameters:
    ///
    /// * <b>beginMask</b>: an integer used to mask <b>begin</b>. <b>beginMask</b> is represented in binary code.
    /// In case of binary(beginMask)[i]==1, for the <i>i</i>th dimension,
    /// elements are sliced from the first element at <b>strides[i]</b> until the end[i]-1 element.
    ///
    /// * <b>endMask</b>: an integer used to mask <b>end</b>. <b>endMask</b> is represented in binary code.
    /// In case of binary(endMask)[i]==1, elements are sliced from the element at the <b>begin[i]</b> position
    /// in the <i>i</i>th dimension until the tensor boundary at <b>strides[i]</b>.
    ///
    /// * <b>ellipsisMask</b>: integer used to mask <b>begin</b> and <b>end</b>.
    /// <b>ellipsisMask</b> is represented in binary code. In case of binary(ellipsisMask)[i]==1,
    /// elements are sliced from the first element at <b>strides[i]</b> in the <i>i</i>th dimension
    /// until the tensor boundary. Only one bit of <b>binary(ellipsisMask)</b> can be a non-zero value.
    ///
    /// * <b>newAxisMask</b>: new dimension, which is an integer. <b>newAxisMask</b> is represented in binary code.
    /// In case of binary(newAxisMask)[i]==1,
    /// a new dimension whose length is 1 is inserted into the <i>i</i>th dimension.
    /// * <b>shrinkAxisMask</b>: shrinking dimension, which is an integer. * <b>shrinkAxisMask</b> is
    /// represented in binary code. In the case of binary(shrinkAxisMask)[i]==1, all elements in the
    /// <i>i</i>th dimension will be discarded, and the length of the <i>i</i>th dimension is shrunk to <b>1</b>.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor, with the same data type as <b>input</b>.
    /// The number of dimensions of the output tensor is rank(input[0])+1.
    pub const OH_NN_OPS_STRIDED_SLICE: OH_NN_OperationType = OH_NN_OperationType(37);
    /// Calculates the difference between two tensors.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: minuend, which is a tensor.
    /// * <b>input2</b>: subtrahend, which is a tensor.
    ///
    /// Parameters:
    ///
    /// * <b>activationType</b> is an integer constant which is contained in <b>OH_NN_FuseType</b>.
    /// The specified activation function is called before output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: difference between the two tensors. The output shape is determined by<b>input1</b> and
    /// <b>input2</b>. If they have the same shape, the output tensor has the same shape as them.
    /// If they have different shapes,
    /// perform the broadcast operation on <b>input1</b> and <b>input2</b> and perform subtraction.
    /// <b>TensorType</b> of the output is the same as that of the input tensor with higher precision.
    pub const OH_NN_OPS_SUB: OH_NN_OperationType = OH_NN_OperationType(38);
    /// Computes hyperbolic tangent of the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: hyperbolic tangent of the input.
    /// The <b>TensorType</b> and tensor shape are the same as those of the input.
    pub const OH_NN_OPS_TANH: OH_NN_OperationType = OH_NN_OperationType(39);
    /// Copies a tensor the specified times.
    ///
    /// Inputs:
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    /// * <b>multiples</b>: number of times that the input tensor is copied in each dimension. The value is a 1D tensor.
    /// The length <i>m</i> is not less than the number of dimensions, that is, <i>n</i>.
    ///
    /// Parameters:
    ///
    /// * <b>dims</b> A 1D tensor that specifies the number of times that data is copied in each dimension.
    /// The length <b>m</b> is not less than the number of dimensions of <b>x</b>.
    ///
    /// Outputs:
    /// * An <i>m</i>-dimensional tensor whose <b>TensorType</b> is the same as that of the input. If <b>input</b> and
    /// <b>multiples</b> have the same length, <b>input</b> and <b>output</b> have the same number of dimensions.
    /// If the length of <b>multiples</b> is greater than <i>n</i>, 1 is used to fill the input dimension, and
    /// then the input is copied in each dimension the specified times to obtain the <i>m</i>-dimensional tensor.
    pub const OH_NN_OPS_TILE: OH_NN_OperationType = OH_NN_OperationType(40);
    /// Transposes data of <b>input</b> based on <b>permutation</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor to be transposed.
    /// * <b>permutation</b>: The value is a 1D tensor whose length is the same as the number of
    /// dimensions of <b>input</b>.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. <b>TensorType</b> of <b>output</b> is the same as that of
    /// <b>input</b>, and the output shape is determined by the shape and <b>permutation</b> of <b>input</b>.
    pub const OH_NN_OPS_TRANSPOSE: OH_NN_OperationType = OH_NN_OperationType(41);
    /// Calculates the average value in the specified dimension.
    /// If <b>keepDims</b> is set to <b>false</b>, the number of dimensions is reduced for the input;
    /// if <b>keepDims</b> is set to <b>true</b>, the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the average value. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    pub const OH_NN_OPS_REDUCE_MEAN: OH_NN_OperationType = OH_NN_OperationType(42);
    /// The Bilinear method is used to deform the input based on the given parameters.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: 4D input tensor. Each element in the input cannot be less than 0.
    /// The input layout must be [batchSize, height, width, channels].
    ///
    /// Parameters:
    ///
    /// * <b>newHeight</b>: resized height of the 4D tensor.
    /// * <b>newWidth</b>: resized width of the 4D tensor.
    /// * <b>preserveAspectRatio</b>: indicates whether to maintain the height/width
    /// ratio of <b>input</b> after resizing.
    /// * <b>coordinateTransformMode</b>: coordinate transformation method used by the resize operation.
    /// The value is an int32 integer. Currently, the following methods are supported:
    /// 0 means ASYMMETRIC, 1 means ALIGN_CORNERS, 2 means HALF_PIXEL.
    /// * <b>excludeOutside</b>: an int64 floating point number. When its value is <b>1</b>,
    /// the sampling weight of the part that
    /// exceeds the boundary of <b>input</b> is set to <b>0</b>, and other weights are normalized.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, with the same shape and data type as <b>input</b>.
    pub const OH_NN_OPS_RESIZE_BILINEAR: OH_NN_OperationType = OH_NN_OperationType(43);
    /// Calculates the reciprocal of the square root of a tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor, where <i>n</i> is less than 8.
    /// Each element of the tensor cannot be less than 0.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, with the same shape and data type as <b>input</b>.
    pub const OH_NN_OPS_RSQRT: OH_NN_OperationType = OH_NN_OperationType(44);
    /// Reshapes a tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    /// * <b>InputShape</b>: shape of the output tensor. The value is a 1D constant tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: tensor whose data type is the same as that of <b>input</b>
    /// and shape is determined by <b>InputShape</b>.
    pub const OH_NN_OPS_RESHAPE: OH_NN_OperationType = OH_NN_OperationType(45);
    /// Calculates the PReLU activation value of <b>input</b> and <b>weight</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor. If <i>n</i> is greater than or equal to 2,
    /// <b>input</b> must be [BatchSize, ..., Channels]. The second dimension is the number of channels.
    /// * <b>weight</b>: 1D tensor. The length of <b>weight</b> must be 1 or equal to the number of channels.
    /// If the length of <b>weight</b> is 1, all channels share the same weight.
    /// If the length of <b>weight</b> is equal to the number of channels, each channel exclusively has a weight.
    /// If <i>n</i> is less than 2 for <b>input</b>, the <b>weight</b> length must be 1.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: PReLU activation value of <b>input</b>, with the same shape and data type as <b>input</b>.
    pub const OH_NN_OPS_PRELU: OH_NN_OperationType = OH_NN_OperationType(46);
    /// Calculates the Relu activation value of <b>input</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, with the same data type and shape as the input tensor.
    pub const OH_NN_OPS_RELU: OH_NN_OperationType = OH_NN_OperationType(47);
    /// Calculates the Relu6 activation value of the input, that is,
    /// calculate min(max(x, 0), 6) for each element x in the input.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional Relu6 tensor, with the same data type and shape as the input tensor.
    pub const OH_NN_OPS_RELU6: OH_NN_OperationType = OH_NN_OperationType(48);
    /// Applies layer normalization for a tensor from the specified axis.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    /// * <b>gamma</b>: <i>m</i>-dimensional tensor. The dimensions of <b>gamma</b> must be the same as
    /// the shape of the part of the input tensor to normalize.
    /// * <b>beta</b>: <i>m</i>-dimensional tensor with the same shape as <b>gamma</b>.
    ///
    /// Parameters:
    ///
    /// * <b>beginAxis</b>: an OH_NN_INT32 scalar that specifies the axis from which normalization starts.
    /// The value range is [1, rank(input)).
    /// * <b>epsilon</b>: a scalar of OH_NN_FLOAT32. It is a tiny amount in the normalization formula.
    /// The common value is 0.00001f.
    /// * <b>beginParamsAxis</b>: an OH_NN_INT32 scalar that specifies the start axis of layer normalization
    /// of input parameter (gamma, beta).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, with the same data type and shape as the input tensor.
    pub const OH_NN_OPS_LAYER_NORM: OH_NN_OperationType = OH_NN_OperationType(49);
    /// Calculates the accumulated value for a tensor along the specified dimension. If <b>keepDims</b> is set to
    /// <b>false</b>, the number of dimensions is reduced for the input; if <b>keepDims</b> is set to <b>true</b>,
    /// the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the product. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    pub const OH_NN_OPS_REDUCE_PROD: OH_NN_OperationType = OH_NN_OperationType(50);
    /// Calculates the logical and value for input tensor along the specified dimension. If <b>keepDims</b> is set to
    /// <b>false</b>, the number of dimensions is reduced for the input; if <b>keepDims</b> is set to <b>true</b>,
    /// the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the logical and value. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    pub const OH_NN_OPS_REDUCE_ALL: OH_NN_OperationType = OH_NN_OperationType(51);
    /// Converts the data type.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor. If it is a conversion between a quantized type and
    /// a floating-point type, the input tensor should contain quantized parameters.
    ///
    /// Parameters:
    ///
    /// * <b>srcT</b>: data type of the input.
    /// * <b>dstT</b>: data type of the output.
    /// * <b>axis</b>: appoint the dimensions from which the quantization parameters are extracted.
    /// If the size of the input tensor quantization parameter is 1, the operator function is
    /// layer quantization conversion, and this parameter does not take effect. If the size of
    /// the input tensor quantization parameter is greater than 1, the operator function is the
    /// quantization conversion along the specific channels, and this parameter takes effect.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The data type is determined by <b>dstT</b>.
    /// The output shape is the same as the input shape.
    pub const OH_NN_OPS_QUANT_DTYPE_CAST: OH_NN_OperationType = OH_NN_OperationType(52);
    /// Obtains the values and indices of the largest <i>k</i> entries in the last dimension.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    /// * <b>k</b>: first <i>k</i> records of data and their indices.
    ///
    /// Parameters:
    ///
    /// * <b>sorted</b>: order of sorting. The value <b>true</b> means descending and <b>false</b> means ascending.
    /// * <b>axis</b>: A OH_NN_INT32 scalar that specifies the dimension that needs to be sorted, default -1,
    /// pointing to the last dimension.
    ///
    /// Outputs:
    ///
    /// * <b>output0</b>: largest <i>k</i> elements in each slice of the last dimension.
    /// * <b>output1</b>: index of the value in the last dimension of the input.
    pub const OH_NN_OPS_TOP_K: OH_NN_OperationType = OH_NN_OperationType(53);
    /// Returns the index of the maximum tensor value across axes.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor (N, ∗), where ∗ means any number of additional dimensions.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension for calculating the index of the maximum.
    /// * <b>keepDims</b>: indicates whether to maintain the input tensor dimension. The value is a Boolean value.
    /// * <b>topK</b>: Whether to keep the output dimensions the same as the input dimensions.
    /// * <b>outMaxValue</b>: Return the index if the value is <b>false</b>.
    /// Return the value if the value is <b>true</b>. The default value is <b>false</b>.
    ///
    /// Outputs:
    /// * <b>output</b>: index of the maximum input tensor on the axis. The value is a tensor.
    pub const OH_NN_OPS_ARG_MAX: OH_NN_OperationType = OH_NN_OperationType(54);
    /// Adds a dimension based on the value of <b>axis</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension to be added. The value of <b>axis</b> can be an integer or an array of integers.
    /// The value range of the integer is [-n, n).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: output tensor.
    pub const OH_NN_OPS_UNSQUEEZE: OH_NN_OperationType = OH_NN_OperationType(55);
    /// Gaussian error linear unit activation function. The int quantization input is not supported.
    /// output=0.5∗input∗(1+tanh(input/2))
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: An <i>n</i>-dimensional input tensor.
    ///
    /// Parameters:
    /// * <b>approximate</b>: Whether to use the approximation algorithm.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, with the same data type and shape as the input tensor.
    pub const OH_NN_OPS_GELU: OH_NN_OperationType = OH_NN_OperationType(56);
    /// Unpacks the input tensors base on the given dimension of axis.
    /// Unpacks tensors from <b>input</b> by chipping it along the <b>axis</b> dimension.
    /// For example, given a tensor of shape (A, B, C, D);
    /// If axis == 0, then the i'th tensor in output is the slice value[i, :, :, :],
    ///
    /// and each tensor in output will have shape (B, C, D).
    /// If axis == 1, then the i'th tensor in output is the slice value[:, i, :, :],
    ///
    /// and each tensor in output will have shape (A, C, D). Etc.
    /// This is the opposite of <b>OH_NN_OPS_STACK</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: dimension along witch to unpack. Default 0. The range is [-n, n).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tuple of tensors, the shape of each objects is same.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_UNSTACK: OH_NN_OperationType = OH_NN_OperationType(57);
    /// Obtains the absolute value of the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The absolute value of the input tensor.
    /// The shape and data type is the same as inputs'.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_ABS: OH_NN_OperationType = OH_NN_OperationType(58);
    /// Computes the Gauss error function of input element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor. The dimension should be less than 8,
    /// and the data type only support OH_NN_FLOAT32 and OH_NN_FLOAT16.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The shape and data type is the same as inputs'.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_ERF: OH_NN_OperationType = OH_NN_OperationType(59);
    /// Calculates the exponential of the given input tensor element-wise.
    /// ExpFusion computes outputs by formula <b>output = base ^ (shift + scale * input)</b>, for base > 0.
    /// And the base is default set to -1, which means nature logarithm 'e',
    /// and the calculate formula changes to <b>output = exp(shift + scale * input)</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>base</b>: The base of exponential function. Default set to -1 representing nature logarithm 'e'.
    /// Input value must be > 0.
    /// * <b>scale</b>: The amplifcation factor of exponential value, default 1.
    /// * <b>shift</b>: The offset of exponential value, default 0.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The element-wise exponential result of the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_EXP: OH_NN_OperationType = OH_NN_OperationType(60);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i] < input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>: can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used, the quantization
    /// parameters of the output cannot be omitted. However, values of the quantization parameters do not
    /// affect the result.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LESS: OH_NN_OperationType = OH_NN_OperationType(61);
    /// Selects output elements from input1 or input2, depending on condition.
    /// If condition is true, choose elements from input1. Otherwise, choose elements from input2 if condition is false.
    /// The three inputs, <b>condition</b> , <b>input1</b> and <b>input2</b> must share the same shape.
    ///
    /// Inputs:
    ///
    /// * <b>condition</b>: <i>n</i>-dimensional tensor or scalar.
    /// The condition tensor, decides which element is chosen.
    /// * <b>input1</b>: <i>n</i>-dimensional tensor. First input tensor to be chosen.
    /// If condition is rank 1, input1 may have higher rank, but its first dimension must match the
    /// size of condition.
    /// * <b>input2</b>: <i>n</i>-dimensional tensor. Second input tensor to be chosen.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor, has the same shape and data type as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SELECT: OH_NN_OperationType = OH_NN_OperationType(62);
    /// Calculates the square of input tensor element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor, has the same shape and dtype as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SQUARE: OH_NN_OperationType = OH_NN_OperationType(63);
    /// Flattens the input tensor into a 2D matrix. If input tensor has shape (d_0, d_1, … d_n),
    /// then the output will have shape (d_0 X d_1 … d_(axis-1), d_axis X d_(axis+1) … X dn).
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor. The rank of input should be greater or equal to axis.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: Indicate up to which input dimensions (exclusive) should be flattened to the outer dimension
    /// of the output. The value for axis must be in the range [-r, r], where r is the rank of the input tensor.
    /// Negative value means counting dimensions from the back. When axis = 0, the shape of the output tensor is
    /// (1, (d_0 X d_1 … d_n)), where the shape of the input tensor is (d_0, d_1, … d_n).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: 2-dimensional tensor after flattened.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_FLATTEN: OH_NN_OperationType = OH_NN_OperationType(64);
    /// DepthToSpace rearranges (permutes) data from depth into blocks of spatial data.
    /// This is the reverse transformation of SpaceToDepth. More specifically, this op outputsa copy of the input tensor
    /// where values from the depth dimension are moved in spatial blocks to the height and width dimensions.
    /// By default, mode = DCR. In the DCR mode, elements along the depth dimension from the input tensor are rearranged
    /// in the following order: depth, column, and then row.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: 4-dimensional tensor with specific format of NHWC or NCHW.
    /// where N is the batch axis, H is the height, W is the width and C is the channel or depth.
    ///
    /// Parameters:
    ///
    /// * <b>blockSize</b>: Blocks of [blocksize, blocksize] are moved.
    /// * <b>mode</b>: DCR (default) for depth-column-row order re-arrangement. Use CRD for column-row-depth order.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Output tensor of [N, H * blocksize, W * blocksize, C/(blocksize * blocksize)] for NHWC format
    /// or [N, C/(blocksize * blocksize), H * blocksize, W * blocksize] for NCHW format.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_DEPTH_TO_SPACE: OH_NN_OperationType = OH_NN_OperationType(65);
    /// Generate a tensor containing a sequence of numbers that begin at <b>start</b>
    ///
    /// and extends by increments of <b>delta</b> up to <b>limit<b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>start</b>: Scalar. First entry for the range of output values.
    /// * <b>limit</b>: Scalar. Exclusive upper limit for the range of output values.
    /// * <b>delta</b>: Scalar. Value to step by.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>1</i>-dimensional tensor with specific data type containing generated range of values.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_RANGE: OH_NN_OperationType = OH_NN_OperationType(66);
    /// Normalize each channel of the input. Make the mean of each channel of the input is 0 and the variance is 1.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: Input data tensor from the previous operator; dimensions for image case are (N x C x H x W),
    /// where N is the batch size, C is the number of channels, and H and W are the height and the width of
    /// the data. For non image case, the dimensions are in the form of (N x C x D1 x D2 … Dn), where N is the
    /// batch size.
    /// * <b>scale</b>: The input 1-dimensional scale tensor of channel size.
    /// * <b>bias</b>: The input 1-dimensional bias tensor of channel size.
    ///
    /// Parameters:
    ///
    /// * <b>epsilon</b>: The epsilon value to use to avoid division by zero.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: The output tensor of the same shape as input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_INSTANCE_NORM: OH_NN_OperationType = OH_NN_OperationType(67);
    /// Generate a tensor with given value and shape.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>1</i>-dimensional tensor. Indicates the shape of the expected output tensor.
    /// All values must be >= 0.
    ///
    /// Parameters:
    ///
    /// * <b>dataType</b>: The data type of the output tensor.
    /// * <b>value</b>: The value of the output elements.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor, has the same shape as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_CONSTANT_OF_SHAPE: OH_NN_OperationType = OH_NN_OperationType(68);
    /// Broadcast a tensor for a compatiable shape.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>shape</b>: A 1-dimensional Tensor, the shape of the desired output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor after broadcasted.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_BROADCAST_TO: OH_NN_OperationType = OH_NN_OperationType(69);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i] = input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>, can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>, can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used,
    /// the quantization output cannot be omitted. However, values of the quantization
    /// parameters do not affect the result.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_EQUAL: OH_NN_OperationType = OH_NN_OperationType(70);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i] > input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>, can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>, can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used,
    /// the quantization parameters of the output cannot be omitted. However,
    /// values of the quantization parameters do not affect the result.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_GREATER: OH_NN_OperationType = OH_NN_OperationType(71);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i] != input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>, can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>, can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used,
    /// the quantization parameters of the output cannot be omitted. However,
    /// values of the quantization parameters do not affect the result.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_NOT_EQUAL: OH_NN_OperationType = OH_NN_OperationType(72);
    /// For <b>input1</b> and <b>input2</b>, calculate the result of input1[i] >= input2[i] for each pair of elements,
    /// where i is the index of each element in the input tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>, can be a real number, Boolean value, or tensor whose data type is real number or OH_NN_BOOL.
    /// * <b>input2</b>, can be a real number or a Boolean value if <b>input1</b> is a tensor and must be a tensor
    /// with the data type of real number or OH_NN_BOOL if <b>input1</b> is not a tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor of the data type OH_NN_BOOL. When a quantization model is used,
    /// the quantization parameters of the output cannot be omitted. However,
    /// values of the quantization parameters do not affect the result.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_GREATER_EQUAL: OH_NN_OperationType = OH_NN_OperationType(73);
    /// LeakyRelu takes input data (Tensor) and an argument alpha, and produces one output data (Tensor)
    /// where the function f(x) = alpha * x for x < 0, f(x) = x for x >= 0,
    /// is applied to the data tensor elementwise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor.
    ///
    /// Parameters:
    ///
    /// * <b>negativeSlope</b>: Coefficient of leakage.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor, with the same data type and shape as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LEAKY_RELU: OH_NN_OperationType = OH_NN_OperationType(74);
    /// Computes an one-layer LSTM. This operator is usually supported via some custom implementation.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor, shape is [seqLen, batchSize, inputSize].
    /// * <b>wIh</b>: Weight tensor of input-layer to hidden-layer,
    /// shape is [numDirections* numLayers, 4 * hiddenSize, inputSize].
    /// * <b>wHh</b>: Weight tensor of hidden-layer to hidden-layer,
    /// shape is [numDirections* numLayers, 4 * hiddenSize, hiddenSize].
    /// * <b>bias</b>: Bias tensor of input-layer and hidden-layer to hidden-layer,
    /// shape is [numDirections* numLayers, 8 * hiddenSize].
    /// * <b>hx</b>: Init state of hidden-layer, shape is [numDirections * numLayers, batchSize, hiddenSize].
    /// * <b>cx</b>: Init state of cell, shape is [numDirections * numLayers, batchSize, hiddenSize].
    ///
    /// Parameters:
    ///
    /// * <b>bidirectional</b>: Whether the LSTM operation is bidirectional.
    /// * <b>hasBias</b>: Whether the operation contains bias.
    /// * <b>inputSize</b>: Size of input tensor.
    /// * <b>hiddenSize</b>: Size of hidden state tensor.
    /// * <b>numLayers</b>: Layers of LSTM network.
    /// * <b>numDirections</b>: Number of directions, value is 2 if direction == bidirectional else 1.
    /// * <b>dropout</b>: Dropout probalility of each layer except first-layer.
    /// * <b>zoneoutCell</b>: Probalility that the cell state retains the previous state. Default: 0.
    /// * <b>zoneoutHidden</b>: Probalility that the hidden state retains the previous state. Default: 0.
    /// * <b>projSize</b>: If projSize > 0, will use LSTM with projections of corresponding size. Default: 0.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor that concats all the intermediate output tensor of the hidden,
    /// shape is [seqLen, batchSize, numDirections * realHiddenSize].
    /// * <b>hy</b>: The last output tensor of the hidden-layer,
    /// shape is [numDirections * numLayers, batchSize, realHiddenSize].
    /// * <b>cy</b>: The last output tensor of the cell,
    /// shape is [numDirections * numLayers, batchSize, hiddenSize].
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LSTM: OH_NN_OperationType = OH_NN_OperationType(75);
    /// Returns a tensor of the same type and shape as input tensor with its value clipped to min and max.
    /// Any values less than <b>min</b> are set to <b>min</b>. Any values greater than <b>max</b> are set to <b>max</b>.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>max</b>: Maximum value, above which element is replaced by max. It must be a scalar(tensor of empty shape).
    /// * <b>min</b>: Minimum value, under which element is replaced by min. It must be a scalar(tensor of empty shape).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor., with the same data type and shape as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_CLIP: OH_NN_OperationType = OH_NN_OperationType(76);
    /// Determine whether all emements in a given tensor are non-zero. It returns a boolean tensor
    /// where each element is 'True' if corresponding element in the input tensor is non-zero, and 'False' otherwise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor of shape <b>(N,*)</b>,
    /// where * indicates any number of additional dimensions.
    /// * <b>aixs</b>: scalar or tensor, indices the dimension to be computed.
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: Whether to keep dimension info.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: 1-dimension or n-dimension tensor with boolean data type.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_ALL: OH_NN_OperationType = OH_NN_OperationType(77);
    /// Asserts that the given condition si true.
    /// If <b>condition</b> evalutes to false, print the list of tensors in data.
    /// Summerize determines how many entries of the tensors to print.
    ///
    /// Inputs:
    ///
    /// * <b>condition</b>: The condition to evalute.
    /// * <b>data</b>: The tensors to print out when condition is false.
    ///
    /// Parameters:
    ///
    /// * <b>summarize</b>: The number of entries for each tensor is printed.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result value judged by condition. If the condition is not true, an Error is returned.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_ASSERT: OH_NN_OperationType = OH_NN_OperationType(78);
    /// Calculates the cosine of the given input tensor, element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The cosine of the input tensor computed element-wise.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_COS: OH_NN_OperationType = OH_NN_OperationType(79);
    /// Calculates the result of nature logarithm of the input.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor. The value must be greater than 0.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor with the same shape as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LOG: OH_NN_OperationType = OH_NN_OperationType(80);
    /// Calculates the logical value of <b>input1</b> and <b>input2</b> element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: Tensor of type boolean or convert to boolean implicitly.
    /// * <b>input2</b>: Tensor of type boolean or convert to boolean implicitly.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The calculation result of logical-and
    /// and the numeric type is OH_NN_BOOL.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LOGICAL_AND: OH_NN_OperationType = OH_NN_OperationType(81);
    /// Calculates the logical value of NOT <b>input</b> element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: Tensor of type boolean or convert to boolean implicitly.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The calculation result of logical-not
    /// and the numeric type is OH_NN_BOOL.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LOGICAL_NOT: OH_NN_OperationType = OH_NN_OperationType(82);
    /// Computes the remainder of dividing the first input tensor by the second input tensor element-wise.
    /// Inputs of input1 and input2 comply with the implicit type conversion rules to make the data types consistent.
    /// The inputs must be two tensors or one tensor and one scalar. When the inputs are two tensors,
    /// both dtypes cannot be bool, and the shapes of them could be broadcast.
    /// When the inputs are one tensor and one scalar, the scalar could only be a constant.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: The remainder of the scalar or tensor, numeric or OH_NN_BOOL type,
    /// or the <i>n</i>-dimensional tensor of the numeric dimension numeric type.
    /// * <b>input2</b>: Remainder factor. When the first input is an n-dimensional tensor,
    /// the second input can be a numeric tensor, a OH_NN_BOOL type, or an n-dimensional
    /// tensor of a numeric type dimension, and when the first input is a numeric or OH_NN_BOOL tensor,
    /// the second input must be a tensor of the numeric dimension of the data type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor. The shape is the same as the input after broadcasting,
    /// and the data type is the data type with the highest accuracy of the two inputs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_MOD: OH_NN_OperationType = OH_NN_OperationType(83);
    /// Calculate the opposite value of the input tensor element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor with numeric data type。
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor with the same shape and data type as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_NEG: OH_NN_OperationType = OH_NN_OperationType(84);
    /// Calculate reciprocal of a tensor element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor with the same shape and data type as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_RECIPROCAL: OH_NN_OperationType = OH_NN_OperationType(85);
    /// Calculate sine of the input element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor the same data type and shape as the input tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SIN: OH_NN_OperationType = OH_NN_OperationType(86);
    /// Selects elements from input1 or input2 based on condition and returns a tensor.
    ///
    /// Inputs:
    ///
    /// * <b>condition</b>: <i>n</i>-dimensional tensor or scalar. Judging conditions. If the OH_NN_BOOL element
    /// is True, then the element corresponding to the position of input1 is selected, and if the OH_NN_BOOL
    /// element is False, the element corresponding to the position of input2 is selected.
    /// * <b>input1</b>: <i>n</i>-dimensional tensor. First tensor to be chosen.
    /// * <b>input2</b>: <i>n</i>-dimensional tensor. Second tensor to be chosen.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>-dimensional tensor with the same shape and data type as the input1 and input2.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_WHERE: OH_NN_OperationType = OH_NN_OperationType(87);
    /// Converts a sparse tensor into a dense tensor.
    ///
    /// Inputs:
    ///
    /// * <b>indices</b>: 2-dimensional tensor. Position of an ellement in a sparse tensor.
    /// Each element value must be non-negative. The shape is (N, 2).
    /// * <b>values</b>: 1-dimensional tensor. The value corresponding to the location of indices. The shape is (N).
    /// * <b>sparseShape</b>: 2-dimensional tensor. The shape of a sparse tensor. The value consists of
    /// two positive integers, indicating that the shape of the sparse tensor is (N, C).
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor. The data type is the same as values, and the shape is specified by sparseShape.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SPARSE_TO_DENSE: OH_NN_OperationType = OH_NN_OperationType(88);
    /// Calculates the logical value of <b>input1</b> or <b>input2</b> element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: Tensor of type boolean or convert to boolean implicitly.
    /// * <b>input2</b>: Tensor of type boolean or convert to boolean implicitly.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>n</i>--dimensional tensor. The calculation result of logical-or
    /// and the numeric type is OH_NN_BOOL.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LOGICAL_OR: OH_NN_OperationType = OH_NN_OperationType(89);
    /// Returns element-wise smallest integer in not less than input.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor after ceiled.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_CEIL: OH_NN_OperationType = OH_NN_OperationType(90);
    /// Crop given tensor acrodding to axis and offset.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    /// * <b>shape</b>: <i>1</i>-dimensional tensor, indices cropped windows dimension.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: Cropped dimension.
    /// * <b>offset</b>: Cropped offset per dimension.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Cropped output tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_CROP: OH_NN_OperationType = OH_NN_OperationType(91);
    /// The output of the object detection model is post-processed, including decoding the bounding box,
    /// class probability and score of the model output, and then performing non-maximum suppression (NMS)
    /// to remove the overlapping bounding box, and finally outputting the detection result.
    ///
    /// Inputs:
    ///
    /// * <b>bbox</b>: Boxes to be predicted.
    /// * <b>scores</b>: Socres of all boxes.
    /// * <b>anchors</b>: Information of boxes, includes box, variance and coordinates.
    ///
    /// Parameters:
    /// * <b>inputSize</b>: The size of the input tensor.
    /// * <b>scale</b>: The scaling factor used to convert the output from
    /// the normalized form to the original image coordinates.
    /// * <b>nmsIoUThreshold</b>: The threshold of overlapping region during NMS.
    /// * <b>nmsScoreThreshold</b>: The socre threshold used to select target bbox duing NMS.
    /// * <b>maxDetections</b>: Maximum of bboxes per image.
    /// * <b>detectionsPerClass</b>: Maximum of bboxes per class.
    /// * <b>maxClassesPerDetection</b>: Maximum of reserved classes per bboxes.
    /// * <b>numClasses</b>: Number of target classes to be detected.
    /// * <b>useRegularNms</b>: Whether use NMS based on IoU threshold.
    /// * <b>outQuantized</b>: Whether need to quantize.
    ///
    /// Outputs:
    ///
    /// * <b>bboxes</b>: The corrdinates of target detected bboxes.
    /// * <b>classes</b>: The target class index of target detected bboxes.
    /// * <b>confidences</b>: The score of target detected bboxes.
    /// * <b>numDetections</b>: The number of target detected bboxes.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_DETECTION_POST_PROCESS: OH_NN_OperationType = OH_NN_OperationType(92);
    /// Returns element-wise largest integer not greater than x.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: A tensor after floored.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_FLOOR: OH_NN_OperationType = OH_NN_OperationType(93);
    /// Calculate the L2-normalize of the input using the given axis.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: Input to compute the L2-normalization.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: The axis on which to apply normalization, -1 means last axis, default: 0.
    /// * <b>epsilon</b>: Value added for numerical stability. default: 1e-6;
    /// * <b>activationType</b>: Activation function type.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same type and shape as input <b>input</b>.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_L2_NORMALIZE: OH_NN_OperationType = OH_NN_OperationType(94);
    /// Computes the log-softmax function to n-dimensional input tensor.
    /// The input is transformed by the Softmax function and then by the log function to lie in range[-inf,0).
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>axis</b>: The axis to apply LogSoftmax operation, -1 means the last dimension.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Tensor output. Has the same data type and shape as input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LOG_SOFTMAX: OH_NN_OperationType = OH_NN_OperationType(95);
    /// Normalize over local input regions.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>depthRadius</b>: Half-width of the 1-dimension normalization window.
    /// * <b>bias</b>: Offset.
    /// * <b>alpha</b>: Scale factor.
    /// * <b>beta</b>: Exponent.
    /// * <b>normRegion</b>: Specifies normalization region. Options: "ACROSS_CHNNEL".
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result output tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_LRN: OH_NN_OperationType = OH_NN_OperationType(96);
    /// Calculates the minimum of <b>input1</b> and <b>input2</b> element-wise. The inputs of <b>input1</b> and
    /// <b>input2</b> comply with the implicit type conversion rules to make the data types are consistent.
    ///
    /// The input must be two tensors or one tensor and one scalar. When the input is two tensors, the data types
    /// cannot be Boolean at the same time, and their shapes can be broadcast to the same size. When the inputs are
    /// one tensor and one scalar, the scalar must be a constant.
    ///
    /// Inputs:
    ///
    /// * <b>input1</b>: <i>n</i>-dimensional tensor, whose data type can be number or Boolean.
    /// * <b>input2</b>: <i>n</i>-dimensional tensor, whose data type can be number or Boolean.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Minimum value of the elements of the two tensors.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_MINIMUM: OH_NN_OperationType = OH_NN_OperationType(97);
    /// Calculate the rank of a tensor.
    /// The rank of a tensor is the number of indices required to uniquely select each element of the tensor.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor. 0-D int32 Tensor representing the rank of input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_RANK: OH_NN_OperationType = OH_NN_OperationType(98);
    /// Calculates the maximum value for input tensor along the specified dimension. If <b>keepDims</b> is set to
    /// <b>false</b>, the number of dimensions is reduced for the input; if <b>keepDims</b> is set to <b>true</b>,
    /// the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the maximum value. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_REDUCE_MAX: OH_NN_OperationType = OH_NN_OperationType(99);
    /// Calculates the minimum value for input tensor along the specified dimension. If <b>keepDims</b> is set to
    /// <b>false</b>, the number of dimensions is reduced for the input; if <b>keepDims</b> is set to <b>true</b>,
    /// the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the minimum value. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_REDUCE_MIN: OH_NN_OperationType = OH_NN_OperationType(100);
    /// Calculates the numerical sum value for input tensor along the specified dimension. If <b>keepDims</b> is set to
    /// <b>false</b>, the number of dimensions is reduced for the input; if <b>keepDims</b> is set to <b>true</b>,
    /// the number of dimensions is retained.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional input tensor, where <i>n</i> is less than 8.
    /// * <b>axis</b>: dimension used to calculate the sum value. The value is a 1D tensor.
    /// The value range of each element in <b>axis</b> is [–n, n).
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: <i>m</i>-dimensional output tensor whose data type is the same as that of the input.
    /// If <b>keepDims</b> is <b>false</b>, m<n. If <b>keepDims</b> is <b>true</b>, m==n.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_REDUCE_SUM: OH_NN_OperationType = OH_NN_OperationType(101);
    /// Calculate half to even of a tensor element-wise.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same shape as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_ROUND: OH_NN_OperationType = OH_NN_OperationType(102);
    /// Scatters a tensor into a new tensor depending on the specified indices.
    ///
    /// Inputs:
    ///
    /// * <b>indices</b>: The index of scattering in the new tensor with int32 or int64 data type.
    /// The rank of indices must be at least 2 and indicesShape[-1] <= len(shape).
    /// * <b>updates</b>: The source tensor to be scattered. It has shape indicesShape[:-1]+shape[indicesShape[-1]:].
    /// * <b>shape</b>: The shape of the output tensor, has the same data type as <b>indices</b>.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same type as <b>update</b> and the same shape as <b>shape</b>.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SCATTER_ND: OH_NN_OperationType = OH_NN_OperationType(103);
    /// Rearrange blocks of spatial data into depth.
    /// The output tensor’s height dimension is height / blocksize;
    /// The output tensor’s weight dimension is weight / blocksize;
    /// The depth of output tensor is blocksize * blocksize * inputDepth;
    /// The input tensor’s height and width must be divisible by blocksize.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>4</i>-dimensional tensor.
    ///
    /// Parameters:
    ///
    /// * <b>blocksize</b>: The block size used to divide spatial data. It must be >= 2.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same dataType as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SPACE_TO_DEPTH: OH_NN_OperationType = OH_NN_OperationType(104);
    /// Swish activation function
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Output tensor.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_SWISH: OH_NN_OperationType = OH_NN_OperationType(105);
    /// Calculates the L2 norm of the input tensor along the specified axis,
    /// replacing other elements of the dimension with the L2 norm value of the specified dimension to
    /// remove the dimension, or to reduce the dimension size to 1. Control whether the dimensions of the
    /// output and input are the same by specifying the keepDims parameter.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: input tensor.
    /// * <b>axis</b>: Dimensions to perform L2-Norm calculations.
    ///
    /// Parameters:
    ///
    /// * <b>keepDims</b>: indicates whether to retain the dimension. The value is a Boolean value.
    /// * <b>reduceToEnd</b>: boolean value, indicates whether the reduce operation needs to be performed
    /// until the last axis.
    /// * <b>coeff</b>: A OH_NN_FLOAT32 scalar that represents the scale factor of the output.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same dataType as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_REDUCE_L2: OH_NN_OperationType = OH_NN_OperationType(106);
    /// HardSigmoid activation function. Calculate the output by element.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same shape and dataType as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_HARD_SIGMOID: OH_NN_OperationType = OH_NN_OperationType(107);
    /// Gets the element at the location specified by the input tensor according to the index.
    ///
    /// Inputs:
    ///
    /// * <b>input</b>: <i>n</i>-dimensional tensor.
    /// * <b>indices</b>: index tensor.
    ///
    /// Outputs:
    ///
    /// * <b>output</b>: Result tensor with the same shape as the input.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_OPS_GATHER_ND: OH_NN_OperationType = OH_NN_OperationType(108);
}
#[repr(transparent)]
/// Defines operator types.
///
///
/// Available since API-level: 9
///
/// Version: 2.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_OperationType(pub ::core::ffi::c_uint);
impl OH_NN_TensorType {
    /// This enumerated value is used when the tensor is used as the input or output of a model (or operator).
    pub const OH_NN_TENSOR: OH_NN_TensorType = OH_NN_TensorType(0);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Add operator.
    pub const OH_NN_ADD_ACTIVATIONTYPE: OH_NN_TensorType = OH_NN_TensorType(1);
    /// This enumerated value is used when the tensor is used as the <b>kernelSize</b> parameter
    /// of the AvgPool operator.
    pub const OH_NN_AVG_POOL_KERNEL_SIZE: OH_NN_TensorType = OH_NN_TensorType(2);
    /// This enumerated value is used when the tensor is used as the <b>stride</b> parameter
    /// of the AvgPool operator.
    pub const OH_NN_AVG_POOL_STRIDE: OH_NN_TensorType = OH_NN_TensorType(3);
    /// This enumerated value is used when the tensor is used as the <b>padMode</b> parameter
    /// of the AvgPool operator.
    pub const OH_NN_AVG_POOL_PAD_MODE: OH_NN_TensorType = OH_NN_TensorType(4);
    /// This enumerated value is used when the tensor is used as the <b>pad</b> parameter of the AvgPool operator.
    pub const OH_NN_AVG_POOL_PAD: OH_NN_TensorType = OH_NN_TensorType(5);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the AvgPool operator.
    pub const OH_NN_AVG_POOL_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(6);
    /// This enumerated value is used when the tensor is used as the <b>eosilon</b> parameter
    /// of the BatchNorm operator.
    pub const OH_NN_BATCH_NORM_EPSILON: OH_NN_TensorType = OH_NN_TensorType(7);
    /// This enumerated value is used when the tensor is used as the <b>blockSize</b> parameter
    /// of the BatchToSpaceND operator.
    pub const OH_NN_BATCH_TO_SPACE_ND_BLOCKSIZE: OH_NN_TensorType = OH_NN_TensorType(8);
    /// This enumerated value is used when the tensor is used as the <b>crops</b> parameter
    /// of the BatchToSpaceND operator.
    pub const OH_NN_BATCH_TO_SPACE_ND_CROPS: OH_NN_TensorType = OH_NN_TensorType(9);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the Concat operator.
    pub const OH_NN_CONCAT_AXIS: OH_NN_TensorType = OH_NN_TensorType(10);
    /// This enumerated value is used when the tensor is used as the <b>strides</b> parameter
    /// of the Conv2D operator.
    pub const OH_NN_CONV2D_STRIDES: OH_NN_TensorType = OH_NN_TensorType(11);
    /// This enumerated value is used when the tensor is used as the <b>pad</b> parameter of the Conv2D operator.
    pub const OH_NN_CONV2D_PAD: OH_NN_TensorType = OH_NN_TensorType(12);
    /// This enumerated value is used when the tensor is used as the <b>dilation</b> parameter
    /// of the Conv2D operator.
    pub const OH_NN_CONV2D_DILATION: OH_NN_TensorType = OH_NN_TensorType(13);
    /// This enumerated value is used when the tensor is used as the <b>padMode</b> parameter
    /// of the Conv2D operator.
    pub const OH_NN_CONV2D_PAD_MODE: OH_NN_TensorType = OH_NN_TensorType(14);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Conv2D operator.
    pub const OH_NN_CONV2D_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(15);
    /// This enumerated value is used when the tensor is used as the <b>group</b> parameter of the Conv2D operator.
    pub const OH_NN_CONV2D_GROUP: OH_NN_TensorType = OH_NN_TensorType(16);
    /// This enumerated value is used when the tensor is used as the <b>strides</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_STRIDES: OH_NN_TensorType = OH_NN_TensorType(17);
    /// This enumerated value is used when the tensor is used as the <b>pad</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_PAD: OH_NN_TensorType = OH_NN_TensorType(18);
    /// This enumerated value is used when the tensor is used as the <b>dilation</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_DILATION: OH_NN_TensorType = OH_NN_TensorType(19);
    /// This enumerated value is used when the tensor is used as the <b>outputPaddings</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_OUTPUT_PADDINGS: OH_NN_TensorType = OH_NN_TensorType(20);
    /// This enumerated value is used when the tensor is used as the <b>padMode</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_PAD_MODE: OH_NN_TensorType = OH_NN_TensorType(21);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(22);
    /// This enumerated value is used when the tensor is used as the <b>group</b> parameter
    /// of the Conv2DTranspose operator.
    pub const OH_NN_CONV2D_TRANSPOSE_GROUP: OH_NN_TensorType = OH_NN_TensorType(23);
    /// This enumerated value is used when the tensor is used as the <b>strides</b> parameter
    /// of the DepthwiseConv2dNative operator.
    pub const OH_NN_DEPTHWISE_CONV2D_NATIVE_STRIDES: OH_NN_TensorType = OH_NN_TensorType(24);
    /// This enumerated value is used when the tensor is used as the <b>pad</b> parameter
    /// of the DepthwiseConv2dNative operator.
    pub const OH_NN_DEPTHWISE_CONV2D_NATIVE_PAD: OH_NN_TensorType = OH_NN_TensorType(25);
    /// This enumerated value is used when the tensor is used as the <b>dilation</b> parameter
    /// of the DepthwiseConv2dNative operator.
    pub const OH_NN_DEPTHWISE_CONV2D_NATIVE_DILATION: OH_NN_TensorType = OH_NN_TensorType(26);
    /// This enumerated value is used when the tensor is used as the <b>padMode</b> parameter
    /// of the DepthwiseConv2dNative operator.
    pub const OH_NN_DEPTHWISE_CONV2D_NATIVE_PAD_MODE: OH_NN_TensorType = OH_NN_TensorType(27);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the DepthwiseConv2dNative operator.
    pub const OH_NN_DEPTHWISE_CONV2D_NATIVE_ACTIVATION_TYPE: OH_NN_TensorType =
        OH_NN_TensorType(28);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Div operator.
    pub const OH_NN_DIV_ACTIVATIONTYPE: OH_NN_TensorType = OH_NN_TensorType(29);
    /// This enumerated value is used when the tensor is used as the <b>mode</b> parameter of the Eltwise operator.
    pub const OH_NN_ELTWISE_MODE: OH_NN_TensorType = OH_NN_TensorType(30);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the FullConnection operator.
    pub const OH_NN_FULL_CONNECTION_AXIS: OH_NN_TensorType = OH_NN_TensorType(31);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the FullConnection operator.
    pub const OH_NN_FULL_CONNECTION_ACTIVATIONTYPE: OH_NN_TensorType = OH_NN_TensorType(32);
    /// This enumerated value is used when the tensor is used as the <b>transposeA</b> parameter
    /// of the Matmul operator.
    pub const OH_NN_MATMUL_TRANSPOSE_A: OH_NN_TensorType = OH_NN_TensorType(33);
    /// This enumerated value is used when the tensor is used as the <b>transposeB</b> parameter
    /// of the Matmul operator.
    pub const OH_NN_MATMUL_TRANSPOSE_B: OH_NN_TensorType = OH_NN_TensorType(34);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Matmul operator.
    pub const OH_NN_MATMUL_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(35);
    /// This enumerated value is used when the tensor is used as the <b>kernelSize</b> parameter
    /// of the MaxPool operator.
    pub const OH_NN_MAX_POOL_KERNEL_SIZE: OH_NN_TensorType = OH_NN_TensorType(36);
    /// This enumerated value is used when the tensor is used as the <b>stride</b> parameter
    /// of the MaxPool operator.
    pub const OH_NN_MAX_POOL_STRIDE: OH_NN_TensorType = OH_NN_TensorType(37);
    /// This enumerated value is used when the tensor is used as the <b>padMode</b> parameter
    /// of the MaxPool operator.
    pub const OH_NN_MAX_POOL_PAD_MODE: OH_NN_TensorType = OH_NN_TensorType(38);
    /// This enumerated value is used when the tensor is used as the <b>pad</b> parameter of the MaxPool operator.
    pub const OH_NN_MAX_POOL_PAD: OH_NN_TensorType = OH_NN_TensorType(39);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the MaxPool operator.
    pub const OH_NN_MAX_POOL_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(40);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Mul operator.
    pub const OH_NN_MUL_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(41);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the OneHot operator.
    pub const OH_NN_ONE_HOT_AXIS: OH_NN_TensorType = OH_NN_TensorType(42);
    /// This enumerated value is used when the tensor is used as the <b>constantValue</b> parameter
    /// of the Pad operator.
    pub const OH_NN_PAD_CONSTANT_VALUE: OH_NN_TensorType = OH_NN_TensorType(43);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the Scale operator.
    pub const OH_NN_SCALE_ACTIVATIONTYPE: OH_NN_TensorType = OH_NN_TensorType(44);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the Scale operator.
    pub const OH_NN_SCALE_AXIS: OH_NN_TensorType = OH_NN_TensorType(45);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the Softmax operator.
    pub const OH_NN_SOFTMAX_AXIS: OH_NN_TensorType = OH_NN_TensorType(46);
    /// This enumerated value is used when the tensor is used as the <b>BlockShape</b> parameter
    /// of the SpaceToBatchND operator.
    pub const OH_NN_SPACE_TO_BATCH_ND_BLOCK_SHAPE: OH_NN_TensorType = OH_NN_TensorType(47);
    /// This enumerated value is used when the tensor is used as the <b>Paddings</b> parameter
    /// of the SpaceToBatchND operator.
    pub const OH_NN_SPACE_TO_BATCH_ND_PADDINGS: OH_NN_TensorType = OH_NN_TensorType(48);
    /// This enumerated value is used when the tensor is used as the <b>Axis</b> parameter of the Split operator.
    pub const OH_NN_SPLIT_AXIS: OH_NN_TensorType = OH_NN_TensorType(49);
    /// This enumerated value is used when the tensor is used as the <b>OutputNum</b> parameter
    /// of the Split operator.
    pub const OH_NN_SPLIT_OUTPUT_NUM: OH_NN_TensorType = OH_NN_TensorType(50);
    /// This enumerated value is used when the tensor is used as the <b>SizeSplits</b> parameter
    /// of the Split operator.
    pub const OH_NN_SPLIT_SIZE_SPLITS: OH_NN_TensorType = OH_NN_TensorType(51);
    /// This enumerated value is used when the tensor is used as the <b>Axis</b> parameter of the Squeeze operator.
    pub const OH_NN_SQUEEZE_AXIS: OH_NN_TensorType = OH_NN_TensorType(52);
    /// This enumerated value is used when the tensor is used as the <b>Axis</b> parameter of the Stack operator.
    pub const OH_NN_STACK_AXIS: OH_NN_TensorType = OH_NN_TensorType(53);
    /// This enumerated value is used when the tensor is used as the <b>BeginMask</b> parameter
    /// of the StridedSlice operator.
    pub const OH_NN_STRIDED_SLICE_BEGIN_MASK: OH_NN_TensorType = OH_NN_TensorType(54);
    /// This enumerated value is used when the tensor is used as the <b>EndMask</b> parameter
    /// of the StridedSlice operator.
    pub const OH_NN_STRIDED_SLICE_END_MASK: OH_NN_TensorType = OH_NN_TensorType(55);
    /// This enumerated value is used when the tensor is used as the <b>EllipsisMask</b> parameter
    /// of the StridedSlice operator.
    pub const OH_NN_STRIDED_SLICE_ELLIPSIS_MASK: OH_NN_TensorType = OH_NN_TensorType(56);
    /// This enumerated value is used when the tensor is used as the <b>NewAxisMask</b> parameter
    /// of the StridedSlice operator.
    pub const OH_NN_STRIDED_SLICE_NEW_AXIS_MASK: OH_NN_TensorType = OH_NN_TensorType(57);
    /// This enumerated value is used when the tensor is used as the <b>ShrinkAxisMask</b> parameter
    /// of the StridedSlice operator.
    pub const OH_NN_STRIDED_SLICE_SHRINK_AXIS_MASK: OH_NN_TensorType = OH_NN_TensorType(58);
    /// This enumerated value is used when the tensor is used as the <b>ActivationType</b> parameter
    /// of the Sub operator.
    pub const OH_NN_SUB_ACTIVATIONTYPE: OH_NN_TensorType = OH_NN_TensorType(59);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceMean operator.
    pub const OH_NN_REDUCE_MEAN_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(60);
    /// This enumerated value is used when the tensor is used as the <b>newHeight</b> parameter
    /// of the ResizeBilinear operator.
    pub const OH_NN_RESIZE_BILINEAR_NEW_HEIGHT: OH_NN_TensorType = OH_NN_TensorType(61);
    /// This enumerated value is used when the tensor is used as the <b>newWidth</b> parameter
    /// of the ResizeBilinear operator.
    pub const OH_NN_RESIZE_BILINEAR_NEW_WIDTH: OH_NN_TensorType = OH_NN_TensorType(62);
    /// This enumerated value is used when the tensor is used as the <b>preserveAspectRatio</b> parameter
    /// of the ResizeBilinear operator.
    pub const OH_NN_RESIZE_BILINEAR_PRESERVE_ASPECT_RATIO: OH_NN_TensorType = OH_NN_TensorType(63);
    /// This enumerated value is used when the tensor is used as the <b>coordinateTransformMode</b> parameter
    /// of the ResizeBilinear operator.
    pub const OH_NN_RESIZE_BILINEAR_COORDINATE_TRANSFORM_MODE: OH_NN_TensorType =
        OH_NN_TensorType(64);
    /// This enumerated value is used when the tensor is used as the <b>excludeOutside</b> parameter
    /// of the ResizeBilinear operator.
    pub const OH_NN_RESIZE_BILINEAR_EXCLUDE_OUTSIDE: OH_NN_TensorType = OH_NN_TensorType(65);
    /// This enumerated value is used when the tensor is used as the <b>beginNormAxis</b> parameter
    /// of the LayerNorm operator.
    pub const OH_NN_LAYER_NORM_BEGIN_NORM_AXIS: OH_NN_TensorType = OH_NN_TensorType(66);
    /// This enumerated value is used when the tensor is used as the <b>epsilon</b> parameter
    /// of the LayerNorm operator.
    pub const OH_NN_LAYER_NORM_EPSILON: OH_NN_TensorType = OH_NN_TensorType(67);
    /// This enumerated value is used when the tensor is used as the <b>beginParamsAxis</b> parameter
    /// of the LayerNorm operator.
    pub const OH_NN_LAYER_NORM_BEGIN_PARAM_AXIS: OH_NN_TensorType = OH_NN_TensorType(68);
    /// This enumerated value is used when the tensor is used as the <b>elementwiseAffine</b> parameter
    /// of the LayerNorm operator.
    pub const OH_NN_LAYER_NORM_ELEMENTWISE_AFFINE: OH_NN_TensorType = OH_NN_TensorType(69);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceProd operator.
    pub const OH_NN_REDUCE_PROD_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(70);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceAll operator.
    pub const OH_NN_REDUCE_ALL_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(71);
    /// This enumerated value is used when the tensor is used as the <b>src_t</b> parameter
    /// of the QuantDTypeCast operator.
    pub const OH_NN_QUANT_DTYPE_CAST_SRC_T: OH_NN_TensorType = OH_NN_TensorType(72);
    /// This enumerated value is used when the tensor is used as the <b>dst_t</b> parameter
    /// of the QuantDTypeCast operator.
    pub const OH_NN_QUANT_DTYPE_CAST_DST_T: OH_NN_TensorType = OH_NN_TensorType(73);
    /// This enumerated value is used when the tensor is used as the <b>Sorted</b> parameter
    /// of the Topk operator.
    pub const OH_NN_TOP_K_SORTED: OH_NN_TensorType = OH_NN_TensorType(74);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the ArgMax operator.
    pub const OH_NN_ARG_MAX_AXIS: OH_NN_TensorType = OH_NN_TensorType(75);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ArgMax operator.
    pub const OH_NN_ARG_MAX_KEEPDIMS: OH_NN_TensorType = OH_NN_TensorType(76);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the Unsqueeze operator.
    pub const OH_NN_UNSQUEEZE_AXIS: OH_NN_TensorType = OH_NN_TensorType(77);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the Unstack operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_UNSTACK_AXIS: OH_NN_TensorType = OH_NN_TensorType(78);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the Flatten operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_FLATTEN_AXIS: OH_NN_TensorType = OH_NN_TensorType(79);
    /// This enumerated value is used when the tensor is used as the <b>blockSize</b> parameter
    /// of the DepthToSpace operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DEPTH_TO_SPACE_BLOCK_SIZE: OH_NN_TensorType = OH_NN_TensorType(80);
    /// This enumerated value is used when the tensor is used as the <b>mode</b> parameter
    /// of the DepthToSpace operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DEPTH_TO_SPACE_MODE: OH_NN_TensorType = OH_NN_TensorType(81);
    /// This enumerated value is used when the tensor is used as the <b>start</b> parameter of the Range operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_RANGE_START: OH_NN_TensorType = OH_NN_TensorType(82);
    /// This enumerated value is used when the tensor is used as the <b>limit</b> parameter of the Range operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_RANGE_LIMIT: OH_NN_TensorType = OH_NN_TensorType(83);
    /// This enumerated value is used when the tensor is used as the <b>delta</b> parameter of the Range operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_RANGE_DELTA: OH_NN_TensorType = OH_NN_TensorType(84);
    /// This enumerated value is used when the tensor is used as the <b>dataType</b> parameter
    /// of the ConstantOfShape operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CONSTANT_OF_SHAPE_DATA_TYPE: OH_NN_TensorType = OH_NN_TensorType(85);
    /// This enumerated value is used when the tensor is used as the <b>value</b> parameter
    /// of the ConstantOfShape operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CONSTANT_OF_SHAPE_VALUE: OH_NN_TensorType = OH_NN_TensorType(86);
    /// This enumerated value is used when the tensor is used as the <b>shape</b> parameter
    /// of the BroadcastTo operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_BROADCAST_TO_SHAPE: OH_NN_TensorType = OH_NN_TensorType(87);
    /// This enumerated value is used when the tensor is used as the <b>epsilon</b> parameter
    /// of the InstanceNorm operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_INSTANCE_NORM_EPSILON: OH_NN_TensorType = OH_NN_TensorType(88);
    /// This enumerated value is used when the tensor is used as the <b>base</b> parameter of the Exp operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_EXP_BASE: OH_NN_TensorType = OH_NN_TensorType(89);
    /// This enumerated value is used when the tensor is used as the <b>scale</b> parameter of the Exp operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_EXP_SCALE: OH_NN_TensorType = OH_NN_TensorType(90);
    /// This enumerated value is used when the tensor is used as the <b>shift</b> parameter of the Exp operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_EXP_SHIFT: OH_NN_TensorType = OH_NN_TensorType(91);
    /// This enumerated value is used when the tensor is used as the <b>negativeSlope</b> parameter
    /// of the LeakyRelu operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LEAKY_RELU_NEGATIVE_SLOPE: OH_NN_TensorType = OH_NN_TensorType(92);
    /// This enumerated value is used when the tensor is used as the <b>bidirectional</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_BIDIRECTIONAL: OH_NN_TensorType = OH_NN_TensorType(93);
    /// This enumerated value is used when the tensor is used as the <b>hasBias</b> parameter of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_HAS_BIAS: OH_NN_TensorType = OH_NN_TensorType(94);
    /// This enumerated value is used when the tensor is used as the <b>inputSize</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_INPUT_SIZE: OH_NN_TensorType = OH_NN_TensorType(95);
    /// This enumerated value is used when the tensor is used as the <b>hiddenSize</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_HIDDEN_SIZE: OH_NN_TensorType = OH_NN_TensorType(96);
    /// This enumerated value is used when the tensor is used as the <b>numLayers</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_NUM_LAYERS: OH_NN_TensorType = OH_NN_TensorType(97);
    /// This enumerated value is used when the tensor is used as the <b>numDirections</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_NUM_DIRECTIONS: OH_NN_TensorType = OH_NN_TensorType(98);
    /// This enumerated value is used when the tensor is used as the <b>dropout</b> parameter of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_DROPOUT: OH_NN_TensorType = OH_NN_TensorType(99);
    /// This enumerated value is used when the tensor is used as the <b>zoneoutCell</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_ZONEOUT_CELL: OH_NN_TensorType = OH_NN_TensorType(100);
    /// This enumerated value is used when the tensor is used as the <b>zoneoutHidden</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_ZONEOUT_HIDDEN: OH_NN_TensorType = OH_NN_TensorType(101);
    /// This enumerated value is used when the tensor is used as the <b>projSize</b> parameter
    /// of the LSTM operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LSTM_PROJ_SIZE: OH_NN_TensorType = OH_NN_TensorType(102);
    /// This enumerated value is used when the tensor is used as the <b>max</b> parameter of the Clip operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CLIP_MAX: OH_NN_TensorType = OH_NN_TensorType(103);
    /// This enumerated value is used when the tensor is used as the <b>min</b> parameter of the Clip operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CLIP_MIN: OH_NN_TensorType = OH_NN_TensorType(104);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter of the All operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_ALL_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(105);
    /// This enumerated value is used when the tensor is used as the <b>summarize</b> parameter
    /// of the Assert operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_ASSERT_SUMMARIZE: OH_NN_TensorType = OH_NN_TensorType(106);
    /// This enumerated value is used when the tensor is used as the <b>scale</b> parameter of the pow operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_POW_SCALE: OH_NN_TensorType = OH_NN_TensorType(107);
    /// This enumerated value is used when the tensor is used as the <b>shift</b> parameter of the pow operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_POW_SHIFT: OH_NN_TensorType = OH_NN_TensorType(108);
    /// This enumerated value is used when the tensor is used as the <b>roundMode</b> parameter
    /// of the AvgPool operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_AVG_POOL_ROUND_MODE: OH_NN_TensorType = OH_NN_TensorType(109);
    /// This enumerated value is used when the tensor is used as the <b>global</b> parameter
    /// of the AvgPool operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_AVG_POOL_GLOBAL: OH_NN_TensorType = OH_NN_TensorType(110);
    /// This enumerated value is used when the tensor is used as the <b>hasBias</b> parameter
    /// of the FullConnection operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_FULL_CONNECTION_HAS_BIAS: OH_NN_TensorType = OH_NN_TensorType(111);
    /// This enumerated value is used when the tensor is used as the <b>useAxis</b> parameter
    /// of the FullConnection operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_FULL_CONNECTION_USE_AXIS: OH_NN_TensorType = OH_NN_TensorType(112);
    /// This enumerated value is used when the tensor is used as the <b>approximate</b> parameter
    /// of the GeLU operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_GELU_APPROXIMATE: OH_NN_TensorType = OH_NN_TensorType(113);
    /// This enumerated value is used when the tensor is used as the <b>roundMode</b> parameter
    /// of the MaxPool operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_MAX_POOL_ROUND_MODE: OH_NN_TensorType = OH_NN_TensorType(114);
    /// This enumerated value is used when the tensor is used as the <b>global</b> parameter
    /// of the MaxPool operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_MAX_POOL_GLOBAL: OH_NN_TensorType = OH_NN_TensorType(115);
    /// This enumerated value is used when the tensor is used as the <b>paddingMode</b> parameter
    /// of the Pad operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_PAD_PADDING_MODE: OH_NN_TensorType = OH_NN_TensorType(116);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceMean operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MEAN_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(117);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceMean operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MEAN_COEFF: OH_NN_TensorType = OH_NN_TensorType(118);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceProd operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_PROD_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(119);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceProd operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_PROD_COEFF: OH_NN_TensorType = OH_NN_TensorType(120);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceAll operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_ALL_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(121);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceAll operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_ALL_COEFF: OH_NN_TensorType = OH_NN_TensorType(122);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the Topk operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_TOP_K_AXIS: OH_NN_TensorType = OH_NN_TensorType(123);
    /// This enumerated value is used when the tensor is used as the <b>topK</b> parameter
    /// of the ArgMax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_ARG_MAX_TOP_K: OH_NN_TensorType = OH_NN_TensorType(124);
    /// This enumerated value is used when the tensor is used as the <b>outMaxValue</b> parameter
    /// of the ArgMax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_ARG_MAX_OUT_MAX_VALUE: OH_NN_TensorType = OH_NN_TensorType(125);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the QuantDTypeCast operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_QUANT_DTYPE_CAST_AXIS: OH_NN_TensorType = OH_NN_TensorType(126);
    /// This enumerated value is used when the tensor is used as the <b>axes</b> parameter of the Slice operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_SLICE_AXES: OH_NN_TensorType = OH_NN_TensorType(127);
    /// This enumerated value is used when the tensor is used as the <b>dims</b> parameter of the Tile operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_TILE_DIMS: OH_NN_TensorType = OH_NN_TensorType(128);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the crop operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CROP_AXIS: OH_NN_TensorType = OH_NN_TensorType(129);
    /// This enumerated value is used when the tensor is used as the <b>offset</b> parameter of the crop operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_CROP_OFFSET: OH_NN_TensorType = OH_NN_TensorType(130);
    /// This enumerated value is used when the tensor is used as the <b>inputSize</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_INPUT_SIZE: OH_NN_TensorType = OH_NN_TensorType(131);
    /// This enumerated value is used when the tensor is used as the <b>scale</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_SCALE: OH_NN_TensorType = OH_NN_TensorType(132);
    /// This enumerated value is used when the tensor is used as the <b>nmsIoUThreshold</b>
    /// parameter of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_NMS_IOU_THRESHOLD: OH_NN_TensorType =
        OH_NN_TensorType(133);
    /// This enumerated value is used when the tensor is used as the <b>nmsScoreThreshold</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_NMS_SCORE_THRESHOLD: OH_NN_TensorType =
        OH_NN_TensorType(134);
    /// This enumerated value is used when the tensor is used as the <b>maxDetections</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_MAX_DETECTIONS: OH_NN_TensorType = OH_NN_TensorType(135);
    /// This enumerated value is used when the tensor is used as the <b>detectionsPerClass</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_DETECTIONS_PER_CLASS: OH_NN_TensorType =
        OH_NN_TensorType(136);
    /// This enumerated value is used when the tensor is used as the <b>maxClassesPerDetection</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_MAX_CLASSES_PER_DETECTION: OH_NN_TensorType =
        OH_NN_TensorType(137);
    /// This enumerated value is used when the tensor is used as the <b>numClasses</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_NUM_CLASSES: OH_NN_TensorType = OH_NN_TensorType(138);
    /// This enumerated value is used when the tensor is used as the <b>useRegularNms</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_USE_REGULAR_NMS: OH_NN_TensorType =
        OH_NN_TensorType(139);
    /// This enumerated value is used when the tensor is used as the <b>outQuantized</b> parameter
    /// of the detectionPostProcess operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_DETECTION_POST_PROCESS_OUT_QUANTIZED: OH_NN_TensorType = OH_NN_TensorType(140);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter
    /// of the L2Normalize operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_L2_NORMALIZE_AXIS: OH_NN_TensorType = OH_NN_TensorType(141);
    /// This enumerated value is used when the tensor is used as the <b>epsilon</b> parameter
    /// of the L2Normalize operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_L2_NORMALIZE_EPSILON: OH_NN_TensorType = OH_NN_TensorType(142);
    /// This enumerated value is used when the tensor is used as the <b>activationType</b> parameter
    /// of the L2Normalize operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_L2_NORMALIZE_ACTIVATION_TYPE: OH_NN_TensorType = OH_NN_TensorType(143);
    /// This enumerated value is used when the tensor is used as the <b>axis</b> parameter of the softmax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LOG_SOFTMAX_AXIS: OH_NN_TensorType = OH_NN_TensorType(144);
    /// This enumerated value is used when the tensor is used as the <b>depthRedius</b>
    /// parameter of the LRN operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LRN_DEPTH_RADIUS: OH_NN_TensorType = OH_NN_TensorType(145);
    /// This enumerated value is used when the tensor is used as the <b>bias</b> parameter of the LRN operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LRN_BIAS: OH_NN_TensorType = OH_NN_TensorType(146);
    /// This enumerated value is used when the tensor is used as the <b>alpha</b> parameter of the LRN operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LRN_ALPHA: OH_NN_TensorType = OH_NN_TensorType(147);
    /// This enumerated value is used when the tensor is used as the <b>beta</b> parameter of the LRN operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LRN_BETA: OH_NN_TensorType = OH_NN_TensorType(148);
    /// This enumerated value is used when the tensor is used as the <b>normRegion</b> parameter
    /// of the LRN operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_LRN_NORM_REGION: OH_NN_TensorType = OH_NN_TensorType(149);
    /// This enumerated value is used when the tensor is used as the <b>blockSize</b> parameter
    /// of the spaceToDepth operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_SPACE_TO_DEPTH_BLOCK_SIZE: OH_NN_TensorType = OH_NN_TensorType(150);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceMax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MAX_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(151);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceMax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MAX_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(152);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceMax operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MAX_COEFF: OH_NN_TensorType = OH_NN_TensorType(153);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceMin operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MIN_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(154);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceMin operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MIN_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(155);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceMin operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_MIN_COEFF: OH_NN_TensorType = OH_NN_TensorType(156);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceSum operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_SUM_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(157);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceSum operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_SUM_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(158);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceSum operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_SUM_COEFF: OH_NN_TensorType = OH_NN_TensorType(159);
    /// This enumerated value is used when the tensor is used as the <b>keepDims</b> parameter
    /// of the ReduceL2 operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_L2_KEEP_DIMS: OH_NN_TensorType = OH_NN_TensorType(160);
    /// This enumerated value is used when the tensor is used as the <b>reduceToEnd</b> parameter
    /// of the ReduceL2 operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_L2_REDUCE_TO_END: OH_NN_TensorType = OH_NN_TensorType(161);
    /// This enumerated value is used when the tensor is used as the <b>coeff</b> parameter
    /// of the ReduceL2 operator.
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub const OH_NN_REDUCE_L2_COEFF: OH_NN_TensorType = OH_NN_TensorType(162);
}
#[repr(transparent)]
/// Enumerates the tensor data types.
///
/// Tensors are usually used to set the input, output, and operator parameters of a model. When a tensor is used
/// as the input or output of a model (or operator), set the tensor type to [`OH_NN_TENSOR`].
/// When the tensor is used as an operator parameter, select an enumerated value other than [`OH_NN_TENSOR`] as the
/// tensor type. Assume that the <b>pad</b> parameter of the [`OH_NN_OPS_CONV2D`] operator is being set.
/// You need to set the <b>type</b> attribute of the [`OH_NN_Tensor`] instance to [`OH_NN_CONV2D_PAD`].
/// The settings of other operator parameters are similar. The enumerated values are named
/// in the format OH_NN_{<i>Operator name</i>}_{<i>Attribute name</i>}.
///
///
/// Available since API-level: 9
///
/// Version: 2.0
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct OH_NN_TensorType(pub ::core::ffi::c_uint);
/// This structure is used to store a 32-bit unsigned integer array.
///
///
/// Available since API-level: 9
///
/// Version: 1.0
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NN_UInt32Array {
    /// Pointer to the unsigned integer array
    pub data: *mut u32,
    /// Array length
    pub size: u32,
}
/// Quantization information.
///
/// In quantization scenarios, the 32-bit floating-point data type is quantized into
/// the fixed-point data type according to the following formula:
/// q = clamp(round(r}{s}+z), q_{min}, q_{max})
/// s and z are quantization parameters, which are stored by <b>scale</b> and <b>zeroPoint</b>
/// in [`OH_NN_QuantParam`].
/// r is a floating point number, q is the quantization result, q_min is the lower bound of the quantization result, and
/// q_max is an upper bound of a quantization result. The calculation method is as follows:
///
/// clamp}(x,min,max) =
/// cases}
/// q_{min} = -(1 << (numBits - 1))
/// q_{max} = (1 << (numBits - 1))
/// cases}
/// The clamp function is defined as follows:
/// clamp}(x,min,max) =
/// cases}
/// max} & if } x > max }
/// min} & if } x < min }
/// x & otherwise }
/// cases}
///
/// **Deprecated** since 11
///
/// **Use instead:** [`NN_QuantParam`]
///
/// Available since API-level: 9
///
/// Version: 1.0
#[deprecated(since = "11", note = "Use instead: [`NN_QuantParam`]")]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NN_QuantParam {
    /// Specifies the length of the numBits, scale, and zeroPoint arrays. In the per-layer quantization scenario,
    /// <b>quantCount</b> is usually set to <b>1</b>.
    /// That is, all channels of a tensor share a set of quantization parameters.
    /// In the per-channel quantization scenario, <b>quantCount</b> is usually the same as the number of tensor
    /// channels, and each channel uses its own quantization parameters.
    pub quantCount: u32,
    /// Number of quantization bits
    pub numBits: *const u32,
    /// Pointer to the scale data in the quantization formula
    pub scale: *const f64,
    /// Pointer to the zero point data in the quantization formula
    pub zeroPoint: *const i32,
}
/// Defines the tensor structure.
///
/// It is usually used to construct data nodes and operator parameters in a model graph. When constructing a tensor,
/// you need to specify the data type, number of dimensions, dimension information, and quantization information.
///
///
/// **Deprecated** since 11
///
/// **Use instead:** [`NN_TensorDesc`]
///
/// Available since API-level: 9
///
/// Version: 1.0
#[deprecated(since = "11", note = "Use instead: [`NN_TensorDesc`]")]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NN_Tensor {
    /// Data type of the specified tensor. The value must be an enumerated value of [`OH_NN_DataType`].
    pub dataType: OH_NN_DataType,
    /// Number of dimensions of the specified tensor
    pub dimensionCount: u32,
    /// Dimension information (shape) of the specified tensor
    pub dimensions: *const i32,
    /// Quantization information of the specified tensor. The data type must be [`OH_NN_QuantParam`].
    pub quantParam: *const OH_NN_QuantParam,
    /// Specifies the tensor type. The value of <b>type</b> is related to the tensor usage.
    /// When the tensor is used as the input or output of the model, set <b>type</b> to [`OH_NN_TENSOR`].
    /// When a tensor is used as an operator parameter, select any enumerated value except [`OH_NN_TENSOR`]
    /// from [`OH_NN_TensorType`].
    pub type_: OH_NN_TensorType,
}
/// Defines the memory structure.
///
///
/// **Deprecated** since 11
///
/// **Use instead:** [`NN_Tensor`]
///
/// Available since API-level: 9
///
/// Version: 1.0
#[deprecated(since = "11", note = "Use instead: [`NN_Tensor`]")]
#[repr(C)]
pub struct OH_NN_Memory {
    /// Pointer to the shared memory. The shared memory is usually allocated by the underlying hardware driver.
    pub data: *mut ::core::ffi::c_void,
    /// Records the length of the shared memory, in bytes.
    pub length: usize,
}
extern "C" {
    /// Creates a compilation instance of the [`OH_NNCompilation`] type.
    ///
    /// After the OH_NNModel module completes model construction, APIs provided by the OH_NNCompilation module pass the
    /// model to underlying device for compilation. This method creates a [`OH_NNCompilation`] instance
    /// based on the passed [`OH_NNModel`] instance. The [`OH_NNCompilation_SetDevice`] method is called
    /// to set the device to compile on, and [`OH_NNCompilation_Build`] is then called to complete compilation.
    ///
    ///
    /// In addition to computing device selection, the OH_NNCompilation module supports features such as model caching,
    /// performance preference, priority setting, and float16 computing, which can be implemented by the following methods:
    ///
    /// [`OH_NNCompilation_SetCache`]
    ///
    /// [`OH_NNCompilation_SetPerformanceMode`]
    ///
    /// [`OH_NNCompilation_SetPriority`]
    ///
    /// [`OH_NNCompilation_EnableFloat16`]
    ///
    ///
    /// After [`OH_NNCompilation_Build`] is called, the [`OH_NNModel`] instance can be released.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`OH_NNCompilation`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the parameters of model are invalid or there is a problem with the model format.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_Construct(model: *const OH_NNModel) -> *mut OH_NNCompilation;
    /// Creates a compilation instance based on an offline model file.
    ///
    /// This method conflicts with the way of passing an online built model or an offline model file buffer,
    /// and you have to choose only one of the three construction methods.
    ///
    ///
    /// Offline model is a type of model that is offline compiled by the model converter provided by a device vendor.
    /// So that the offline model can only be used on the specified device, but the compilation time of offline model is usually
    /// much less than [`OH_NNModel`].
    ///
    ///
    /// You should perform the offline compilation during your development and deploy the offline model in your app package.
    ///
    ///
    /// # Arguments
    ///
    /// * `modelPath` - Offline model file path.
    ///
    /// # Returns
    ///
    /// * Pointer to an [`OH_NNCompilation`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the modelPath is invalid.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_ConstructWithOfflineModelFile(
        modelPath: *const ::core::ffi::c_char,
    ) -> *mut OH_NNCompilation;
    /// Creates a compilation instance based on an offline model file buffer.
    ///
    /// This method conflicts with the way of passing an online built model or an offline model file path,
    /// and you have to choose only one of the three construction methods.
    ///
    ///
    /// Note that the returned [`OH_NNCompilation`] instance only saves the <b>modelBuffer</b> pointer inside, instead of
    /// copying its data. You should not release <b>modelBuffer</b> before the [`OH_NNCompilation`] instance is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `modelBuffer` - Offline model file buffer.
    ///
    /// * `modelSize` - Offfline model buffer size.
    ///
    /// # Returns
    ///
    /// * Pointer to an [`OH_NNCompilation`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the modelBuffer or modelSize is invalid.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_ConstructWithOfflineModelBuffer(
        modelBuffer: *const ::core::ffi::c_void,
        modelSize: usize,
    ) -> *mut OH_NNCompilation;
    /// Creates a empty compilation instance for restoration from cache later.
    ///
    /// See [`OH_NNCompilation_SetCache`] for the description of cache.
    ///
    ///
    /// The restoration time from the cache is less than compilation with [`OH_NNModel`].
    ///
    ///
    /// You should call [`OH_NNCompilation_SetCache`] or [`OH_NNCompilation_ImportCacheFromBuffer`] first,
    /// and then call [`OH_NNCompilation_Build`] to complete the restoration.
    ///
    ///
    ///
    /// # Returns
    ///
    /// * Pointer to an [`OH_NNCompilation`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the cache file saved before is invalid.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_ConstructForCache() -> *mut OH_NNCompilation;
    /// Exports the cache to a given buffer.
    ///
    /// See [`OH_NNCompilation_SetCache`] for the description of cache.
    ///
    ///
    /// Note that the cache is the result of compilation building [`OH_NNCompilation_Build`],
    /// so that this method must be called after [`OH_NNCompilation_Build`].
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `buffer` - Pointer to the given buffer.
    ///
    /// * `length` - Buffer length.
    ///
    /// * `modelSize` - Byte size of the model cache.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] export cache to buffer successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to export cache to buffer. The possible reason for failure
    /// is that the <b>compilation</b>, <b>buffer</b> or <b>modelSize</b> is nullptr, or <b>length</b> is 0,
    /// or <b>compilation</b> is invalid.
    ///
    /// [`OH_NN_UNSUPPORTED`] exporting cache to buffer is unsupported.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_ExportCacheToBuffer(
        compilation: *mut OH_NNCompilation,
        buffer: *const ::core::ffi::c_void,
        length: usize,
        modelSize: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Imports the cache from a given buffer.
    ///
    /// See [`OH_NNCompilation_SetCache`] for the description of cache.
    ///
    ///
    /// [`OH_NNCompilation_Build`] should be called to complete the restoration after
    /// [`OH_NNCompilation_ImportCacheFromBuffer`] is called.
    ///
    ///
    /// Note that <b>compilation</b> only saves the <b>buffer</b> pointer inside, instead of copying its data. You should not
    /// release <b>buffer</b> before <b>compilation</b> is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `buffer` - Pointer to the given buffer.
    ///
    /// * `modelSize` - Byte size of the model cache.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] import cache from buffer successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to import cache from buffer. The possible reason for failure is that
    /// the <b>compilation</b> or <b>buffer</b> is nullptr, or <b>modelSize</b> is 0, or content of <b>buffer</b>
    /// is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_ImportCacheFromBuffer(
        compilation: *mut OH_NNCompilation,
        buffer: *const ::core::ffi::c_void,
        modelSize: usize,
    ) -> OH_NN_ReturnCode;
    /// Adds an extension config for a custom hardware attribute.
    ///
    /// Some devices have their own specific attributes which have not been opened in NNRt. This method provides an additional way for you
    /// to set these custom hardware attributes of the device. You should query their names and values from the device
    /// vendor's documents, and add them into compilation instance one by one. These attributes will be passed directly to device
    /// driver, and this method will return error code if the driver cannot parse them.
    ///
    ///
    /// After [`OH_NNCompilation_Build`] is called, the <b>configName</b> and <b>configValue</b> can be released.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `configName` - Config name.
    ///
    /// * `configValue` - A byte buffer saving the config value.
    ///
    /// * `configValueSize` - Byte size of the config value.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] add extension config successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to add extension config. The possible reason for failure is that the
    /// <b>compilation</b>, <b>configName</b> or <b>configValue</b> is nullptr, or <b>configValueSize</b> is 0.
    ///
    /// [`OH_NN_FAILED`] other failures, such as memory error during object creation.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNCompilation_AddExtensionConfig(
        compilation: *mut OH_NNCompilation,
        configName: *const ::core::ffi::c_char,
        configValue: *const ::core::ffi::c_void,
        configValueSize: usize,
    ) -> OH_NN_ReturnCode;
    /// Specifies the device for model compilation and computing.
    ///
    /// In the compilation phase, you need to specify the device for model compilation and computing. Call [`OH_NNDevice_GetAllDevicesID`]
    /// to obtain available device IDs. Call [`OH_NNDevice_GetType`] and [`OH_NNDevice_GetName`] to obtain device information
    /// and pass target device ID to this method for setting.
    ///
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `deviceID` - Device id. If it is 0, the first device in the current device list will be used by default.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set device successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set device. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_SetDevice(
        compilation: *mut OH_NNCompilation,
        deviceID: usize,
    ) -> OH_NN_ReturnCode;
    /// Set the cache directory and version of the compiled model.
    ///
    /// On the device that supports caching, a model can be saved as a cache file after being compiled on the device driver.
    /// The model can be directly read from the cache file in the next compilation, saving recompilation time.
    /// This method performs different operations based on the passed cache directory and version:
    ///
    ///
    /// - No file exists in the cache directory:
    /// Caches the compiled model to the directory and sets the cache version to <b>version</b>.
    ///
    ///
    /// - A complete cache file exists in the cache directory, and its version is <b>version</b>:
    /// Reads the cache file in the path and passes the data to the underlying device for conversion into executable model instances.
    ///
    ///
    /// - A complete cache file exists in the cache directory, and its version is earlier than <b>version</b>:
    /// When model compilation is complete on the underlying device, overwrites the cache file and changes the version number to <b>version</b>.
    ///
    ///
    /// - A complete cache file exists in the cache directory, and its version is later than <b>version</b>:
    /// Returns the [`OH_NN_INVALID_PARAMETER`] error code without reading the cache file.
    ///
    ///
    /// - The cache file in the cache directory is incomplete or you do not have the permission to access the cache file.
    /// Returns the [`OH_NN_INVALID_FILE`] error code.
    ///
    ///
    /// - The cache directory does not exist or you do not have the access permission.
    /// Returns the [`OH_NN_INVALID_PATH`] error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `cachePath` - Directory for storing model cache files. This method creates directories for different devices in the <b>cachePath</b> directory.
    /// You are advised to use a separate cache directory for each model.
    ///
    /// * `version` - Cache version.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set cache path and version successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set cache path and version. The possible reason for failure
    /// is that the <b>compilation</b> or <b>cachePath</b> is nullptr.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_SetCache(
        compilation: *mut OH_NNCompilation,
        cachePath: *const ::core::ffi::c_char,
        version: u32,
    ) -> OH_NN_ReturnCode;
    /// Sets the performance mode for model computing.
    ///
    /// Allows you to set the performance mode for model computing to meet the requirements of low power consumption
    /// and ultimate performance. If this method is not called to set the performance mode in the compilation phase, the compilation instance assigns
    /// the [`OH_NN_PERFORMANCE_NONE`] mode for the model by default. In this case, the device performs computing in the default performance mode.
    ///
    ///
    /// If this method is called on the device that does not support the setting of the performance mode, the [`OH_NN_UNAVALIDABLE_DEVICE`] error code is returned.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `performanceMode` - Performance mode. For details about the available performance modes, see [`OH_NN_PerformanceMode`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set performance mode successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set performance mode. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr, or <b>performanceMode</b> is invalid.
    ///
    /// [`OH_NN_FAILED`] fail to query whether the backend device supports setting performance mode.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to set performance mode.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_SetPerformanceMode(
        compilation: *mut OH_NNCompilation,
        performanceMode: OH_NN_PerformanceMode,
    ) -> OH_NN_ReturnCode;
    /// Sets the model computing priority.
    ///
    /// Allows you to set computing priorities for models.
    /// The priorities apply only to models created by the process with the same UID.
    /// The settings will not affect models created by processes with different UIDs on different devices.
    ///
    ///
    /// If this method is called on the device that does not support the priority setting, the [`OH_NN_UNAVALIDABLE_DEVICE`] error code is returned.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `priority` - Priority. For details about the optional priorities, see [`OH_NN_Priority`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set priority successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set priority. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr, or <b>priority</b> is invalid.
    ///
    /// [`OH_NN_FAILED`] fail to query whether the backend device supports setting priority.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to set priority.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_SetPriority(
        compilation: *mut OH_NNCompilation,
        priority: OH_NN_Priority,
    ) -> OH_NN_ReturnCode;
    /// Enables float16 for computing.
    ///
    /// Float32 is used by default for the model of float type. If this method is called on a device that supports float16,
    /// float16 will be used for computing the float32 model to reduce memory usage and execution time.
    ///
    ///
    /// This option is useless for the model of int type, e.g. int8 type.
    ///
    ///
    /// If this method is called on the device that does not support float16, the [`OH_NN_UNAVALIDABLE_DEVICE`] error code is returned.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// * `enableFloat16` - Indicates whether to enable float16. If this parameter is set to <b>true</b>, float16 inference is performed.
    /// If this parameter is set to <b>false</b>, float32 inference is performed.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] enable fp16 successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to enable fp16. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_EnableFloat16(
        compilation: *mut OH_NNCompilation,
        enableFloat16: bool,
    ) -> OH_NN_ReturnCode;
    /// Compiles a model.
    ///
    /// After the compilation configuration is complete, call this method to return the compilation result. The compilation instance pushes the model and
    /// compilation options to the device for compilation. After this method is called, additional compilation operations cannot be performed.
    ///
    ///
    /// If the [`OH_NNCompilation_SetDevice`], [`OH_NNCompilation_SetCache`], [`OH_NNCompilation_SetPerformanceMode`],
    /// [`OH_NNCompilation_SetPriority`], and [`OH_NNCompilation_EnableFloat16`] methods are called, [`OH_NN_OPERATION_FORBIDDEN`] is returned.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] build model successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to build model. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr, or the parameters set before is invalid.
    ///
    /// [`OH_NN_FAILED`] fail to build model.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported the model.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_Build(compilation: *mut OH_NNCompilation) -> OH_NN_ReturnCode;
    /// Releases the <b>Compilation</b> object.
    ///
    /// This method needs to be called to release the compilation instance created by [`OH_NNCompilation_Construct`],
    /// [`OH_NNCompilation_ConstructWithOfflineModelFile`], [`OH_NNCompilation_ConstructWithOfflineModelBuffer`] and
    /// [`OH_NNCompilation_ConstructForCache`]. Otherwise, the memory leak will occur.
    ///
    ///
    /// If <b>compilation</b> or <b>*compilation</b> is a null pointer, this method only prints warning logs and does not execute the release.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Double pointer to the [`OH_NNCompilation`] instance. After a compilation instance is destroyed,
    /// this method sets <b>*compilation</b> to a null pointer.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNCompilation_Destroy(compilation: *mut *mut OH_NNCompilation);
    /// Creates an [`NN_TensorDesc`] instance.
    ///
    /// The [`NN_TensorDesc`] describes various tensor attributes, such as name/data type/shape/format, etc.
    ///
    ///
    /// The following methods can be called to create a [`NN_Tensor`] instance based on the passed [`NN_TensorDesc`]
    /// instance:
    ///
    /// [`OH_NNTensor_Create`]
    ///
    /// [`OH_NNTensor_CreateWithSize`]
    ///
    /// [`OH_NNTensor_CreateWithFd`]
    ///
    ///
    /// Note that these methods will copy the [`NN_TensorDesc`] instance into [`NN_Tensor`]. Therefore you can create
    /// multiple [`NN_Tensor`] instances with the same [`NN_TensorDesc`] instance. And you should destroy the
    /// [`NN_TensorDesc`] instance by [`OH_NNTensorDesc_Destroy`] when it is no longer used.
    ///
    ///
    ///
    /// # Returns
    ///
    /// * Pointer to a [`NN_TensorDesc`] instance, or NULL if it fails to create. The possible reason for failure
    /// is that the memory error occurred during object creation.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_Create() -> *mut NN_TensorDesc;
    /// Releases an [`NN_TensorDesc`] instance.
    ///
    /// When the [`NN_TensorDesc`] instance is no longer used, this method needs to be called to release it. Otherwise,
    /// the memory leak will occur.
    ///
    ///
    /// If <b>tensorDesc</b> or <b>*tensorDesc</b> is a null pointer, this method will return error code and does not execute the release.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Double pointer to the [`NN_TensorDesc`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] destroy tensor description successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to destroy tensor description. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>*tensorDesc</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_Destroy(tensorDesc: *mut *mut NN_TensorDesc) -> OH_NN_ReturnCode;
    /// Sets the name of a [`NN_TensorDesc`].
    ///
    /// After the [`NN_TensorDesc`] instance is created, call this method to set the tensor name.
    /// The value of <b>*name</b> is a C-style string ended with <b>'if <b>tensorDesc</b> or <b>name</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `name` - The name of the tensor that needs to be set.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor name successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor name. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>name</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_SetName(
        tensorDesc: *mut NN_TensorDesc,
        name: *const ::core::ffi::c_char,
    ) -> OH_NN_ReturnCode;
    /// Gets the name of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the name of the specified [`NN_TensorDesc`] instance.
    /// The value of <b>*name</b> is a C-style string ended with <b>'if <b>tensorDesc</b> or <b>name</b> is a null pointer, this method will return error code.
    /// As an output parameter, <b>*name</b> must be a null pointer, otherwise the method will return an error code.
    /// Fou example, you should define char* tensorName = NULL, and pass &tensorName as the argument of <b>name</b>.
    ///
    ///
    /// You do not need to release the memory of <b>name</b>. It will be released when <b>tensorDesc</b> is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `name` - The retured name of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor name successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor name. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>name</b> is nullptr, or <b>*name</b> is not nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetName(
        tensorDesc: *const NN_TensorDesc,
        name: *mut *const ::core::ffi::c_char,
    ) -> OH_NN_ReturnCode;
    /// Sets the data type of a [`NN_TensorDesc`].
    ///
    /// After the [`NN_TensorDesc`] instance is created, call this method to set the tensor data type.
    ///
    ///
    /// if <b>tensorDesc</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `dataType` - The data type of the tensor that needs to be set.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor data type successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor data type. The possible reason for failure
    /// is that the <b>tensorDesc</b> is nullptr, or <b>dataType</b> is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_SetDataType(
        tensorDesc: *mut NN_TensorDesc,
        dataType: OH_NN_DataType,
    ) -> OH_NN_ReturnCode;
    /// Gets the data type of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the data type of the specified [`NN_TensorDesc`] instance.
    ///
    ///
    /// if <b>tensorDesc</b> or <b>dataType</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `dataType` - The returned data type of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor data type successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor data type. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>dataType</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetDataType(
        tensorDesc: *const NN_TensorDesc,
        dataType: *mut OH_NN_DataType,
    ) -> OH_NN_ReturnCode;
    /// Sets the shape of a [`NN_TensorDesc`].
    ///
    /// After the [`NN_TensorDesc`] instance is created, call this method to set the tensor shape.
    ///
    ///
    /// if <b>tensorDesc</b> or <b>shape</b> is a null pointer, or <b>shapeLength</b> is 0, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `shape` - The shape list of the tensor that needs to be set.
    ///
    /// * `shapeLength` - The length of the shape list that needs to be set.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor shape successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor shape. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>shape</b> is nullptr, or <b>shapeLength</b> is 0.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_SetShape(
        tensorDesc: *mut NN_TensorDesc,
        shape: *const i32,
        shapeLength: usize,
    ) -> OH_NN_ReturnCode;
    /// Gets the shape of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the shape of the specified [`NN_TensorDesc`] instance.
    ///
    ///
    /// if <b>tensorDesc</b>, <b>shape</b> or <b>shapeLength</b> is a null pointer, this method will return error code.
    /// As an output parameter, <b>*shape</b> must be a null pointer, otherwise the method will return an error code.
    /// Fou example, you should define int32_t* tensorShape = NULL, and pass &tensorShape as the argument of <b>shape</b>.
    ///
    ///
    /// You do not need to release the memory of <b>shape</b>. It will be released when <b>tensorDesc</b> is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `shape` - Return the shape list of the tensor.
    ///
    /// * `shapeLength` - The returned length of the shape list.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor shape successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor shape. The possible reason for failure is that the
    /// <b>tensorDesc</b>, <b>shape</b> or <b>shapeLength</b> is nullptr, or <b>*shape</b> is not nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetShape(
        tensorDesc: *const NN_TensorDesc,
        shape: *mut *mut i32,
        shapeLength: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the format of a [`NN_TensorDesc`].
    ///
    /// After the [`NN_TensorDesc`] instance is created, call this method to set the tensor format.
    ///
    ///
    /// if <b>tensorDesc</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `format` - The format of the tensor that needs to be set.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor format successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor format. The possible reason for failure
    /// is that the <b>tensorDesc</b> is nullptr, or <b>format</b> is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_SetFormat(
        tensorDesc: *mut NN_TensorDesc,
        format: OH_NN_Format,
    ) -> OH_NN_ReturnCode;
    /// Gets the format of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the format of the specified [`NN_TensorDesc`] instance.
    ///
    ///
    /// if <b>tensorDesc</b> or <b>format</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `format` - The returned format of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor format successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor format. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>format</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetFormat(
        tensorDesc: *const NN_TensorDesc,
        format: *mut OH_NN_Format,
    ) -> OH_NN_ReturnCode;
    /// Gets the element count of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the element count of the specified [`NN_TensorDesc`] instance.
    /// If you need to obtain byte size of the tensor data, call [`OH_NNTensorDesc_GetByteSize`].
    ///
    ///
    /// If the tensor shape is dynamic, this method will return error code, and <b>elementCount</b> will be 0.
    ///
    ///
    /// if <b>tensorDesc</b> or <b>elementCount</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `elementCount` - The returned element count of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor element count successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor element count. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>elementCount</b> is nullptr.
    ///
    /// [`OH_NN_DYNAMIC_SHAPE`] dim is less than zero.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetElementCount(
        tensorDesc: *const NN_TensorDesc,
        elementCount: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Gets the byte size of a [`NN_TensorDesc`].
    ///
    /// Call this method to obtain the byte size of the specified [`NN_TensorDesc`] instance.
    ///
    ///
    /// If the tensor shape is dynamic, this method will return error code, and <b>byteSize</b> will be 0.
    ///
    ///
    /// If you need to obtain element count of the tensor data, call [`OH_NNTensorDesc_GetElementCount`].
    ///
    ///
    /// if <b>tensorDesc</b> or <b>byteSize</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `byteSize` - The returned byte size of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor byte size successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor byte size. The possible reason for failure
    /// is that the <b>tensorDesc</b> or <b>byteSize</b> is nullptr, or tensor data type is invalid.
    ///
    /// [`OH_NN_DYNAMIC_SHAPE`] dim is less than zero.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensorDesc_GetByteSize(
        tensorDesc: *const NN_TensorDesc,
        byteSize: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Creates a [`NN_Tensor`] instance from [`NN_TensorDesc`].
    ///
    /// This method use [`OH_NNTensorDesc_GetByteSize`] to calculate the byte size of tensor data and allocate shared
    /// memory on device for it. The device dirver will get the tensor data directly by the "zero-copy" way.
    ///
    ///
    /// Note that this method will copy the <b>tensorDesc</b> into [`NN_Tensor`]. Therefore you should destroy
    /// <b>tensorDesc</b> by [`OH_NNTensorDesc_Destroy`] if it is no longer used.
    ///
    ///
    /// If the tensor shape is dynamic, this method will return error code.
    ///
    ///
    /// <b>deviceID</b> indicates the selected device. If it is 0, the first device in the current device list will be used
    /// by default.
    ///
    ///
    /// <b>tensorDesc</b> must be provided, and this method will return an error code if it is a null pointer.
    ///
    ///
    /// Call [`OH_NNTensor_Destroy`] to release the [`NN_Tensor`] instance if it is no longer used.
    ///
    ///
    /// # Arguments
    ///
    /// * `deviceID` - Device id. If it is 0, the first device in the current device list will be used by default.
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`NN_Tensor`] instance, or NULL if it fails to create. The possible reason for failure
    /// is that the <b>tensorDesc</b> is nullptr, or <b>deviceID</b> is invalid, or memory error occurred.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_Create(deviceID: usize, tensorDesc: *mut NN_TensorDesc) -> *mut NN_Tensor;
    /// Creates a [`NN_Tensor`] instance with specified size and [`NN_TensorDesc`].
    ///
    /// This method use <b>size</b> as the byte size of tensor data and allocate shared memory on device for it.
    /// The device dirver will get the tensor data directly by the "zero-copy" way.
    ///
    ///
    /// Note that this method will copy the <b>tensorDesc</b> into [`NN_Tensor`]. Therefore you should destroy
    /// <b>tensorDesc</b> by [`OH_NNTensorDesc_Destroy`] if it is no longer used.
    ///
    ///
    /// <b>deviceID</b> indicates the selected device. If it is 0, the first device in the current device list will be used
    /// by default.
    ///
    ///
    /// <b>tensorDesc</b> must be provided, if it is a null pointer, the method returns an error code.
    /// <b>size</b> must be no less than the byte size of tensorDesc. Otherwise, this method will return an error code.
    /// If the tensor shape is dynamic, the <b>size</b> will not be checked.
    ///
    ///
    /// Call [`OH_NNTensor_Destroy`] to release the [`NN_Tensor`] instance if it is no longer used.
    ///
    ///
    /// # Arguments
    ///
    /// * `deviceID` - Device id. If it is 0, the first device in the current device list will be used by default.
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `size` - Size of tensor data that need to be allocated.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`NN_Tensor`] instance, or NULL if it fails to create. The possible reason for failure
    /// is that the <b>tensorDesc</b> is nullptr, or <b>deviceID</b> or size is invalid, or memory error occurred.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_CreateWithSize(
        deviceID: usize,
        tensorDesc: *mut NN_TensorDesc,
        size: usize,
    ) -> *mut NN_Tensor;
    /// Creates a [`NN_Tensor`] instance with specified file descriptor and [`NN_TensorDesc`].
    ///
    /// This method reuses the shared memory corresponding to the file descriptor <b>fd</b> passed. It may comes from another
    /// [`NN_Tensor`] instance. When you call the [`OH_NNTensor_Destroy`] method to release the tensor created by
    /// this method, the tensor data memory will not be released.
    ///
    ///
    /// Note that this method will copy the <b>tensorDesc</b> into [`NN_Tensor`]. Therefore you should destroy
    /// <b>tensorDesc</b> by [`OH_NNTensorDesc_Destroy`] if it is no longer used.
    ///
    ///
    /// <b>deviceID</b> indicates the selected device. If it is 0, the first device in the current device list will be used
    /// by default.
    ///
    ///
    /// <b>tensorDesc</b> must be provided, if it is a null pointer, the method returns an error code.
    ///
    ///
    /// Call [`OH_NNTensor_Destroy`] to release the [`NN_Tensor`] instance if it is no longer used.
    ///
    ///
    /// # Arguments
    ///
    /// * `deviceID` - Device id. If it is 0, the first device in the current device list will be used by default.
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance.
    ///
    /// * `fd` - file descriptor of the shared memory to be resued.
    ///
    /// * `size` - Size of the shared memory to be resued.
    ///
    /// * `offset` - Offset of the shared memory to be resued.
    ///
    /// # Returns
    ///
    /// * Pinter to a [`NN_Tensor`] instance, or NULL if it fails to create. The possible reason for failure
    /// is that the <b>tensorDesc</b> is nullptr, or <b>deviceID</b>, <b>fd</b>, <b>size</b> or <b>offset</b> is
    /// invalid, or memory error occurred.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_CreateWithFd(
        deviceID: usize,
        tensorDesc: *mut NN_TensorDesc,
        fd: ::core::ffi::c_int,
        size: usize,
        offset: usize,
    ) -> *mut NN_Tensor;
    /// Releases a [`NN_Tensor`] instance.
    ///
    /// When the [`NN_Tensor`] instance is no longer used, this method needs to be called to release the instance.
    /// Otherwise, the memory leak will occur.
    ///
    ///
    /// If <b>tensor</b> or <b>*tensor</b> is a null pointer, this method will return error code and does not execute the
    /// release.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Double pointer to the [`NN_Tensor`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] destroy tensor successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to destroy tensor. The possible reason for failure
    /// is that the <b>tensor</b> is nullptr, or <b>*tensor</b> is not nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_Destroy(tensor: *mut *mut NN_Tensor) -> OH_NN_ReturnCode;
    /// Gets the [`NN_TensorDesc`] instance of a [`NN_Tensor`].
    ///
    /// Call this method to obtain the inner [`NN_TensorDesc`] instance pointer of the specified [`NN_Tensor`]
    /// instance. You can get various types of the tensor attributes such as name/format/data type/shape from the returned
    /// [`NN_TensorDesc`] instance.
    ///
    ///
    /// You should not destory the returned [`NN_TensorDesc`] instance because it points to the inner instance of
    /// [`NN_Tensor`]. Otherwise, a menory corruption of double free will occur when [`OH_NNTensor_Destroy`]
    /// is called.
    ///
    ///
    /// if <b>tensor</b> is a null pointer, this method will return null pointer.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Pointer to the [`NN_Tensor`] instance.
    ///
    /// # Returns
    ///
    /// * Pointer to the [`NN_TensorDesc`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the <b>tensor</b> is nullptr, or <b>tensor</b> is invalid.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_GetTensorDesc(tensor: *const NN_Tensor) -> *mut NN_TensorDesc;
    /// Gets the data buffer of a [`NN_Tensor`].
    ///
    /// You can read/write data from/to the tensor data buffer. The buffer is mapped from a shared memory on device,
    /// so the device dirver will get the tensor data directly by this "zero-copy" way.
    ///
    ///
    /// Note that the real tensor data only uses the segment [offset, size) of the shared memory. The offset can be got by
    /// [`OH_NNTensor_GetOffset`] and the size can be got by [`OH_NNTensor_GetSize`].
    ///
    ///
    /// if <b>tensor</b> is a null pointer, this method will return null pointer.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Pointer to the [`NN_Tensor`] instance.
    ///
    /// # Returns
    ///
    /// * Pointer to data buffer of the tensor, or NULL if it fails to create. The possible reason for failure
    /// is that the <b>tensor</b> is nullptr, or <b>tensor</b> is invalid.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_GetDataBuffer(tensor: *const NN_Tensor) -> *mut ::core::ffi::c_void;
    /// Gets the file descriptor of the shared memory of a [`NN_Tensor`].
    ///
    /// The file descriptor <b>fd</b> corresponds to the shared memory of the tensor data, and can be resued
    /// by another [`NN_Tensor`] through [`OH_NNTensor_CreateWithFd`].
    ///
    ///
    /// if <b>tensor</b> or <b>fd</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Pointer to the [`NN_Tensor`] instance.
    ///
    /// * `fd` - The returned file descriptor of the shared memory.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor fd successfully. The return value is saved in parameter fd.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor fd. The possible reason for failure
    /// is that the <b>tensor</b> or <b>fd</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_GetFd(
        tensor: *const NN_Tensor,
        fd: *mut ::core::ffi::c_int,
    ) -> OH_NN_ReturnCode;
    /// Gets the size of the shared memory of a [`NN_Tensor`].
    ///
    /// The <b>size</b> corresponds to the shared memory of the tensor data, and can be resued by another [`NN_Tensor`]
    /// through [`OH_NNTensor_CreateWithFd`].
    ///
    ///
    /// The <b>size</b> is as same as the argument <b>size</b> of [`OH_NNTensor_CreateWithSize`] and
    /// [`OH_NNTensor_CreateWithFd`]. But for a tensor created by [`OH_NNTensor_Create`],
    /// it equals to the tensor byte size.
    ///
    ///
    /// Note that the real tensor data only uses the segment [offset, size) of the shared memory. The offset can be got by
    /// [`OH_NNTensor_GetOffset`] and the size can be got by [`OH_NNTensor_GetSize`].
    ///
    ///
    /// if <b>tensor</b> or <b>size</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Pointer to the [`NN_Tensor`] instance.
    ///
    /// * `size` - The returned size of tensor data.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor size successfully. The return value is saved in <b>size</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor size. The possible reason for failure
    /// is that the <b>tensor</b> or <b>size</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_GetSize(tensor: *const NN_Tensor, size: *mut usize) -> OH_NN_ReturnCode;
    /// Get the data offset of a tensor.
    ///
    /// The <b>offset</b> corresponds to the shared memory of the tensor data, and can be resued by another [`NN_Tensor`]
    /// through [`OH_NNTensor_CreateWithFd`].
    ///
    ///
    /// Note that the real tensor data only uses the segment [offset, size) of the shared memory. The offset can be got by
    /// [`OH_NNTensor_GetOffset`] and the size can be got by [`OH_NNTensor_GetSize`].
    ///
    ///
    /// if <b>tensor</b> or <b>offset</b> is a null pointer, this method will return error code.
    ///
    ///
    /// # Arguments
    ///
    /// * `tensor` - Pointer to the [`NN_Tensor`] instance.
    ///
    /// * `offset` - The returned offset of tensor data.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor offset successfully. The return value is saved in <b>offset</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor offset. The possible reason for failure
    /// is that the <b>tensor</b> or <b>offset</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNTensor_GetOffset(tensor: *const NN_Tensor, offset: *mut usize) -> OH_NN_ReturnCode;
    /// Creates an executor instance of the [`OH_NNExecutor`] type.
    ///
    /// This method constructs a model inference executor associated with the device based on the passed compilation.
    ///
    ///
    /// After the [`OH_NNExecutor`] instance is created, you can release the [`OH_NNCompilation`]
    /// instance if you do not need to create any other executors.
    ///
    ///
    /// # Arguments
    ///
    /// * `compilation` - Pointer to the [`OH_NNCompilation`] instance.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`OH_NNExecutor`] instance, or NULL if it fails to create. The possible reason for failure
    /// is that the <b>compilation</b> is nullptr, or memory error occurred.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNExecutor_Construct(compilation: *mut OH_NNCompilation) -> *mut OH_NNExecutor;
    /// Obtains the dimension information about the output tensor.
    ///
    /// After [`OH_NNExecutor_Run`] is called to complete a single inference, call this method to obtain the specified
    /// output dimension information and number of dimensions. It is commonly used in dynamic shape input and output
    /// scenarios.
    ///
    ///
    /// If the <b>outputIndex</b> is greater than or equal to the output tensor number, this method will return error code.
    /// The output tensor number can be got by [`OH_NNExecutor_GetOutputCount`].
    ///
    ///
    /// As an output parameter, <b>*shape</b> must be a null pointer, otherwise the method will return an error code.
    /// Fou example, you should define int32_t* tensorShape = NULL, and pass &tensorShape as the argument of <b>shape</b>.
    ///
    ///
    /// You do not need to release the memory of <b>shape</b>. It will be released when <b>executor</b> is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `outputIndex` - Output Index value, which is in the same sequence of the data output when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that <b>outputIndices</b> is <b>{4, 6, 8}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// When [`OH_NNExecutor_GetOutputShape`] is called to obtain dimension information about
    /// the output tensor, <b>outputIndices</b> is <b>{0, 1, 2}</b>.
    ///
    /// * `shape` - Pointer to the int32_t array. The value of each element in the array is the length of the output tensor
    /// in each dimension.
    ///
    /// * `shapeLength` - Pointer to the uint32_t type. The number of output dimensions is returned.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get tensor output shape successfully. The return value is saved in
    /// <b>shape</b> and <b>shapeLength</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get tensor output shape. The possible reason for failure is that
    /// the <b>executor</b>, <b>shape</b> or <b>shapeLength</b> is nullptr, or <b>*shape</b> is not nullptr,
    /// or <b>outputIndex</b> is out of range.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNExecutor_GetOutputShape(
        executor: *mut OH_NNExecutor,
        outputIndex: u32,
        shape: *mut *mut i32,
        shapeLength: *mut u32,
    ) -> OH_NN_ReturnCode;
    /// Destroys an executor instance to release the memory occupied by the executor.
    ///
    /// This method needs to be called to release the executor instance created by calling [`OH_NNExecutor_Construct`]. Otherwise,
    /// the memory leak will occur.
    ///
    ///
    /// If <b>executor</b> or <b>*executor</b> is a null pointer, this method only prints warning logs and does not execute the release.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Double pointer to the [`OH_NNExecutor`] instance.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNExecutor_Destroy(executor: *mut *mut OH_NNExecutor);
    /// Gets the input tensor count.
    ///
    /// You can get the input tensor count from the executor, and then create an input tensor descriptor with its index by
    /// [`OH_NNExecutor_CreateInputTensorDesc`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputCount` - Input tensor count returned.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get input count successfully. The return value is saved in <b>inputCount</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get input count. The possible reason for failure is that
    /// the <b>executor</b> or <b>inputCount</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_GetInputCount(
        executor: *const OH_NNExecutor,
        inputCount: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Gets the output tensor count.
    ///
    /// You can get the output tensor count from the executor, and then create an output tensor descriptor with its index by
    /// [`OH_NNExecutor_CreateOutputTensorDesc`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `outputCount` - Output tensor count returned.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get output count successfully. The return value is saved in <b>outputCount</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get output count. The possible reason for failure is that
    /// the <b>executor</b> or <b>outputCount</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_GetOutputCount(
        executor: *const OH_NNExecutor,
        outputCount: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Creates an input tensor descriptor with its index.
    ///
    /// The input tensor descriptor contains all attributes of the input tensor.
    /// If the <b>index</b> is greater than or equal to the input tensor number, this method will return error code.
    /// The input tensor number can be got by [`OH_NNExecutor_GetInputCount`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `index` - Input tensor index.
    ///
    /// # Returns
    ///
    /// * Pointer to [`NN_TensorDesc`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the <b>executor</b> is nullptr, or <b>index</b> is out of range.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_CreateInputTensorDesc(
        executor: *const OH_NNExecutor,
        index: usize,
    ) -> *mut NN_TensorDesc;
    /// Creates an output tensor descriptor with its index.
    ///
    /// The output tensor descriptor contains all attributes of the output tensor.
    /// If the <b>index</b> is greater than or equal to the output tensor number, this method will return error code.
    /// The output tensor number can be got by [`OH_NNExecutor_GetOutputCount`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `index` - Output tensor index.
    ///
    /// # Returns
    ///
    /// * Pointer to [`NN_TensorDesc`] instance, or NULL if it fails to create. The possible reason for
    /// failure is that the <b>executor</b> is nullptr, or <b>index</b> is out of range.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_CreateOutputTensorDesc(
        executor: *const OH_NNExecutor,
        index: usize,
    ) -> *mut NN_TensorDesc;
    /// Gets the dimension ranges of an input tensor.
    ///
    /// The supported dimension ranges of an input tensor with dynamic shape may be different among various devices.
    /// You can call this method to get the dimension ranges of the input tensor supported by the device.
    /// <b>*minInputDims</b> contains the minimum demensions of the input tensor, and <b>*maxInputDims</b> contains the
    /// maximum, e.g. if an input tensor has dynamic shape [-1, -1, -1, 3], its <b>*minInputDims</b> may be [1, 10, 10, 3]
    /// and <b>*maxInputDims</b> may be [100, 1024, 1024, 3] on the device.
    ///
    ///
    /// If the <b>index</b> is greater than or equal to the input tensor number, this method will return error code.
    /// The input tensor number can be got by [`OH_NNExecutor_GetInputCount`].
    ///
    ///
    /// As an output parameter, <b>*minInputDims</b> or <b>*maxInputDims</b> must be a null pointer, otherwise the method
    /// will return an error code. For example, you should define int32_t* minInDims = NULL, and pass &minInDims as the
    /// argument of <b>minInputDims</b>.
    ///
    ///
    /// You do not need to release the memory of <b>*minInputDims</b> or <b>*maxInputDims</b>.
    /// It will be released when <b>executor</b> is destroied.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `index` - Input tensor index.
    ///
    /// * `minInputDims` - Returned pointer to an array contains the minimum dimensions of the input tensor.
    ///
    /// * `maxInputDims` - Returned pointer to an array contains the maximum dimensions of the input tensor.
    ///
    /// * `shapeLength` - Returned length of the shape of input tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get input dim range successfully. The return value is saved in <b>minInputDims</b>,
    /// <b>maxInputDims</b> and <b>shapeLength</b>.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get input dim range. The possible reason for failure is that
    /// the <b>executor</b>, <b>minInputDims</b>, <b>maxInputDims</b> or <b>shapeLength</b> is nullptr, or
    /// <b>*minInputDims</b> or <b>*maxInputDims</b> is not nullptr, or <b>index</b> is out of range.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to get input dim range.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_GetInputDimRange(
        executor: *const OH_NNExecutor,
        index: usize,
        minInputDims: *mut *mut usize,
        maxInputDims: *mut *mut usize,
        shapeLength: *mut usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the callback function handle for the post-process when the asynchronous execution has been done.
    ///
    /// The definition fo the callback function: [`NN_OnRunDone`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `onRunDone` - Callback function handle [`NN_OnRunDone`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set on run done successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set on run done. The possible reason for failure is that
    /// the <b>executor</b> or <b>onRunDone</b> is nullptr.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to set on run done.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_SetOnRunDone(
        executor: *mut OH_NNExecutor,
        onRunDone: NN_OnRunDone,
    ) -> OH_NN_ReturnCode;
    /// Sets the callback function handle for the post-process when the device driver service is dead during asynchronous execution.
    ///
    /// The definition fo the callback function: [`NN_OnServiceDied`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `onServiceDied` - Callback function handle [`NN_OnServiceDied`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set on service died successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set on service died. The possible reason for failure is that
    /// the <b>executor</b> or <b>onServiceDied</b> is nullptr.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to set on service died.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_SetOnServiceDied(
        executor: *mut OH_NNExecutor,
        onServiceDied: NN_OnServiceDied,
    ) -> OH_NN_ReturnCode;
    /// Synchronous execution of the model inference.
    ///
    /// Input and output tensors should be created first by [`OH_NNTensor_Create`], [`OH_NNTensor_CreateWithSize`] or
    /// [`OH_NNTensor_CreateWithFd`]. And then the input tensors data which is got by [`OH_NNTensor_GetDataBuffer`] must be filled.
    /// The executor will then yield out the results by inference execution and fill them into output tensors data for you to read.
    ///
    ///
    /// In the case of dynamic shape, you can get the real output shape directly by [`OH_NNExecutor_GetOutputShape`], or you
    /// can create a tensor descriptor from an output tensor by [`OH_NNTensor_GetTensorDesc`], and then read its real shape
    /// by [`OH_NNTensorDesc_GetShape`].
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputTensor` - An array of input tensors [`NN_Tensor`].
    ///
    /// * `inputCount` - Number of input tensors.
    ///
    /// * `outputTensor` - An array of output tensors [`NN_Tensor`].
    ///
    /// * `outputCount` - Number of output tensors.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] run successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to run. The possible reason for failure is that the <b>executor</b>,
    /// <b>inputTensor</b> or <b>outputTensor</b> is nullptr, or <b>inputCount</b> or <b>outputCount</b> is 0.
    ///
    /// [`OH_NN_FAILED`] the backend device failed to run.
    ///
    /// [`OH_NN_NULL_PTR`] the parameters of input or output tensor is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_RunSync(
        executor: *mut OH_NNExecutor,
        inputTensor: *mut *mut NN_Tensor,
        inputCount: usize,
        outputTensor: *mut *mut NN_Tensor,
        outputCount: usize,
    ) -> OH_NN_ReturnCode;
    /// Asynchronous execution of the model inference.
    ///
    /// Input and output tensors should be created first by [`OH_NNTensor_Create`], [`OH_NNTensor_CreateWithSize`] or
    /// [`OH_NNTensor_CreateWithFd`]. And then the input tensors data which is got by [`OH_NNTensor_GetDataBuffer`]
    /// must be filled. The executor will yield out the results by inference execution and fill them into output tensors data
    /// for you to read.
    ///
    ///
    /// In the case of dynamic shape, you can get the real output shape directly by [`OH_NNExecutor_GetOutputShape`], or
    /// you can create a tensor descriptor from an output tensor by [`OH_NNTensor_GetTensorDesc`], and then read its real
    /// shape by [`OH_NNTensorDesc_GetShape`].
    ///
    ///
    /// The method is non-blocked and will return immediately.
    ///
    ///
    /// The callback function handles are set by [`OH_NNExecutor_SetOnRunDone`]
    /// and [`OH_NNExecutor_SetOnServiceDied`]. The inference results and error code can be got by
    /// [`NN_OnRunDone`]. And you can deal with the abnormal termination of device driver service during
    /// asynchronous execution by [`NN_OnServiceDied`].
    ///
    ///
    /// If the execution time reaches the <b>timeout</b>, the execution will be terminated
    /// with no outputs, and the <b>errCode<b> returned in callback function [`NN_OnRunDone`] will be
    /// [`OH_NN_TIMEOUT`].
    ///
    ///
    /// The <b>userData</b> is asynchronous execution identifier and will be returned as the first parameter of the callback
    /// function. You can input any value you want as long as it can identify different asynchronous executions.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputTensor` - An array of input tensors [`NN_Tensor`].
    ///
    /// * `inputCount` - Number of input tensors.
    ///
    /// * `outputTensor` - An array of output tensors [`NN_Tensor`].
    ///
    /// * `outputCount` - Number of output tensors.
    ///
    /// * `timeout` - Time limit (millisecond) of the asynchronous execution, e.g. 1000.
    ///
    /// * `userData` - Asynchronous execution identifier.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] run successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to run. The possible reason for failure is that the <b>executor</b>,
    /// <b>inputTensor</b>, <b>outputTensor</b> or <b>userData</b> is nullptr, or <b>inputCount</b> or
    /// <b>outputCount</b> is 0.
    ///
    /// [`OH_NN_FAILED`] the backend device failed to run.
    ///
    /// [`OH_NN_NULL_PTR`] the parameters of input or output tensor is invalid.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] the backend device is not supported to run async.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNExecutor_RunAsync(
        executor: *mut OH_NNExecutor,
        inputTensor: *mut *mut NN_Tensor,
        inputCount: usize,
        outputTensor: *mut *mut NN_Tensor,
        outputCount: usize,
        timeout: i32,
        userData: *mut ::core::ffi::c_void,
    ) -> OH_NN_ReturnCode;
    /// Obtains the IDs of all devices connected.
    ///
    /// Each device has an unique and fixed ID. This method returns device IDs on the current device through the uint32_t
    /// array.
    ///
    ///
    /// Device IDs are returned through the size_t array. Each element of the array is the ID of a single device.
    ///
    ///
    /// The array memory is managed inside, so you do not need to care about it.
    /// The data pointer is valid before this method is called next time.
    ///
    ///
    /// # Arguments
    ///
    /// * `allDevicesID` - Pointer to the size_t array. The input <b>*allDevicesID</b> must be a null pointer.
    /// Otherwise, [`OH_NN_INVALID_PARAMETER`] is returned.
    ///
    /// * `deviceCount` - Pointer of the uint32_t type, which is used to return the length of <b>*allDevicesID</b>.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get all devices id successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get all devices id. The possible reason for failure is that
    /// the <b>allDevicesID</b> or <b>deviceCount</b> is nullptr, or <b>*allDevicesID</b> is not nullptr.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNDevice_GetAllDevicesID(
        allDevicesID: *mut *const usize,
        deviceCount: *mut u32,
    ) -> OH_NN_ReturnCode;
    /// Obtains the name of the specified device.
    ///
    /// <b>deviceID</b> specifies the device whose name will be obtained. The device ID needs to be obtained by calling
    /// [`OH_NNDevice_GetAllDevicesID`].
    /// If it is 0, the first device in the current device list will be used by default.
    ///
    ///
    /// The value of <b>*name</b> is a C-style string ended with <b>'<b>*name</b> must be a null pointer.
    /// Otherwise, [`OH_NN_INVALID_PARAMETER`] is returned.
    /// Fou example, you should define char* deviceName = NULL, and pass &deviceName as the argument of <b>name</b>.
    ///
    ///
    /// # Arguments
    ///
    /// * `deviceID` - Device ID. If it is 0, the first device in the current device list will be used by default.
    ///
    /// * `name` - The device name returned.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get name of specific device successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get name of specific device. The possible reason for failure is that
    /// the <b>name</b> is nullptr or <b>*name</b> is not nullptr.
    ///
    /// [`OH_NN_FAILED`] fail to get name of specific device. The possible reason for failure is that
    /// the <b>deviceID</b> is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNDevice_GetName(
        deviceID: usize,
        name: *mut *const ::core::ffi::c_char,
    ) -> OH_NN_ReturnCode;
    /// Obtains the type information of the specified device.
    ///
    /// <b>deviceID</b> specifies the device whose type will be obtained. If it is 0, the first device in the current device
    /// list will be used. Currently the following device types are supported:
    /// - <b>OH_NN_CPU</b>: CPU device.
    /// - <b>OH_NN_GPU</b>: GPU device.
    /// - <b>OH_NN_ACCELERATOR</b>: machine learning dedicated accelerator.
    /// - <b>OH_NN_OTHERS</b>: other hardware types.
    ///
    ///
    /// # Arguments
    ///
    /// * `deviceID` - Device ID. If it is 0, the first device in the current device list will be used by default.
    ///
    /// * `deviceType` - The device type [`OH_NN_DeviceType`] returned.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get type of specific device successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get type of specific device. The possible reason for failure is that
    /// the <b>deviceType</b> is nullptr.
    ///
    /// [`OH_NN_FAILED`] fail to get type of specific device. The possible reason for failure is that
    /// the <b>deviceID</b> is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNDevice_GetType(
        deviceID: usize,
        deviceType: *mut OH_NN_DeviceType,
    ) -> OH_NN_ReturnCode;
    /// Creates a [`NN_QuantParam`] instance.
    ///
    /// After the [`NN_QuantParam`] instance is created, call [`OH_NNQuantParam_SetScales`], [`OH_NNQuantParam_SetZeroPoints`],
    /// [`OH_NNQuantParam_SetNumBits`] to set its attributes, and then call [`OH_NNModel_SetTensorQuantParams`] to set it
    /// to a tensor. After that you should destroy it by calling [`OH_NNQuantParam_Destroy`] to avoid memory leak.
    ///
    ///
    ///
    /// # Returns
    ///
    /// * Pointer to a [`NN_QuantParam`] instance, or NULL if it fails to create.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNQuantParam_Create() -> *mut NN_QuantParam;
    /// Sets the scales of the [`NN_QuantParam`] instance.
    ///
    /// The parameter <b>quantCount</b> is the number of quantization parameters of a tensor, e.g. the quantCount is the
    /// channel count if the tensor is per-channel quantized.
    ///
    ///
    /// # Arguments
    ///
    /// * `quantParams` - Pointer to the [`NN_QuantParam`] instance.
    ///
    /// * `scales` - An array of scales for all quantization parameters of the tensor.
    ///
    /// * `quantCount` - Number of quantization parameters of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set scales of quant parameters successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set scales of quant parameters. The possible reason for failure
    /// is that the <b>quantParams</b> or <b>scales</b> is nullptr, or <b>quantCount</b> is 0.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNQuantParam_SetScales(
        quantParams: *mut NN_QuantParam,
        scales: *const f64,
        quantCount: usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the zero points of the [`NN_QuantParam`] instance.
    ///
    /// The parameter <b>quantCount</b> is the number of quantization parameters of a tensor, e.g. the quantCount is the
    /// channel count if the tensor is per-channel quantized.
    ///
    ///
    /// # Arguments
    ///
    /// * `quantParams` - Pointer to the [`NN_QuantParam`] instance.
    ///
    /// * `zeroPoints` - An array of zero points for all quantization parameters of the tensor.
    ///
    /// * `quantCount` - Number of quantization parameters of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set zero points of quant parameters successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set zero points of quant parameters. The possible reason for failure
    /// is that the <b>quantParams</b> or <b>zeroPoints</b> is nullptr, or <b>quantCount</b> is 0.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNQuantParam_SetZeroPoints(
        quantParams: *mut NN_QuantParam,
        zeroPoints: *const i32,
        quantCount: usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the number bits of the [`NN_QuantParam`] instance.
    ///
    /// The parameter <b>quantCount</b> is the number of quantization parameters of a tensor, e.g. the quantCount is the
    /// channel count if the tensor is per-channel quantized.
    ///
    ///
    /// # Arguments
    ///
    /// * `quantParams` - Pointer to the [`NN_QuantParam`] instance.
    ///
    /// * `numBits` - An array of number bits for all quantization parameters of the tensor.
    ///
    /// * `quantCount` - Number of quantization parameters of the tensor.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set num bits of quant parameters successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set num bits of quant parameters. The possible reason for failure
    /// is that the <b>quantParams</b> or <b>numBits</b> is nullptr, or <b>quantCount</b> is 0.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNQuantParam_SetNumBits(
        quantParams: *mut NN_QuantParam,
        numBits: *const u32,
        quantCount: usize,
    ) -> OH_NN_ReturnCode;
    /// Releases a [`NN_QuantParam`] instance.
    ///
    /// The [`NN_QuantParam`] instance needs to be released to avoid memory leak after it is set to a [`NN_TensorDesc`].
    ///
    ///
    /// If <b>quantParams</b> or <b>*quantParams</b> is a null pointer, this method only prints warning logs and does not
    /// execute the release.
    ///
    ///
    /// # Arguments
    ///
    /// * `quantParams` - Double pointer to the [`NN_QuantParam`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] destroy quant parameters object successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to destroy quant parameters object. The possible reason for failure
    /// is that the <b>quantParams</b> or <b>*quantParams</b> is nullptr.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNQuantParam_Destroy(quantParams: *mut *mut NN_QuantParam) -> OH_NN_ReturnCode;
    /// Creates a model instance of the [`OH_NNModel`] type and uses other APIs provided by OH_NNModel to construct the model instance.
    ///
    /// Before composition, call [`OH_NNModel_Construct`] to create a model instance. Based on the model topology,
    /// call the [`OH_NNModel_AddTensorToModel`], [`OH_NNModel_AddOperation`], and [`OH_NNModel_SetTensorData`] methods
    /// to fill in the data and operator nodes of the model, and then call [`OH_NNModel_SpecifyInputsAndOutputs`] to specify the inputs and outputs of the model.
    /// After the model topology is constructed, call [`OH_NNModel_Finish`] to build the model.
    ///
    ///
    /// After a model instance is no longer used, you need to destroy it by calling [`OH_NNModel_Destroy`] to avoid memory leak.
    ///
    ///
    ///
    /// # Returns
    ///
    /// * Pointer to a [`OH_NNModel`] instance, or NULL if it fails to create.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_Construct() -> *mut OH_NNModel;
    /// Adds a tensor to the model instance.
    ///
    /// The data node and operator parameters in the Neural Network Runtime model are composed of tensors of the model.
    /// This method is used to add tensors to a model instance based on the <b>tensorDesc</b> parameter with type of [`NN_TensorDesc`].
    /// [`NN_TensorDesc`] contains some attributes such as shape, format, data type and provides corresponding APIs to access them.
    /// The order of adding tensors is specified by the indices recorded in the model. The [`OH_NNModel_SetTensorData`], [`OH_NNModel_AddOperation`],
    /// and [`OH_NNModel_SpecifyInputsAndOutputs`] methods specify tensors based on the indices.
    ///
    ///
    /// Neural Network Runtime supports inputs and outputs of the dynamic shape. When adding a data node with a dynamic shape,
    /// you need to set the dimensions that support dynamic changes to <b>-1</b>.
    /// For example, if the shape of a four-dimensional tensor is set to <b>[1, -1, 2, 2]</b>, the second dimension supports dynamic changes.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `tensorDesc` - Pointer to the [`NN_TensorDesc`] instance. The tensor descriptor specifies the attributes of the tensor added to the model instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] add tensor to model successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to add tensor to model. The possible reason for failure
    /// is that the <b>model</b> or <b>tensorDesc</b> is nullptr.
    ///
    /// [`OH_NN_MEMORY_ERROR`] fail to add tensor to model. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNModel_AddTensorToModel(
        model: *mut OH_NNModel,
        tensorDesc: *const NN_TensorDesc,
    ) -> OH_NN_ReturnCode;
    /// Sets the tensor value.
    ///
    /// For tensors with constant values (such as model weights), you need to use this method to set their data.
    /// The index of a tensor is determined by the order in which the tensor is added to the model.
    /// For details about how to add a tensor, see [`OH_NNModel_AddTensorToModel`].
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `index` - Index of a tensor.
    ///
    /// * `dataBuffer` - Pointer to real data.
    ///
    /// * `length` - Length of the data buffer.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor data successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor data. The possible reason for failure is that the
    /// <b>model</b> or <b>dataBuffer</b> is nullptr, or <b>length</b> is 0, or <b>index</b> is out of range.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to set tensor data. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_SetTensorData(
        model: *mut OH_NNModel,
        index: u32,
        dataBuffer: *const ::core::ffi::c_void,
        length: usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the quantization parameter of a tensor.
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `index` - Index of a tensor.
    ///
    /// * `quantParam` - Pointer to the quantization parameter instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor quant parameters successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor quant parameters. The possible reason for failure
    /// is that the <b>model</b> or <b>quantParam</b> is nullptr, or <b>index</b> is out of range.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to set tensor quant parameters. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNModel_SetTensorQuantParams(
        model: *mut OH_NNModel,
        index: u32,
        quantParam: *mut NN_QuantParam,
    ) -> OH_NN_ReturnCode;
    /// Sets the tensor type. See [`OH_NN_TensorType`] for details.
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `index` - Index of a tensor.
    ///
    /// * `tensorType` - Tensor type of [`OH_NN_TensorType`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set tensor type successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set tensor type. The possible reason for failure
    /// is that the <b>model</b> is nullptr, or <b>index</b> is out of range, or <b>tensorType</b> is invalid.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to set tensor type. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NNModel_SetTensorType(
        model: *mut OH_NNModel,
        index: u32,
        tensorType: OH_NN_TensorType,
    ) -> OH_NN_ReturnCode;
    /// Adds an operator to a model instance.
    ///
    /// This method is used to add an operator to a model instance. The operator type is specified by <b>op</b>, and
    /// the operator parameters, inputs, and outputs are specified by <b>paramIndices</b>, <b>inputIndices</b>, and <b>outputIndices</b> respectively.
    /// This method verifies the attributes of operator parameters and the number of input and output parameters.
    /// These attributes must be correctly set when [`OH_NNModel_AddTensorToModel`] is called to add tensors.
    /// For details about the expected parameters, input attributes, and output attributes of each operator, see [`OH_NN_OperationType`].
    ///
    ///
    /// <b>paramIndices</b>, <b>inputIndices</b>, and <b>outputIndices</b> store the indices of tensors.
    /// The indices are determined by the order in which tensors are added to the model.
    /// For details about how to add a tensor, see [`OH_NNModel_AddTensorToModel`].
    ///
    ///
    /// If unnecessary parameters are added when adding an operator, this method returns [`OH_NN_INVALID_PARAMETER`].
    /// If no operator parameter is set, the operator uses the default parameter value.
    /// For details about the default values, see [`OH_NN_OperationType`].
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `op` - Specifies the type of an operator to be added. For details, see the enumerated values of [`OH_NN_OperationType`].
    ///
    /// * `paramIndices` - Pointer to the <b>OH_NN_UInt32Array</b> instance, which is used to set operator parameters.
    ///
    /// * `inputIndices` - Pointer to the <b>OH_NN_UInt32Array</b> instance, which is used to set the operator input.
    ///
    /// * `outputIndices` - Pointer to the <b>OH_NN_UInt32Array</b> instance, which is used to set the operator output.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] add operation to model successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to add operation to model. The possible reason for failure is that the
    /// <b>model</b>, <b>paramIndices</b>, <b>inputIndices</b> or <b>outputIndices</b> is nullptr, or parameters are
    /// invalid.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to add operation to model. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_AddOperation(
        model: *mut OH_NNModel,
        op: OH_NN_OperationType,
        paramIndices: *const OH_NN_UInt32Array,
        inputIndices: *const OH_NN_UInt32Array,
        outputIndices: *const OH_NN_UInt32Array,
    ) -> OH_NN_ReturnCode;
    /// Specifies the inputs and outputs of a model.
    ///
    /// A tensor must be specified as the end-to-end inputs and outputs of a model instance. This type of tensor cannot be set
    /// using [`OH_NNModel_SetTensorData`].
    ///
    ///
    /// The index of a tensor is determined by the order in which the tensor is added to the model.
    /// For details about how to add a tensor, see [`OH_NNModel_AddTensorToModel`].
    ///
    ///
    /// Currently, the model inputs and outputs cannot be set asynchronously.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `inputIndices` - Pointer to the <b>OH_NN_UInt32Array</b> instance, which is used to set the operator input.
    ///
    /// * `outputIndices` - Pointer to the <b>OH_NN_UInt32Array</b> instance, which is used to set the operator output.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] specify inputs and outputs successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to specify inputs and outputs. The possible reason for failure is that
    /// the <b>model</b>, <b>inputIndices</b> or <b>outputIndices</b> is nullptr, or parameters are invalid.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to specify inputs and outputs. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_SpecifyInputsAndOutputs(
        model: *mut OH_NNModel,
        inputIndices: *const OH_NN_UInt32Array,
        outputIndices: *const OH_NN_UInt32Array,
    ) -> OH_NN_ReturnCode;
    /// Completes model composition.
    ///
    /// After the model topology is set up, call this method to indicate that the composition is complete. After this method is called,
    /// additional composition operations cannot be performed. If [`OH_NNModel_AddTensorToModel`], [`OH_NNModel_AddOperation`],
    /// [`OH_NNModel_SetTensorData`], and [`OH_NNModel_SpecifyInputsAndOutputs`] are called,
    /// [`OH_NN_OPERATION_FORBIDDEN`] is returned.
    ///
    ///
    /// Before calling [`OH_NNModel_GetAvailableOperations`] and [`OH_NNCompilation_Construct`],
    /// you must call this method to complete composition.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] the composition is complete successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] composition failed. The possible reason for failure
    /// is that the <b>model</b> is nullptr, or parameters set before are invalid.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] composition failed. The possible reason for failure
    /// is that the model is invalid.
    ///
    /// [`OH_NN_MEMORY_ERROR`] composition failed. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_Finish(model: *mut OH_NNModel) -> OH_NN_ReturnCode;
    /// Releases a model instance.
    ///
    /// This method needs to be called to release the model instance created by calling [`OH_NNModel_Construct`]. Otherwise, memory leak will occur.
    ///
    ///
    /// If <b>model</b> or <b>*model</b> is a null pointer, this method only prints warning logs and does not execute the release.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Double pointer to the [`OH_NNModel`] instance. After a model instance is destroyed, this method sets <b>*model</b> to a null pointer.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_Destroy(model: *mut *mut OH_NNModel);
    /// Queries whether the device supports operators in the model. The support status is indicated by the Boolean value.
    ///
    /// Queries whether underlying device supports operators in a model instance. The device is specified by <b>deviceID</b>,
    /// and the result is represented by the array pointed by <b>isSupported</b>. If the <i>i</i>th operator is supported,
    /// the value of <b>(*isSupported)</b>[<i>i</i>] is <b>true</b>. Otherwise, the value is <b>false</b>.
    ///
    ///
    /// After this method is successfully executed, <b>(*isSupported)</b> points to the bool array that records the operator support status.
    /// The operator quantity for the array length is the same as that for the model instance. The memory corresponding to this array is
    /// managed by Neural Network Runtime and is automatically destroyed after the model instance is destroyed or this method is called again.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `deviceID` - Device ID to be queried, which can be obtained by using [`OH_NNDevice_GetAllDevicesID`].
    ///
    /// * `isSupported` - Pointer to the bool array. When this method is called, <b>(*isSupported)</b> must be a null pointer.
    /// Otherwise, [`OH_NN_INVALID_PARAMETER`] is returned.
    ///
    /// * `opCount` - Number of operators in a model instance, corresponding to the length of the <b>(*isSupported)</b> array.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] get available operations successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to get available operations. The possible reason for failure
    /// is that the <b>model</b>, <b>isSupported</b> or <b>opCount</b> is nullptr, or <b>*isSupported</b> is
    /// not nullptr.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to get available operations. The possible reason for failure
    /// is that the model is invalid.
    ///
    /// [`OH_NN_FAILED`] fail to get available operations. The possible reason for failure
    /// is that the <b>deviceID</b> is invalid.
    ///
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NNModel_GetAvailableOperations(
        model: *mut OH_NNModel,
        deviceID: usize,
        isSupported: *mut *const bool,
        opCount: *mut u32,
    ) -> OH_NN_ReturnCode;
    /// Adds a tensor to a model instance.
    ///
    /// The data node and operator parameters in the Neural Network Runtime model are composed of tensors of the model.
    /// This method is used to add tensors to a model instance based on the <b>tensor</b> parameter.
    /// The sequence of adding tensors is specified by the index value recorded in the model.
    /// The [`OH_NNModel_SetTensorData`], [`OH_NNModel_AddOperation`],
    /// and [`OH_NNModel_SpecifyInputsAndOutputs`] methods specifies tensors based on the index value.
    ///
    ///
    /// Neural Network Runtime supports inputs and outputs of the dynamic shape. When adding a data node with a dynamic
    /// shape, you need to set the dimensions that support dynamic changes in <b>tensor.dimensions</b> to <b>-1</b>.
    /// For example, if <b>tensor.dimensions</b> of a four-dimensional tensor is set to <b>[1, -1, 2, 2]</b>,
    /// the second dimension supports dynamic changes.
    ///
    ///
    /// # Arguments
    ///
    /// * `model` - Pointer to the [`OH_NNModel`] instance.
    ///
    /// * `tensor` - Pointer to the [`OH_NN_Tensor`] tensor. The tensor specifies the attributes of the tensor added to
    /// the model instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] add tensor to model successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to add tensor to model. The possible reason for failure
    /// is that the <b>model</b> or <b>tensor</b> is nullptr.
    ///
    /// [`OH_NN_OPERATION_FORBIDDEN`] fail to add tensor to model. The possible reason for failure
    /// is that the model is invalid.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNModel_AddTensorToModel`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNModel_AddTensorToModel`]")]
    pub fn OH_NNModel_AddTensor(
        model: *mut OH_NNModel,
        tensor: *const OH_NN_Tensor,
    ) -> OH_NN_ReturnCode;
    /// Sets the single input data for a model.
    ///
    /// This method copies the data whose length is specified by <b>length</b> (in bytes) in <b>dataBuffer</b> to the shared
    /// memory of the underlying device. <b>inputIndex</b> specifies the input to be set and <b>tensor</b> sets information
    /// such as the input shape, type, and quantization parameters.
    ///
    ///
    /// Neural Network Runtime supports models with dynamical shape input. For fixed shape input and dynamic shape input
    /// scenarios, this method uses different processing policies.
    ///
    ///
    /// - Fixed shape input: The attributes of <b>tensor</b> must be the same as those of the tensor added by calling
    /// [`OH_NNModel_AddTensor`] in the composition phase.
    /// - Dynamic shape input: In the composition phase, because the shape is not fixed, each value in
    /// <b>tensor.dimensions</b> must be greater than <b>0</b> in the method calls to determine the shape input in the
    /// calculation phase. When setting the shape, you can modify only the dimension whose value is <b>-1</b>.
    /// Assume that <b>[-1, 224, 224, 3]</b> is input as the the dimension of A in the composition phase.
    /// When this method is called, only the size of the first dimension can be modified, e.g. to <b>[3, 224, 224, 3]</b>.
    /// If other dimensions are adjusted, [`OH_NN_INVALID_PARAMETER`] is returned.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputIndex` - Input index value, which is in the same sequence of the data input when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>inputIndices</b> is <b>{1, 5, 9}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In input settings, the index value for the three inputs is <b>{0, 1, 2}</b>.
    ///
    ///
    /// * `tensor` - Sets the tensor corresponding to the input data.
    ///
    /// * `dataBuffer` - Pointer to the input data.
    ///
    /// * `length` - Length of the data buffer, in bytes.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set model input successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set model input. The possible reason for failure
    /// is that the <b>executor</b>, <b>tensor</b> or <b>dataBuffer</b> is nullptr, or <b>inputIndex</b>
    /// is out of range, or <b>length</b> is 0.
    ///
    /// [`OH_NN_MEMORY_ERROR`] fail to set model input. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNExecutor_RunSync`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNExecutor_RunSync`]")]
    pub fn OH_NNExecutor_SetInput(
        executor: *mut OH_NNExecutor,
        inputIndex: u32,
        tensor: *const OH_NN_Tensor,
        dataBuffer: *const ::core::ffi::c_void,
        length: usize,
    ) -> OH_NN_ReturnCode;
    /// Sets the buffer for a single output of a model.
    ///
    /// This method binds the buffer to which <b>dataBuffer</b> points to the output specified by <b>outputIndex</b>.
    /// The length of the buffer is specified by <b>length</b>.
    ///
    ///
    /// After [`OH_NNExecutor_Run`] is called to complete a single model inference, Neural Network Runtime compares
    /// the length of the buffer to which <b>dataBuffer</b> points with the length of the output data and returns different
    /// results based on the actual situation.
    ///
    ///
    /// - If the buffer length is greater than or equal to the data length, the inference result is copied to the buffer and
    /// [`OH_NN_SUCCESS`] is returned. You can read the inference result from <b>dataBuffer</b>.
    /// - If the buffer length is smaller than the data length, [`OH_NNExecutor_Run`] returns
    /// [`OH_NN_INVALID_PARAMETER`] and generates a log indicating that the buffer is too small.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `outputIndex` - Output Index value, which is in the same sequence of the data output when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>outputIndices</b> is <b>{4, 6, 8}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In output buffer settings, the index value for the three outputs is <b>{0, 1, 2}</b>.
    ///
    /// * `dataBuffer` - Pointer to the output data.
    ///
    /// * `length` - Length of the data buffer, in bytes.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set model output successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set model output. The possible reason for failure
    /// is that the <b>executor</b>, <b>tensor</b> or <b>dataBuffer</b> is nullptr, or <b>outputIndex</b>
    /// is out of range, or <b>length</b> is 0.
    ///
    /// [`OH_NN_MEMORY_ERROR`] fail to set model output. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNExecutor_RunSync`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNExecutor_RunSync`]")]
    pub fn OH_NNExecutor_SetOutput(
        executor: *mut OH_NNExecutor,
        outputIndex: u32,
        dataBuffer: *mut ::core::ffi::c_void,
        length: usize,
    ) -> OH_NN_ReturnCode;
    /// Performs inference.
    ///
    /// Performs end-to-end inference and computing of the model on the device associated with the executor.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] run model successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to run model. The possible reason for failure
    /// is that the <b>executor</b> is nullptr.
    ///
    /// [`OH_NN_FAILED`] fail to set model output. The possible reason for failure
    /// is that the backend device failed to run model.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNExecutor_RunSync`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNExecutor_RunSync`]")]
    pub fn OH_NNExecutor_Run(executor: *mut OH_NNExecutor) -> OH_NN_ReturnCode;
    /// Allocates shared memory to a single input on a device.
    ///
    /// Neural Network Runtime provides a method for proactively allocating shared memory on a device.
    /// By specifying the executor and input index value, this method allocates shared memory whose size is specified by
    /// <b>length</b> on the device associated with a single input and returns the operation result through the
    /// [`OH_NN_Memory`] instance.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputIndex` - Input index value, which is in the same sequence of the data input when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>inputIndices</b> is <b>{1, 5, 9}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In the memory input application, the index value for the three inputs is <b>{0, 1, 2}</b>.
    ///
    /// * `length` - Memory size to be applied for, in bytes.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`OH_NN_Memory`] instance, or NULL if it fails to create.
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNTensor_CreateWithSize`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNTensor_CreateWithSize`]")]
    pub fn OH_NNExecutor_AllocateInputMemory(
        executor: *mut OH_NNExecutor,
        inputIndex: u32,
        length: usize,
    ) -> *mut OH_NN_Memory;
    /// Allocates shared memory to a single output on a device.
    ///
    /// Neural Network Runtime provides a method for proactively allocating shared memory on a device.
    /// By specifying the executor and output index value, this method allocates shared memory whose size is specified by
    /// <b>length</b> on the device associated with a single output and returns the operation result through the
    /// [`OH_NN_Memory`] instance.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `outputIndex` - Output Index value, which is in the same sequence of the data output when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>outputIndices</b> is <b>{4, 6, 8}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In output memory application, the index value for the three outputs is <b>{0, 1, 2}</b>.
    ///
    /// * `length` - Memory size to be applied for, in bytes.
    ///
    /// # Returns
    ///
    /// * Pointer to a [`OH_NN_Memory`] instance, or NULL if it fails to create.
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNTensor_CreateWithSize`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNTensor_CreateWithSize`]")]
    pub fn OH_NNExecutor_AllocateOutputMemory(
        executor: *mut OH_NNExecutor,
        outputIndex: u32,
        length: usize,
    ) -> *mut OH_NN_Memory;
    /// Releases the input memory to which the [`OH_NN_Memory`] instance points.
    ///
    /// This method needs to be called to release the memory instance created by calling
    /// [`OH_NNExecutor_AllocateInputMemory`]. Otherwise, memory leak will occur.
    /// The mapping between <b>inputIndex</b> and <b>memory</b> must be the same as that in memory instance creation.
    ///
    ///
    /// If <b>memory</b> or <b>*memory</b> is a null pointer, this method only prints warning logs and does not execute
    /// the release logic.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputIndex` - Input index value, which is in the same sequence of the data input when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>inputIndices</b> is <b>{1, 5, 9}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In memory input release, the index value for the three inputs is <b>{0, 1, 2}</b>.
    ///
    /// * `memory` - Double pointer to the [`OH_NN_Memory`] instance. After shared memory is destroyed,
    /// this method sets <b>*memory</b> to a null pointer.
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNTensor_Destroy`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNTensor_Destroy`]")]
    pub fn OH_NNExecutor_DestroyInputMemory(
        executor: *mut OH_NNExecutor,
        inputIndex: u32,
        memory: *mut *mut OH_NN_Memory,
    );
    /// Releases the output memory to which the [`OH_NN_Memory`] instance points.
    ///
    /// This method needs to be called to release the memory instance created by calling
    /// [`OH_NNExecutor_AllocateOutputMemory`]. Otherwise, memory leak will occur.
    /// The mapping between <b>outputIndex</b> and <b>memory</b> must be the same as that in memory instance creation.
    ///
    ///
    /// If <b>memory</b> or <b>*memory</b> is a null pointer, this method only prints warning logs and does not execute
    /// the release logic.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `outputIndex` - Output Index value, which is in the same sequence of the data output when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>outputIndices</b> is <b>{4, 6, 8}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// In output memory release, the index value for the three outputs is <b>{0, 1, 2}</b>.
    ///
    /// * `memory` - Double pointer to the [`OH_NN_Memory`] instance. After shared memory is destroyed,
    /// this method sets <b>*memory</b> to a null pointer.
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNTensor_Destroy`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNTensor_Destroy`]")]
    pub fn OH_NNExecutor_DestroyOutputMemory(
        executor: *mut OH_NNExecutor,
        outputIndex: u32,
        memory: *mut *mut OH_NN_Memory,
    );
    /// Specifies the hardware shared memory pointed to by the [`OH_NN_Memory`] instance as the shared memory
    /// used by a single input.
    ///
    /// In scenarios where memory needs to be managed by yourself, this method binds the execution input to the
    /// [`OH_NN_Memory`] memory instance. During computing, the underlying device reads the input data from the shared
    /// memory pointed to by the memory instance. By using this method, concurrent execution of input setting, computing,
    /// and read can be implemented to improve inference efficiency of a data flow.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Pointer to the [`OH_NNExecutor`] instance.
    ///
    /// * `inputIndex` - Input index value, which is in the same sequence of the data input when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>inputIndices</b> is <b>{1, 5, 9}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// When the input shared memory is specified, the index value for the three inputs is
    /// <b>{0, 1, 2}</b>.
    ///
    /// * `tensor` - Pointer to [`OH_NN_Tensor`], used to set the tensor corresponding to a single input.
    ///
    /// * `memory` - Pointer to [`OH_NN_Memory`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set input with memory successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set input with memory. The possible reason for failure
    /// is that the <b>executor</b>, <b>tensor</b> or <b>memory</b> is nullptr, or <b>inputIndex</b> is out of range,
    /// or memory length is less than tensor length.
    ///
    /// [`OH_NN_MEMORY_ERROR`] fail to set input with memory. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNExecutor_RunSync`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNExecutor_RunSync`]")]
    pub fn OH_NNExecutor_SetInputWithMemory(
        executor: *mut OH_NNExecutor,
        inputIndex: u32,
        tensor: *const OH_NN_Tensor,
        memory: *const OH_NN_Memory,
    ) -> OH_NN_ReturnCode;
    /// Specifies the hardware shared memory pointed to by the [`OH_NN_Memory`] instance as the shared memory
    /// used by a single output.
    ///
    /// In scenarios where memory needs to be managed by yourself, this method binds the execution output to the
    /// [`OH_NN_Memory`] memory instance. When computing is performed, the underlying hardware directly writes the
    /// computing result to the shared memory to which the memory instance points. By using this method, concurrent execution
    /// of input setting, computing, and read can be implemented to improve inference efficiency of a data flow.
    ///
    ///
    /// # Arguments
    ///
    /// * `executor` - Executor.
    ///
    /// * `outputIndex` - Output Index value, which is in the same sequence of the data output when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// Assume that the value of <b>outputIndices</b> is <b>{4, 6, 8}</b> when
    /// [`OH_NNModel_SpecifyInputsAndOutputs`] is called.
    /// When the output shared memory is specified, the index value for the three outputs is
    /// <b>{0, 1, 2}</b>.
    ///
    /// * `memory` - Pointer to [`OH_NN_Memory`].
    ///
    /// # Returns
    ///
    /// * Execution result of the function.
    /// [`OH_NN_SUCCESS`] set output with memory successfully.
    ///
    /// [`OH_NN_INVALID_PARAMETER`] fail to set output with memory. The possible reason for failure
    /// is that the <b>executor</b>, <b>tensor</b> or <b>memory</b> is nullptr, or <b>outputIndex</b> is
    /// out of range, or memory length is less than tensor length.
    ///
    /// [`OH_NN_MEMORY_ERROR`] fail to set output with memory. The possible reason for failure
    /// is that the memory error occurred such as failure to create an object.
    ///
    ///
    /// **Deprecated** since 11
    ///
    /// **Use instead:** [`OH_NNExecutor_RunSync`]
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    #[deprecated(since = "11", note = "Use instead: [`OH_NNExecutor_RunSync`]")]
    pub fn OH_NNExecutor_SetOutputWithMemory(
        executor: *mut OH_NNExecutor,
        outputIndex: u32,
        memory: *const OH_NN_Memory,
    ) -> OH_NN_ReturnCode;
}
