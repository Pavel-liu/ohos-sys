// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]

pub const JSVM_VERSION_EXPERIMENTAL: u32 = 2147483647;
pub const JSVM_VERSION: u32 = 8;
pub const JSVM_AUTO_LENGTH: u32 = 4294967295;
pub type char16_t = u16;
#[repr(C)]
pub struct JSVM_VM__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM instance.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_VM = *mut JSVM_VM__;
#[repr(C)]
pub struct JSVM_VMScope__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM scope.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_VMScope = *mut JSVM_VMScope__;
#[repr(C)]
pub struct JSVM_EnvScope__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM environment scope.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_EnvScope = *mut JSVM_EnvScope__;
#[repr(C)]
pub struct JSVM_Script__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript code.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Script = *mut JSVM_Script__;
#[repr(C)]
pub struct JSVM_Env__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM instance.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Env = *mut JSVM_Env__;
#[repr(C)]
pub struct JSVM_CpuProfiler__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript profiler.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
pub type JSVM_CpuProfiler = *mut JSVM_CpuProfiler__;
#[repr(C)]
pub struct JSVM_Value__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM environment.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Value = *mut JSVM_Value__;
#[repr(C)]
pub struct JSVM_Ref__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript value references.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Ref = *mut JSVM_Ref__;
#[repr(C)]
pub struct JSVM_HandleScope__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM handle scope.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_HandleScope = *mut JSVM_HandleScope__;
#[repr(C)]
pub struct JSVM_EscapableHandleScope__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM escapable handle scope.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_EscapableHandleScope = *mut JSVM_EscapableHandleScope__;
#[repr(C)]
pub struct JSVM_CallbackInfo__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM callback additional information.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_CallbackInfo = *mut JSVM_CallbackInfo__;
#[repr(C)]
pub struct JSVM_Deferred__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript VM value deferred.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Deferred = *mut JSVM_Deferred__;
/// Callback function pointer and data for user-provided native function which are to exposed to js via JSVM-API.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_CallbackStruct {
    pub callback: ::core::option::Option<
        unsafe extern "C" fn(env: JSVM_Env, info: JSVM_CallbackInfo) -> JSVM_Value,
    >,
    pub data: *mut ::core::ffi::c_void,
}
/// Function pointer type for user-provided native function which are to exposed to js via JSVM-API.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Callback = *mut JSVM_CallbackStruct;
/// Function pointer type for add-on provided function that allow the user to be notified.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
pub type JSVM_Finalize = ::core::option::Option<
    unsafe extern "C" fn(
        env: JSVM_Env,
        finalizeData: *mut ::core::ffi::c_void,
        finalizeHint: *mut ::core::ffi::c_void,
    ),
>;
/// Function pointer type for callback of ASCII output stream. The first parameter data is the data pointer.
/// And the second parameter size is the data size to output. A null data pointer indicates the end of the stream.
/// The third parameter streamData is the pointer passed in together with the callback to the API functions that
/// generate data to the output stream. The callback returns true to indicate the stream can continue to accept
/// data. Otherwise, it will abort the stream.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
pub type JSVM_OutputStream = ::core::option::Option<
    unsafe extern "C" fn(
        data: *const ::core::ffi::c_char,
        size: ::core::ffi::c_int,
        streamData: *mut ::core::ffi::c_void,
    ) -> bool,
>;
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_PropertyAttributes {
    /// No explicit attributes are set on the property.
    pub const JSVM_DEFAULT: JSVM_PropertyAttributes = JSVM_PropertyAttributes(0);
    /// The property is writable.
    pub const JSVM_WRITABLE: JSVM_PropertyAttributes = JSVM_PropertyAttributes(1);
    /// The property is enumeable.
    pub const JSVM_ENUMERABLE: JSVM_PropertyAttributes = JSVM_PropertyAttributes(2);
    /// The property is configurable.
    pub const JSVM_CONFIGURABLE: JSVM_PropertyAttributes = JSVM_PropertyAttributes(4);
    /// Used to mark the receiver of a native method need not be checked.
    /// If JSVM_NO_RECEIVER_CHECK is not set, the method only accept instance of the defined class as receiver,
    /// Otherwise Exception "Type Error: Illegal Ivocation" will be throw into JSVM.
    pub const JSVM_NO_RECEIVER_CHECK: JSVM_PropertyAttributes = JSVM_PropertyAttributes(8);
    /// Used with OH_JSVM_DefineClass to distinguish static properties from instance properties.
    pub const JSVM_STATIC: JSVM_PropertyAttributes = JSVM_PropertyAttributes(1024);
    /// Default for class methods.
    pub const JSVM_DEFAULT_METHOD: JSVM_PropertyAttributes = JSVM_PropertyAttributes(5);
    /// Class method with no receiver check
    pub const JSVM_METHOD_NO_RECEIVER_CHECK: JSVM_PropertyAttributes = JSVM_PropertyAttributes(13);
    /// Default for object properties, like in JS obj[prop].
    pub const JSVM_DEFAULT_JSPROPERTY: JSVM_PropertyAttributes = JSVM_PropertyAttributes(7);
    /// Object properties with no receiver check
    pub const JSVM_JSPROPERTY_NO_RECEIVER_CHECK: JSVM_PropertyAttributes =
        JSVM_PropertyAttributes(15);
}
#[repr(transparent)]
/// JSVM_PropertyAttributes are flag used to control the behavior of properties set on a js object.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_PropertyAttributes(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_ValueType {
    /// undefined type.
    pub const JSVM_UNDEFINED: JSVM_ValueType = JSVM_ValueType(0);
    /// null type.
    pub const JSVM_NULL: JSVM_ValueType = JSVM_ValueType(1);
    /// boolean type.
    pub const JSVM_BOOLEAN: JSVM_ValueType = JSVM_ValueType(2);
    /// number type.
    pub const JSVM_NUMBER: JSVM_ValueType = JSVM_ValueType(3);
    /// string type.
    pub const JSVM_STRING: JSVM_ValueType = JSVM_ValueType(4);
    /// symbol type.
    pub const JSVM_SYMBOL: JSVM_ValueType = JSVM_ValueType(5);
    /// object type.
    pub const JSVM_OBJECT: JSVM_ValueType = JSVM_ValueType(6);
    /// function type.
    pub const JSVM_FUNCTION: JSVM_ValueType = JSVM_ValueType(7);
    /// external type.
    pub const JSVM_EXTERNAL: JSVM_ValueType = JSVM_ValueType(8);
    /// bigint type.
    pub const JSVM_BIGINT: JSVM_ValueType = JSVM_ValueType(9);
}
#[repr(transparent)]
/// Describes the type of a JSVM_Value.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_ValueType(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_TypedarrayType {
    /// int8 type.
    pub const JSVM_INT8_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(0);
    /// uint8 type.
    pub const JSVM_UINT8_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(1);
    /// uint8 clamped type.
    pub const JSVM_UINT8_CLAMPED_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(2);
    /// int16 type.
    pub const JSVM_INT16_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(3);
    /// uint16 type.
    pub const JSVM_UINT16_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(4);
    /// int32 type.
    pub const JSVM_INT32_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(5);
    /// uint32 type.
    pub const JSVM_UINT32_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(6);
    /// float32 type.
    pub const JSVM_FLOAT32_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(7);
    /// float64 type.
    pub const JSVM_FLOAT64_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(8);
    /// bigint64 type.
    pub const JSVM_BIGINT64_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(9);
    /// biguint64 type.
    pub const JSVM_BIGUINT64_ARRAY: JSVM_TypedarrayType = JSVM_TypedarrayType(10);
}
#[repr(transparent)]
/// Describes the type of a typedarray.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_TypedarrayType(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_Status {
    /// success status.
    pub const JSVM_OK: JSVM_Status = JSVM_Status(0);
    /// invalidarg status.
    pub const JSVM_INVALID_ARG: JSVM_Status = JSVM_Status(1);
    /// object expected status.
    pub const JSVM_OBJECT_EXPECTED: JSVM_Status = JSVM_Status(2);
    /// string expected status.
    pub const JSVM_STRING_EXPECTED: JSVM_Status = JSVM_Status(3);
    /// name expected status.
    pub const JSVM_NAME_EXPECTED: JSVM_Status = JSVM_Status(4);
    /// function expected status.
    pub const JSVM_FUNCTION_EXPECTED: JSVM_Status = JSVM_Status(5);
    /// number expected status.
    pub const JSVM_NUMBER_EXPECTED: JSVM_Status = JSVM_Status(6);
    /// boolean expected status.
    pub const JSVM_BOOLEAN_EXPECTED: JSVM_Status = JSVM_Status(7);
    /// array expected status.
    pub const JSVM_ARRAY_EXPECTED: JSVM_Status = JSVM_Status(8);
    /// generic failure status.
    pub const JSVM_GENERIC_FAILURE: JSVM_Status = JSVM_Status(9);
    /// pending exception status.
    pub const JSVM_PENDING_EXCEPTION: JSVM_Status = JSVM_Status(10);
    /// cancelled status.
    pub const JSVM_CANCELLED: JSVM_Status = JSVM_Status(11);
    /// escape called twice status.
    pub const JSVM_ESCAPE_CALLED_TWICE: JSVM_Status = JSVM_Status(12);
    /// handle scope mismatch status.
    pub const JSVM_HANDLE_SCOPE_MISMATCH: JSVM_Status = JSVM_Status(13);
    /// callback scope mismatch status.
    pub const JSVM_CALLBACK_SCOPE_MISMATCH: JSVM_Status = JSVM_Status(14);
    /// queue full status.
    pub const JSVM_QUEUE_FULL: JSVM_Status = JSVM_Status(15);
    /// closing status.
    pub const JSVM_CLOSING: JSVM_Status = JSVM_Status(16);
    /// bigint expected status.
    pub const JSVM_BIGINT_EXPECTED: JSVM_Status = JSVM_Status(17);
    /// date expected status.
    pub const JSVM_DATE_EXPECTED: JSVM_Status = JSVM_Status(18);
    /// arraybuffer expected status.
    pub const JSVM_ARRAYBUFFER_EXPECTED: JSVM_Status = JSVM_Status(19);
    /// detachable arraybuffer expected status.
    pub const JSVM_DETACHABLE_ARRAYBUFFER_EXPECTED: JSVM_Status = JSVM_Status(20);
    /// would deadlock status.
    pub const JSVM_WOULD_DEADLOCK: JSVM_Status = JSVM_Status(21);
    /// no external buffers allowed status.
    pub const JSVM_NO_EXTERNAL_BUFFERS_ALLOWED: JSVM_Status = JSVM_Status(22);
    /// cannot run +js status.
    pub const JSVM_CANNOT_RUN_JS: JSVM_Status = JSVM_Status(23);
    /// invalid input type status.
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub const JSVM_INVALID_TYPE: JSVM_Status = JSVM_Status(24);
    /// jit mode expected status.
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub const JSVM_JIT_MODE_EXPECTED: JSVM_Status = JSVM_Status(25);
}
#[repr(transparent)]
/// Integral status code indicating the success or failure of a JSVM-API call.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_Status(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_KeyCollectionMode {
    /// will include all keys of the objects's prototype chain as well.
    pub const JSVM_KEY_INCLUDE_PROTOTYPES: JSVM_KeyCollectionMode = JSVM_KeyCollectionMode(0);
    /// limits the collected properties to the given object only.
    pub const JSVM_KEY_OWN_ONLY: JSVM_KeyCollectionMode = JSVM_KeyCollectionMode(1);
}
#[repr(transparent)]
/// limits the range of collected properties..
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_KeyCollectionMode(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_KeyFilter {
    /// key all properties.
    pub const JSVM_KEY_ALL_PROPERTIES: JSVM_KeyFilter = JSVM_KeyFilter(0);
    /// key writable.
    pub const JSVM_KEY_WRITABLE: JSVM_KeyFilter = JSVM_KeyFilter(1);
    /// key enumerable.
    pub const JSVM_KEY_ENUMERABLE: JSVM_KeyFilter = JSVM_KeyFilter(2);
    /// key configurable.
    pub const JSVM_KEY_CONFIGURABLE: JSVM_KeyFilter = JSVM_KeyFilter(4);
    /// key skip strings.
    pub const JSVM_KEY_SKIP_STRINGS: JSVM_KeyFilter = JSVM_KeyFilter(8);
    /// key skip symbols.
    pub const JSVM_KEY_SKIP_SYMBOLS: JSVM_KeyFilter = JSVM_KeyFilter(16);
}
#[repr(transparent)]
/// Property filter bits. They can be or'ed to build a composite filter..
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_KeyFilter(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_KeyConversion {
    /// will return numbers for integer indices.
    pub const JSVM_KEY_KEEP_NUMBERS: JSVM_KeyConversion = JSVM_KeyConversion(0);
    /// will convert integer indices to strings.
    pub const JSVM_KEY_NUMBERS_TO_STRINGS: JSVM_KeyConversion = JSVM_KeyConversion(1);
}
#[repr(transparent)]
/// key conversion select.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_KeyConversion(pub ::core::ffi::c_uint);
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
impl JSVM_MemoryPressureLevel {
    /// none pressure.
    pub const JSVM_MEMORY_PRESSURE_LEVEL_NONE: JSVM_MemoryPressureLevel =
        JSVM_MemoryPressureLevel(0);
    /// moderate pressure.
    pub const JSVM_MEMORY_PRESSURE_LEVEL_MODERATE: JSVM_MemoryPressureLevel =
        JSVM_MemoryPressureLevel(1);
    /// critical pressure.
    pub const JSVM_MEMORY_PRESSURE_LEVEL_CRITICAL: JSVM_MemoryPressureLevel =
        JSVM_MemoryPressureLevel(2);
    /// Notifies that the system is running low on memory.
    /// WARNING: It has strong negative impact on the garbage collection performance.
    /// RECOMMEND: Use the other value instead to influence the garbage collection schedule.
    ///
    ///
    /// Available since API-level: 22
    #[cfg(feature = "api-22")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-22")))]
    pub const JSVM_MEMORY_PRESSURE_LEVEL_LOW_MEMORY: JSVM_MemoryPressureLevel =
        JSVM_MemoryPressureLevel(3);
}
#[repr(transparent)]
/// Memory pressure level.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_MemoryPressureLevel(pub ::core::ffi::c_uint);
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_CompileMode {
    /// default mode.
    pub const JSVM_COMPILE_MODE_DEFAULT: JSVM_CompileMode = JSVM_CompileMode(0);
    /// consume code cache.
    pub const JSVM_COMPILE_MODE_CONSUME_CODE_CACHE: JSVM_CompileMode = JSVM_CompileMode(1);
    /// apply eager compile.
    pub const JSVM_COMPILE_MODE_EAGER_COMPILE: JSVM_CompileMode = JSVM_CompileMode(2);
    /// preset for compile profile.
    pub const JSVM_COMPILE_MODE_PRODUCE_COMPILE_PROFILE: JSVM_CompileMode = JSVM_CompileMode(3);
    /// consume compile profile.
    pub const JSVM_COMPILE_MODE_CONSUME_COMPILE_PROFILE: JSVM_CompileMode = JSVM_CompileMode(4);
}
#[repr(transparent)]
/// Compile mode
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_CompileMode(pub ::core::ffi::c_uint);
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_CompileOptionId {
    /// compile mode.
    pub const JSVM_COMPILE_MODE: JSVM_CompileOptionId = JSVM_CompileOptionId(0);
    /// code cache content.
    pub const JSVM_COMPILE_CODE_CACHE: JSVM_CompileOptionId = JSVM_CompileOptionId(1);
    /// script origin.
    pub const JSVM_COMPILE_SCRIPT_ORIGIN: JSVM_CompileOptionId = JSVM_CompileOptionId(2);
    /// compile profile content.
    pub const JSVM_COMPILE_COMPILE_PROFILE: JSVM_CompileOptionId = JSVM_CompileOptionId(3);
    /// switch for source map support.
    pub const JSVM_COMPILE_ENABLE_SOURCE_MAP: JSVM_CompileOptionId = JSVM_CompileOptionId(4);
}
#[repr(transparent)]
/// Compile option id
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_CompileOptionId(pub ::core::ffi::c_uint);
/// Heap statisics.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
pub struct JSVM_HeapStatistics {
    /// the size of the total heap.
    pub totalHeapSize: usize,
    /// the executable size of the total heap.
    pub totalHeapSizeExecutable: usize,
    /// the physical size of the total heap.
    pub totalPhysicalSize: usize,
    /// the available size of the total heap.
    pub totalAvailableSize: usize,
    /// used size of the heap.
    pub usedHeapSize: usize,
    /// heap size limit.
    pub heapSizeLimit: usize,
    /// memory requested by the heap.
    pub mallocedMemory: usize,
    /// heap-requested external memory.
    pub externalMemory: usize,
    /// peak memory requested by the heap.
    pub peakMallocedMemory: usize,
    /// the number of native contexts.
    pub numberOfNativeContexts: usize,
    /// the number of detached contexts.
    pub numberOfDetachedContexts: usize,
    /// the size of the total global handles.
    pub totalGlobalHandlesSize: usize,
    /// the size of the used global handles.
    pub usedGlobalHandlesSize: usize,
}
/// Init the JavaScript VM with init option.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_InitOptions {
    /// Optional nullptr-terminated array of raw adddresses in the embedder that the
    /// VM can match against during serialization and use for deserialization. This
    /// array and its content must stay valid for the entire lifetime of the VM
    /// instance.
    pub externalReferences: *const isize,
    /// Flags for the VM. IF removeFlags is true, recognized flags will be removed
    /// from (argc, argv). Note that these flags are specific to VM.
    /// They are mainly used for development. Do not include them in production as
    /// they might not take effect if the VM is different from the development
    /// environment.
    pub argc: *mut ::core::ffi::c_int,
    /// argv .
    pub argv: *mut *mut ::core::ffi::c_char,
    /// remove flags.
    pub removeFlags: bool,
}
/// Create the JavaScript VM with init option.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
pub struct JSVM_CreateVMOptions {
    /// optional limits of memory use of the vm.
    pub maxOldGenerationSize: usize,
    /// optional limits of memory use of the vm.
    pub maxYoungGenerationSize: usize,
    /// optional limits of memory use of the vm.
    pub initialOldGenerationSize: usize,
    /// optional limits of memory use of the vm.
    pub initialYoungGenerationSize: usize,
    /// Optional startup snapshot data.
    pub snapshotBlobData: *const ::core::ffi::c_char,
    /// Optional size of the startup snapshot data.
    pub snapshotBlobSize: usize,
    /// Whether the VM is used for creating snapshot.
    pub isForSnapshotting: bool,
}
/// JavaScript VM info.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_VMInfo {
    /// The highest API version this VM supports.
    pub apiVersion: u32,
    /// The engine name implementing the VM.
    pub engine: *const ::core::ffi::c_char,
    /// The version of the VM.
    pub version: *const ::core::ffi::c_char,
    /// The cached data version tag.
    pub cachedDataVersionTag: u32,
}
/// Property descriptor.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_PropertyDescriptor {
    /// Optional string describing the key for the property, encoded as UTF8.
    /// One of utf8name or name must be provided for the property.
    pub utf8name: *const ::core::ffi::c_char,
    /// Optional value that points to a JavaScript string or symbol to be used as the key for the property.
    pub name: JSVM_Value,
    /// Set this to make the property descriptor object's value property to be
    /// a JavaScript function represented by method.
    pub method: JSVM_Callback,
    /// A function to call when a get access of the property is performed.
    pub getter: JSVM_Callback,
    /// A function to call when a set access of the property is performed.
    pub setter: JSVM_Callback,
    /// The value that's retrieved by a get access of the property if the property is a data property.
    pub value: JSVM_Value,
    /// The attributes associated with the particular property.
    pub attributes: JSVM_PropertyAttributes,
}
/// JSVM-API uses both return values and JavaScript exceptions for error handling
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_ExtendedErrorInfo {
    /// UTF8-encoded string containing a VM-neutral description of the error.
    pub errorMessage: *const ::core::ffi::c_char,
    /// Reserved for VM-specific error details. This is currently not implemented for any VM.
    pub engineReserved: *mut ::core::ffi::c_void,
    /// VM-specific error code. This is currently not implemented for any VM.
    pub engineErrorCode: u32,
    /// The JSVM-API status code that originated with the last error.
    pub errorCode: JSVM_Status,
}
/// A 128-bit value stored as two unsigned 64-bit integers.
/// It serves as a UUID with which JavaScript objects or externals can be "tagged"
/// in order to ensure that they are of a certain type.
///
///
/// Available since API-level: 11
#[cfg(feature = "api-11")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_TypeTag {
    /// lower type.
    pub lower: u64,
    /// upper type.
    pub upper: u64,
}
/// When the object's getter, setter, deleter, and enumerator operations are performed, the corresponding
/// callback will be triggered.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_PropertyHandlerConfigurationStruct {
    /// A callback function triggered by getting a named property of an instance object.
    pub genericNamedPropertyGetterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            name: JSVM_Value,
            thisArg: JSVM_Value,
            namedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by setting a named property of an instance object.
    pub genericNamedPropertySetterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            name: JSVM_Value,
            property: JSVM_Value,
            thisArg: JSVM_Value,
            namedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by deleting a named property of an instance object.
    pub genericNamedPropertyDeleterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            name: JSVM_Value,
            thisArg: JSVM_Value,
            namedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by getting all named properties requests on an object.
    pub genericNamedPropertyEnumeratorCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            thisArg: JSVM_Value,
            namedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by getting an indexed property of an instance object.
    pub genericIndexedPropertyGetterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            index: JSVM_Value,
            thisArg: JSVM_Value,
            indexedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by setting an indexed property of an instance object.
    pub genericIndexedPropertySetterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            index: JSVM_Value,
            property: JSVM_Value,
            thisArg: JSVM_Value,
            indexedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by deleting an indexed property of an instance object.
    pub genericIndexedPropertyDeleterCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            index: JSVM_Value,
            thisArg: JSVM_Value,
            indexedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// A callback function triggered by getting all indexed properties requests on an object.
    pub genericIndexedPropertyEnumeratorCallback: ::core::option::Option<
        unsafe extern "C" fn(
            env: JSVM_Env,
            thisArg: JSVM_Value,
            indexedPropertyData: JSVM_Value,
        ) -> JSVM_Value,
    >,
    /// data will be utilized by the named property callbacks in this struct.
    pub namedPropertyData: JSVM_Value,
    /// data will be utilized by the indexed property callbacks in this struct.
    pub indexedPropertyData: JSVM_Value,
}
/// The pointer type of the structure which contains the property handlers.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
pub type JSVM_PropertyHandlerCfg = *mut JSVM_PropertyHandlerConfigurationStruct;
/// Source code information.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
pub struct JSVM_ScriptOrigin {
    /// Sourcemap url.
    pub sourceMapUrl: *const ::core::ffi::c_char,
    /// Resource name.
    pub resourceName: *const ::core::ffi::c_char,
    /// Resource line offset.
    pub resourceLineOffset: usize,
    /// Resource column offset.
    pub resourceColumnOffset: usize,
}
/// Compile Options
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct JSVM_CompileOptions {
    /// compile option id.
    pub id: JSVM_CompileOptionId,
    pub content: JSVM_CompileOptions__bindgen_ty_1,
}
/// option content.
#[repr(C)]
#[derive(Copy, Clone)]
pub union JSVM_CompileOptions__bindgen_ty_1 {
    /// ptr type.
    pub ptr: *mut ::core::ffi::c_void,
    /// int type.
    pub num: ::core::ffi::c_int,
    /// bool type.
    pub boolean: bool,
}
/// code cache passed with JSVM_COMPILE_CODE_CACHE
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
pub struct JSVM_CodeCache {
    /// cache pointer.
    pub cache: *mut u8,
    /// length.
    pub length: usize,
}
/// compile profile passed with JSVM_COMPILE_COMPILE_PROFILE
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[repr(C)]
pub struct _bindgen_ty_1 {
    /// profile pointer.
    pub profile: *mut ::core::ffi::c_int,
    /// length.
    pub length: usize,
}
/// compile profile passed with JSVM_COMPILE_COMPILE_PROFILE
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
pub type JSVM_CompileProfile = _bindgen_ty_1;
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_RegExpFlags {
    /// None mode.
    pub const JSVM_REGEXP_NONE: JSVM_RegExpFlags = JSVM_RegExpFlags(0);
    /// Global mode.
    pub const JSVM_REGEXP_GLOBAL: JSVM_RegExpFlags = JSVM_RegExpFlags(1);
    /// Ignore Case mode.
    pub const JSVM_REGEXP_IGNORE_CASE: JSVM_RegExpFlags = JSVM_RegExpFlags(2);
    /// Multiline mode.
    pub const JSVM_REGEXP_MULTILINE: JSVM_RegExpFlags = JSVM_RegExpFlags(4);
    /// Sticky mode.
    pub const JSVM_REGEXP_STICKY: JSVM_RegExpFlags = JSVM_RegExpFlags(8);
    /// Unicode mode.
    pub const JSVM_REGEXP_UNICODE: JSVM_RegExpFlags = JSVM_RegExpFlags(16);
    /// dotAll mode.
    pub const JSVM_REGEXP_DOT_ALL: JSVM_RegExpFlags = JSVM_RegExpFlags(32);
    /// Linear mode.
    pub const JSVM_REGEXP_LINEAR: JSVM_RegExpFlags = JSVM_RegExpFlags(64);
    /// Has Indices mode.
    pub const JSVM_REGEXP_HAS_INDICES: JSVM_RegExpFlags = JSVM_RegExpFlags(128);
    /// Unicode Sets mode.
    pub const JSVM_REGEXP_UNICODE_SETS: JSVM_RegExpFlags = JSVM_RegExpFlags(256);
}
#[repr(transparent)]
/// Regular expression flag bits. They can be or'ed to enable a set of flags.
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_RegExpFlags(pub ::core::ffi::c_uint);
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_InitializedFlag {
    /// initialize with zero.
    pub const JSVM_ZERO_INITIALIZED: JSVM_InitializedFlag = JSVM_InitializedFlag(0);
    /// leave uninitialized.
    pub const JSVM_UNINITIALIZED: JSVM_InitializedFlag = JSVM_InitializedFlag(1);
}
#[repr(transparent)]
/// initialization flag
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_InitializedFlag(pub ::core::ffi::c_uint);
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_WasmOptLevel {
    /// baseline optimization level.
    pub const JSVM_WASM_OPT_BASELINE: JSVM_WasmOptLevel = JSVM_WasmOptLevel(10);
    /// high optimization level.
    pub const JSVM_WASM_OPT_HIGH: JSVM_WasmOptLevel = JSVM_WasmOptLevel(20);
}
#[repr(transparent)]
/// WebAssembly function optimization level
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_WasmOptLevel(pub ::core::ffi::c_uint);
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
impl JSVM_CacheType {
    /// js code cache, generated by OH_JSVM_CreateCodeCache
    pub const JSVM_CACHE_TYPE_JS: JSVM_CacheType = JSVM_CacheType(0);
    /// WebAssembly cache, generated by OH_JSVM_CreateWasmCache
    pub const JSVM_CACHE_TYPE_WASM: JSVM_CacheType = JSVM_CacheType(1);
}
#[repr(transparent)]
/// Cache data type
///
///
/// Available since API-level: 12
#[cfg(feature = "api-12")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_CacheType(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_MicrotaskPolicy {
    /// Microtasks are invoked with the OH_JSVM_PerformMicrotaskCheckpoint() method.
    pub const JSVM_MICROTASK_EXPLICIT: JSVM_MicrotaskPolicy = JSVM_MicrotaskPolicy(0);
    /// Microtasks are invoked when the script call depth decrements to zero.
    /// Default mode.
    pub const JSVM_MICROTASK_AUTO: JSVM_MicrotaskPolicy = JSVM_MicrotaskPolicy(1);
}
#[repr(transparent)]
/// Microtask policies of JSVM.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_MicrotaskPolicy(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_TraceCategory {
    /// Tracing main interface invoking of JSVM, such as run scripts.
    pub const JSVM_TRACE_VM: JSVM_TraceCategory = JSVM_TraceCategory(0);
    /// Tracing interface invoking about compilation, such as CompileCodeBackground.
    pub const JSVM_TRACE_COMPILE: JSVM_TraceCategory = JSVM_TraceCategory(1);
    /// Tracing interface invoking about execution status, such as Interrupts and Microtasks.
    pub const JSVM_TRACE_EXECUTE: JSVM_TraceCategory = JSVM_TraceCategory(2);
    /// Tracing external callback
    pub const JSVM_TRACE_RUNTIME: JSVM_TraceCategory = JSVM_TraceCategory(3);
    /// Tracing stack trace in JSVM.
    pub const JSVM_TRACE_STACK_TRACE: JSVM_TraceCategory = JSVM_TraceCategory(4);
    /// Tracing Main interface invoking of WASM, such as Compile Wasm Module and Instantiate.
    pub const JSVM_TRACE_WASM: JSVM_TraceCategory = JSVM_TraceCategory(5);
    /// Tracing more detailed interface invoking of WASM, such as background compilation and wrappers.
    pub const JSVM_TRACE_WASM_DETAILED: JSVM_TraceCategory = JSVM_TraceCategory(6);
}
#[repr(transparent)]
/// Trace category for jsvm internal trace events.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_TraceCategory(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_PromiseRejectEvent {
    /// Promise is rejected for other reasons.
    pub const JSVM_PROMISE_REJECT_OTHER_REASONS: JSVM_PromiseRejectEvent =
        JSVM_PromiseRejectEvent(0);
    /// Promise rejected with no handler.
    pub const JSVM_PROMISE_REJECT_WITH_NO_HANDLER: JSVM_PromiseRejectEvent =
        JSVM_PromiseRejectEvent(1);
    /// Add the handler after Promise has been rejected.
    pub const JSVM_PROMISE_ADD_HANDLER_AFTER_REJECTED: JSVM_PromiseRejectEvent =
        JSVM_PromiseRejectEvent(2);
    /// After the Promise has been resolved, try to reject the Promise again.
    pub const JSVM_PROMISE_REJECT_AFTER_RESOLVED: JSVM_PromiseRejectEvent =
        JSVM_PromiseRejectEvent(3);
    /// After the Promise has been resolved, try resolving the Promise again.
    pub const JSVM_PROMISE_RESOLVE_AFTER_RESOLVED: JSVM_PromiseRejectEvent =
        JSVM_PromiseRejectEvent(4);
}
#[repr(transparent)]
/// The promise-reject event.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_PromiseRejectEvent(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_MessageErrorLevel {
    /// Log level message.
    pub const JSVM_MESSAGE_LOG: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(1);
    /// Debug level message.
    pub const JSVM_MESSAGE_DEBUG: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(2);
    /// Info level message.
    pub const JSVM_MESSAGE_INFO: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(4);
    /// Error level message.
    pub const JSVM_MESSAGE_ERROR: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(8);
    /// Warning level message.
    pub const JSVM_MESSAGE_WARNING: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(16);
    /// All level message.
    pub const JSVM_MESSAGE_ALL: JSVM_MessageErrorLevel = JSVM_MessageErrorLevel(31);
}
#[repr(transparent)]
/// The level of message error.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_MessageErrorLevel(pub ::core::ffi::c_uint);
/// Function pointer type of OOM-Error callback.
///
/// # Arguments
///
/// * `location` - The location information of the OOM error.
///
/// * `detail` - The detail of the OOM error.
///
/// * `isHeapOOM` - Determine whether the OOM type is Heap OOM.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
pub type JSVM_HandlerForOOMError = ::core::option::Option<
    unsafe extern "C" fn(
        location: *const ::core::ffi::c_char,
        detail: *const ::core::ffi::c_char,
        isHeapOOM: bool,
    ),
>;
/// Function pointer type of Fatal-Error callback.
///
/// # Arguments
///
/// * `location` - The location information of the Fatal error.
///
/// * `message` - The message of the Fatal error.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
pub type JSVM_HandlerForFatalError = ::core::option::Option<
    unsafe extern "C" fn(location: *const ::core::ffi::c_char, message: *const ::core::ffi::c_char),
>;
/// Function pointer type of Promise-Reject callback.
///
/// # Arguments
///
/// * `env` - The environment that the function is invoked under.
///
/// * `rejectEvent` - The promise-reject event.
///
/// * `rejectInfo` - An JS-object containing two properties: 'promise' and 'value'.
/// The 'promise' represents a reference to the Promise object that was rejected.
/// The 'value' represents the rejection reason associated with that promise.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
pub type JSVM_HandlerForPromiseReject = ::core::option::Option<
    unsafe extern "C" fn(
        env: JSVM_Env,
        rejectEvent: JSVM_PromiseRejectEvent,
        rejectInfo: JSVM_Value,
    ),
>;
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_DefineClassOptionsId {
    /// Defining a class in normal mode.
    pub const JSVM_DEFINE_CLASS_NORMAL: JSVM_DefineClassOptionsId = JSVM_DefineClassOptionsId(0);
    /// Defining a class with count.
    pub const JSVM_DEFINE_CLASS_WITH_COUNT: JSVM_DefineClassOptionsId =
        JSVM_DefineClassOptionsId(1);
    /// Defining a class with property handler.
    pub const JSVM_DEFINE_CLASS_WITH_PROPERTY_HANDLER: JSVM_DefineClassOptionsId =
        JSVM_DefineClassOptionsId(2);
}
#[repr(transparent)]
/// DefineClass options id.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_DefineClassOptionsId(pub ::core::ffi::c_uint);
/// DefineClass options.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct JSVM_DefineClassOptions {
    /// DefineClass option id.
    pub id: JSVM_DefineClassOptionsId,
    pub content: JSVM_DefineClassOptions__bindgen_ty_1,
}
/// option content.
#[repr(C)]
#[derive(Copy, Clone)]
pub union JSVM_DefineClassOptions__bindgen_ty_1 {
    /// for option value with pointer type.
    pub ptr: *mut ::core::ffi::c_void,
    /// for option value with integer type
    pub num: ::core::ffi::c_int,
    /// for option value with bool type
    pub boolean: bool,
}
/// The property-handler used to define class.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct JSVM_PropertyHandler {
    /// The instance object triggers the corresponding callback function.
    pub propertyHandlerCfg: JSVM_PropertyHandlerCfg,
    /// Calling an instance object as a function will trigger this callback.
    pub callAsFunctionCallback: JSVM_Callback,
}
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_CBTriggerTimeForGC {
    /// Trigger GC callback before GC.
    pub const JSVM_CB_TRIGGER_BEFORE_GC: JSVM_CBTriggerTimeForGC = JSVM_CBTriggerTimeForGC(0);
    /// Trigger GC callback after GC.
    pub const JSVM_CB_TRIGGER_AFTER_GC: JSVM_CBTriggerTimeForGC = JSVM_CBTriggerTimeForGC(1);
}
#[repr(transparent)]
/// The timing of GC callback trigger.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_CBTriggerTimeForGC(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_GCType {
    /// The GC algorithm is Scavenge.
    pub const JSVM_GC_TYPE_SCAVENGE: JSVM_GCType = JSVM_GCType(1);
    /// The GC algorithm is Minor-Mark-Compact.
    pub const JSVM_GC_TYPE_MINOR_MARK_COMPACT: JSVM_GCType = JSVM_GCType(2);
    /// The GC algorithm is Mark-Sweep-Compact.
    pub const JSVM_GC_TYPE_MARK_SWEEP_COMPACT: JSVM_GCType = JSVM_GCType(4);
    /// The GC algorithm is Incremental-Marking.
    pub const JSVM_GC_TYPE_INCREMENTAL_MARKING: JSVM_GCType = JSVM_GCType(8);
    /// The GC algorithm is Weak-Callbacks.
    pub const JSVM_GC_TYPE_PROCESS_WEAK_CALLBACKS: JSVM_GCType = JSVM_GCType(16);
    /// All GC algorithm.
    pub const JSVM_GC_TYPE_ALL: JSVM_GCType = JSVM_GCType(31);
}
#[repr(transparent)]
/// The GC type.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_GCType(pub ::core::ffi::c_uint);
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
impl JSVM_GCCallbackFlags {
    /// No GC callback falgs.
    pub const JSVM_NO_GC_CALLBACK_FLAGS: JSVM_GCCallbackFlags = JSVM_GCCallbackFlags(0);
    /// Reserved object information will be built in the garbage collection callback.
    pub const JSVM_GC_CALLBACK_CONSTRUCT_RETAINED_OBJECT_INFOS: JSVM_GCCallbackFlags =
        JSVM_GCCallbackFlags(1);
    /// Enforce Garbage Collection Callback.
    pub const JSVM_GC_CALLBACK_FORCED: JSVM_GCCallbackFlags = JSVM_GCCallbackFlags(2);
    /// Synchronous phantom callback processing.
    pub const JSVM_GC_CALLBACK_SYNCHRONOUS_PHANTOM_CALLBACK_PROCESSING: JSVM_GCCallbackFlags =
        JSVM_GCCallbackFlags(3);
    /// All available garbage objects are collected during garbage collection.
    pub const JSVM_GC_CALLBACK_COLLECT_ALL_AVAILABLE_GARBAGE: JSVM_GCCallbackFlags =
        JSVM_GCCallbackFlags(4);
    /// Garbage collection collects all external memory.
    pub const JSVM_GC_CALLBACK_COLLECT_ALL_EXTERNAL_MEMORY: JSVM_GCCallbackFlags =
        JSVM_GCCallbackFlags(5);
    /// Schedule Garbage Collection at Idle Time.
    pub const JSVM_GC_CALLBACK_SCHEDULE_IDLE_GARBAGE_COLLECTION: JSVM_GCCallbackFlags =
        JSVM_GCCallbackFlags(6);
}
#[repr(transparent)]
/// The GC callback flags.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_GCCallbackFlags(pub ::core::ffi::c_uint);
/// Function pointer type of GC callback.
///
/// # Arguments
///
/// * `vm` - The VM instance that the JSVM-API call is invoked under.
///
/// * `gcType` - The gc type.
///
/// * `flags` - The GC callback flags.
///
/// * `data` - The native pointer data.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
pub type JSVM_HandlerForGC = ::core::option::Option<
    unsafe extern "C" fn(
        vm: JSVM_VM,
        gcType: JSVM_GCType,
        flags: JSVM_GCCallbackFlags,
        data: *mut ::core::ffi::c_void,
    ),
>;
#[repr(C)]
pub struct JSVM_Data__ {
    _unused: [u8; 0],
}
/// To represent a JavaScript Data type.
///
///
/// Available since API-level: 18
#[cfg(feature = "api-18")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
pub type JSVM_Data = *mut JSVM_Data__;
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl JSVM_DebugOption {
    /// Scope check.
    pub const JSVM_SCOPE_CHECK: JSVM_DebugOption = JSVM_DebugOption(0);
}
#[repr(transparent)]
/// Debug options.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct JSVM_DebugOption(pub ::core::ffi::c_uint);
extern "C" {
    /// Init a JavaScript vm.
    ///
    /// # Arguments
    ///
    /// * `options` - The options for initialize the JavaScript VM.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_GENERIC_FAILURE`] If the execution fails, it means that the current process has completed
    /// JSVM initialization and there is no need to repeat the execution.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Init(options: *const JSVM_InitOptions) -> JSVM_Status;
    /// This API create a new VM instance.
    ///
    /// # Arguments
    ///
    /// * `options` - The options for create the VM instance.
    ///
    /// * `result` - The new VM instance.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateVM(
        options: *const JSVM_CreateVMOptions,
        result: *mut JSVM_VM,
    ) -> JSVM_Status;
    /// This function controls how Microtasks are invoked of the vm. If the method is not
    /// called, the default microtask policy of vm is JSVM_MicrotaskPolicy::JSVM_MICROTASK_AUTO.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance to set mircrotasks policy.
    ///
    /// * `policy` - Policy for running microtasks.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] If `vm` is NULL or `policy` is out of range.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_SetMicrotaskPolicy(vm: JSVM_VM, policy: JSVM_MicrotaskPolicy) -> JSVM_Status;
    /// Destroys VM instance.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance to be Destroyed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] If `vm` is NULL.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DestroyVM(vm: JSVM_VM) -> JSVM_Status;
    /// This API allocates a default JavaScript Proxy. It is the equivalent of
    /// doing new Proxy(target, handler) in JavaScript.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `target` - A JSVM_Value representing the JavaScript Object which you want to proxy.
    ///
    /// * `handler` - A JSVM_Value representing the JavaScript Object that defines which
    /// operations will be intercepted and how to redefine intercepted operations.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Proxy.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code.
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    /// [`JSVM_OBJECT_EXPECTED`] if target or handler is not Javascript Object.
    ///
    /// [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_CreateProxy(
        env: JSVM_Env,
        target: JSVM_Value,
        handler: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is a Proxy.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isProxy` - Whether the given value is Proxy.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code.
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsProxy(env: JSVM_Env, value: JSVM_Value, isProxy: *mut bool) -> JSVM_Status;
    /// This API gets target from proxy.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript Proxy whose target to return.
    ///
    /// * `result` - Target of the given proxy.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code.
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    /// [`JSVM_INVALID_TYPE`] if value is not a Javascript Proxy.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_ProxyGetTarget(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API open a new VM scope for the VM instance.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance to open scope for.
    ///
    /// * `result` - The new VM scope.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_OpenVMScope(vm: JSVM_VM, result: *mut JSVM_VMScope) -> JSVM_Status;
    /// This function close the VM scope for the VM instance.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance to close scope for.
    ///
    /// * `scope` - The VM scope to be closed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CloseVMScope(vm: JSVM_VM, scope: JSVM_VMScope) -> JSVM_Status;
    /// This function create a new environment with optional properties for the context of the new environment.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance that the env will be created in.
    ///
    /// * `propertyCount` - The number of elements in the properties array.
    ///
    /// * `properties` - The array of property descriptor.
    ///
    /// * `result` - The new environment created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateEnv(
        vm: JSVM_VM,
        propertyCount: usize,
        properties: *const JSVM_PropertyDescriptor,
        result: *mut JSVM_Env,
    ) -> JSVM_Status;
    /// This function create a new environment from the start snapshot of the vm.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance that the env will be created in.
    ///
    /// * `index` - The index of the environment in the snapshot.
    ///
    /// * `result` - The new environment created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] If the snapshot context for `index` could not be created.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateEnvFromSnapshot(
        vm: JSVM_VM,
        index: usize,
        result: *mut JSVM_Env,
    ) -> JSVM_Status;
    /// This function destroys the environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment to be destroyed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DestroyEnv(env: JSVM_Env) -> JSVM_Status;
    /// This function open a new environment scope.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `result` - The new environment scope.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_OpenEnvScope(env: JSVM_Env, result: *mut JSVM_EnvScope) -> JSVM_Status;
    /// This function closes the environment scope of the environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `scope` - The environment scope to be closed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CloseEnvScope(env: JSVM_Env, scope: JSVM_EnvScope) -> JSVM_Status;
    /// This function retrieves the VM instance of the given environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `result` - The VM instance of the environment.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] in all cases.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_GetVM(env: JSVM_Env, result: *mut JSVM_VM) -> JSVM_Status;
    /// This function compiles a string of JavaScript code and returns the compiled script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script yo be compiled.
    ///
    /// * `cachedData` - Optional code cache data for the script.
    ///
    /// * `cacheDataLength` - The length of cachedData array.
    ///
    /// * `eagerCompile` - Whether to compile the script eagerly.
    ///
    /// * `cacheRejected` - Whether the code cache rejected by compilation.
    ///
    /// * `result` - The compiled script.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    /// Returns [`JSVM_STRING_EXPECTED`] If `script` is not a string.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] If compilation failed (e.g. compiler returned empty).
    ///
    /// Returns [`JSVM_CANNOT_RUN_JS`] if an exception occurs.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CompileScript(
        env: JSVM_Env,
        script: JSVM_Value,
        cachedData: *const u8,
        cacheDataLength: usize,
        eagerCompile: bool,
        cacheRejected: *mut bool,
        result: *mut JSVM_Script,
    ) -> JSVM_Status;
    /// This function compiles a string of JavaScript code with the source code information
    /// and returns the compiled script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script to be compiled.
    ///
    /// * `cachedData` - Optional code cache data for the script.
    ///
    /// * `cacheDataLength` - The length of cachedData array.
    ///
    /// * `eagerCompile` - Whether to compile the script eagerly.
    ///
    /// * `cacheRejected` - Whether the code cache rejected by compilation.
    ///
    /// * `origin` - The information of source code.
    ///
    /// * `result` - The compiled script.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] If the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if the any of the input arguments is NULL.
    ///
    /// Returns [`JSVM_STRING_EXPECTED`] If `script` is not a string.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] If compilation failed.
    ///
    /// Returns [`JSVM_CANNOT_RUN_JS`] if an exception occurs.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CompileScriptWithOrigin(
        env: JSVM_Env,
        script: JSVM_Value,
        cachedData: *const u8,
        cacheDataLength: usize,
        eagerCompile: bool,
        cacheRejected: *mut bool,
        origin: *mut JSVM_ScriptOrigin,
        result: *mut JSVM_Script,
    ) -> JSVM_Status;
    /// This function creates code cache for the compiled script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `script` - A compiled script to create code cache for.
    ///
    /// * `data` - The data of the code cache.
    ///
    /// * `length` - The length of the code cache data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateCodeCache(
        env: JSVM_Env,
        script: JSVM_Script,
        data: *mut *const u8,
        length: *mut usize,
    ) -> JSVM_Status;
    /// This function executes a string of JavaScript code and returns its result with the following caveats:
    /// Unlike eval, this function does not allow the script to access the current lexical scope, and therefore also
    /// does not allow to access the module scope, meaning that pseudo-globals such as require will not be available.
    /// The script can access the global scope. Function and var declarations in the script will be added to the
    /// global object. Variable declarations made using let and const will be visible globally, but will not be added
    /// to the global object.The value of this is global within the script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script to execute.
    ///
    /// * `result` - The value resulting from having executed the script.
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_RunScript(
        env: JSVM_Env,
        script: JSVM_Script,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API associates data with the currently running JSVM environment. data can later be retrieved
    /// using OH_JSVM_GetInstanceData().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `data` - The data item to make available to bindings of this instance.
    ///
    /// * `finalizeCb` - The function to call when the environment is being torn down. The function receives
    /// data so that it might free it. JSVM_Finalize provides more details.
    ///
    /// * `finalizeHint` - Optional hint to pass to the finalize callback during collection.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_SetInstanceData(
        env: JSVM_Env,
        data: *mut ::core::ffi::c_void,
        finalizeCb: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This API retrieves data that was previously associated with the currently running JSVM environment
    /// via OH_JSVM_SetInstanceData(). If no data is set, the call will succeed and data will be set to NULL.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `data` - The data item that was previously associated with the currently running JSVM environment by
    /// a call to OH_JSVM_SetInstanceData().
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetInstanceData(
        env: JSVM_Env,
        data: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This API retrieves a JSVM_ExtendedErrorInfo structure with information about the last error that
    /// occurred.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `result` - The JSVM_ExtendedErrorInfo structure with more information about the error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetLastErrorInfo(
        env: JSVM_Env,
        result: *mut *const JSVM_ExtendedErrorInfo,
    ) -> JSVM_Status;
    /// This API throws the JavaScript value provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `error` - The JavaScript value to be thrown.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Throw(env: JSVM_Env, error: JSVM_Value) -> JSVM_Status;
    /// This API throws a JavaScript Error with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional error code to be set on the error.
    ///
    /// * `msg` - C string representing the text to be associated with the error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ThrowError(
        env: JSVM_Env,
        code: *const ::core::ffi::c_char,
        msg: *const ::core::ffi::c_char,
    ) -> JSVM_Status;
    /// This API throws a JavaScript TypeError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional error code to be set on the error.
    ///
    /// * `msg` - C string representing the text to be associated with the error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ThrowTypeError(
        env: JSVM_Env,
        code: *const ::core::ffi::c_char,
        msg: *const ::core::ffi::c_char,
    ) -> JSVM_Status;
    /// This API throws a JavaScript RangeError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional error code to be set on the error.
    ///
    /// * `msg` - C string representing the text to be associated with the error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ThrowRangeError(
        env: JSVM_Env,
        code: *const ::core::ffi::c_char,
        msg: *const ::core::ffi::c_char,
    ) -> JSVM_Status;
    /// This API throws a JavaScript SyntaxError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional error code to be set on the error.
    ///
    /// * `msg` - C string representing the text to be associated with the error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ThrowSyntaxError(
        env: JSVM_Env,
        code: *const ::core::ffi::c_char,
        msg: *const ::core::ffi::c_char,
    ) -> JSVM_Status;
    /// This API queries a JSVM_Value to check if it represents an error object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JSVM_Value to be checked.
    ///
    /// * `result` - Boolean value that is set to true if JSVM_Value represents an error,
    /// false otherwise.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsError(env: JSVM_Env, value: JSVM_Value, result: *mut bool) -> JSVM_Status;
    /// This API returns a JavaScript Error with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional JSVM_Value with the string for the error code to be associated with the error.
    ///
    /// * `msg` - JSVM_Value that references a JavaScript string to be used as the message for the Error.
    ///
    /// * `result` - JSVM_Value representing the error created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateError(
        env: JSVM_Env,
        code: JSVM_Value,
        msg: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JavaScript TypeError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional JSVM_Value with the string for the error code to be associated with the error.
    ///
    /// * `msg` - JSVM_Value that references a JavaScript string to be used as the message for the Error.
    ///
    /// * `result` - JSVM_Value representing the error created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateTypeError(
        env: JSVM_Env,
        code: JSVM_Value,
        msg: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JavaScript RangeError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional JSVM_Value with the string for the error code to be associated with the error.
    ///
    /// * `msg` - JSVM_Value that references a JavaScript string to be used as the message for the Error.
    ///
    /// * `result` - JSVM_Value representing the error created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateRangeError(
        env: JSVM_Env,
        code: JSVM_Value,
        msg: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JavaScript SyntaxError with the text provided.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `code` - Optional JSVM_Value with the string for the error code to be associated with the error.
    ///
    /// * `msg` - JSVM_Value that references a JavaScript string to be used as the message for the Error.
    ///
    /// * `result` - JSVM_Value representing the error created.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateSyntaxError(
        env: JSVM_Env,
        code: JSVM_Value,
        msg: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JavaScript exception if one is pending, NULL otherwise.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The exception if one is pending, NULL otherwise.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetAndClearLastException(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns true if an exception is pending, false otherwise.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - Boolean value that is set to true if an exception is pending.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsExceptionPending(env: JSVM_Env, result: *mut bool) -> JSVM_Status;
    /// This API opens a new scope.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - JSVM_Value representing the new scope.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_OpenHandleScope(env: JSVM_Env, result: *mut JSVM_HandleScope) -> JSVM_Status;
    /// This API closes the scope passed in. Scopes must be closed in the reverse
    /// order from which they were created.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `scope` - JSVM_Value representing the scope to be closed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CloseHandleScope(env: JSVM_Env, scope: JSVM_HandleScope) -> JSVM_Status;
    /// This API opens a new scope from which one object can be promoted to the outer scope.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - JSVM_Value representing the new scope.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_OpenEscapableHandleScope(
        env: JSVM_Env,
        result: *mut JSVM_EscapableHandleScope,
    ) -> JSVM_Status;
    /// This API closes the scope passed in. Scopes must be closed in the reverse order
    /// from which they were created.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `scope` - JSVM_Value representing the scope to be closed.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CloseEscapableHandleScope(
        env: JSVM_Env,
        scope: JSVM_EscapableHandleScope,
    ) -> JSVM_Status;
    /// This API promotes the handle to the JavaScript object so that it is valid for the lifetime
    /// of the outer scope. It can only be called once per scope. If it is called more than once an error
    /// will be returned.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `scope` - JSVM_Value representing the current scope.
    ///
    /// * `escapee` - JSVM_Value representing the JavaScript Object to be escaped.
    ///
    /// * `result` - JSVM_Value representing the handle to the escaped Object in the outer scope.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_EscapeHandle(
        env: JSVM_Env,
        scope: JSVM_EscapableHandleScope,
        escapee: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a new reference with the specified reference count to the value passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JSVM_Value for which a reference is being created.
    ///
    /// * `initialRefcount` - Initial reference count for the new reference.
    ///
    /// * `result` - JSVM_Ref pointing to the new reference.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateReference(
        env: JSVM_Env,
        value: JSVM_Value,
        initialRefcount: u32,
        result: *mut JSVM_Ref,
    ) -> JSVM_Status;
    /// his API deletes the reference passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `ref` - JSVM_Ref to be deleted.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DeleteReference(env: JSVM_Env, ref_: JSVM_Ref) -> JSVM_Status;
    /// his API increments the reference count for the reference passed in and
    /// returns the resulting reference count.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `ref` - JSVM_Ref for which the reference count will be incremented.
    ///
    /// * `result` - The new reference count.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ReferenceRef(env: JSVM_Env, ref_: JSVM_Ref, result: *mut u32) -> JSVM_Status;
    /// This API decrements the reference count for the reference passed in and
    /// returns the resulting reference count.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `ref` - JSVM_Ref for which the reference count will be decremented.
    ///
    /// * `result` - The new reference count.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ReferenceUnref(env: JSVM_Env, ref_: JSVM_Ref, result: *mut u32) -> JSVM_Status;
    /// If still valid, this API returns the JSVM_Value representing the
    /// JavaScript value associated with the JSVM_Ref. Otherwise, result will be NULL.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `ref` - The JSVM_Ref for which the corresponding value is being requested.
    ///
    /// * `result` - The JSVM_Value referenced by the JSVM_Ref.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetReferenceValue(
        env: JSVM_Env,
        ref_: JSVM_Ref,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JSVM-API value corresponding to a JavaScript Array type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateArray(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns a JSVM-API value corresponding to a JavaScript Array type. The Array's length property
    /// is set to the passed-in length parameter. However, the underlying buffer is not guaranteed to be pre-allocated
    /// by the VM when the array is created. That behavior is left to the underlying VM implementation.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `length` - The initial length of the Array.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateArrayWithLength(
        env: JSVM_Env,
        length: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns a JSVM-API value corresponding to a JavaScript ArrayBuffer. ArrayBuffers are used to
    /// represent fixed-length binary data buffers. They are normally used as a backing-buffer for TypedArray objects.
    /// The ArrayBuffer allocated will have an underlying byte buffer whose size is determined by the length parameter
    /// that's passed in. The underlying buffer is optionally returned back to the caller in case the caller wants to
    /// directly manipulate the buffer. This buffer can only be written to directly from native code. To write to this
    /// buffer from JavaScript, a typed array or DataView object would need to be created.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `byteLength` - The length in bytes of the array buffer to create.
    ///
    /// * `data` - Pointer to the underlying byte buffer of the ArrayBuffer.data can optionally be ignored by passing NULL.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateArraybuffer(
        env: JSVM_Env,
        byteLength: usize,
        data: *mut *mut ::core::ffi::c_void,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API allocate the memory of array buffer backing store.
    ///
    /// # Arguments
    ///
    /// * `byteLength` - size of backing store memory.
    ///
    /// * `initialized` - initialization status of the backing store memory.
    ///
    /// * `data` - pointer that recieve the backing store memory pointer.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if allocation succeed.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if data is null pointer.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] if allocation failed.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_AllocateArrayBufferBackingStoreData(
        byteLength: usize,
        initialized: JSVM_InitializedFlag,
        data: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This API release the memory of an array buffer backing store.
    ///
    /// # Arguments
    ///
    /// * `data` - pointer to the backing store memory.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if run succeed.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if data is null pointer.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_FreeArrayBufferBackingStoreData(data: *mut ::core::ffi::c_void) -> JSVM_Status;
    /// This API create an array buffer using the backing store data.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `data` - pointer to the backing store memory.
    ///
    /// * `backingStoreSize` - size of backing store memory.
    ///
    /// * `offset` - start position of the array buffer in the backing store memory.
    ///
    /// * `arrayBufferSize` - size of the array buffer.
    ///
    /// * `result` - pointer that recieve the array buffer.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if creation succeed.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if any of the following condition reached:
    ///
    /// 1. offset + arrayBufferSize > backingStoreSize
    ///
    /// 2. backingStoreSize or arrayBufferSize equals zero
    /// 3. data or result is null pointer
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateArrayBufferFromBackingStoreData(
        env: JSVM_Env,
        data: *mut ::core::ffi::c_void,
        backingStoreSize: usize,
        offset: usize,
        arrayBufferSize: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API does not observe leap seconds; they are ignored, as ECMAScript aligns with POSIX time specification.
    /// This API allocates a JavaScript Date object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `time` - ECMAScript time value in milliseconds since 01 January, 1970 UTC.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Date.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateDate(env: JSVM_Env, time: f64, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API allocates a JavaScript value with external data attached to it. This is used to pass external
    /// data through JavaScript code, so it can be retrieved later by native code using OH_JSVM_GetValueExternal.
    /// The API adds a JSVM_Finalize callback which will be called when the JavaScript object just created has been garbage
    /// collected.The created value is not an object, and therefore does not support additional properties. It is considered
    /// a distinct value type calling OH_JSVM_Typeof() with an external value yields JSVM_EXTERNAL.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `data` - Raw pointer to the external data.
    ///
    /// * `finalizeCb` - Optional callback to call when the external value is being collected. JSVM_Finalize provides
    /// more details.
    ///
    /// * `finalizeHint` - Optional hint to pass to the finalize callback during collection.
    ///
    /// * `result` - A JSVM_Value representing an external value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateExternal(
        env: JSVM_Env,
        data: *mut ::core::ffi::c_void,
        finalizeCb: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API allocates a default JavaScript Object. It is the equivalent of doing new Object() in JavaScript.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateObject(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API creates a JavaScript symbol value from a UTF8-encoded C string.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `description` - Optional JSVM_Value which refers to a JavaScript string to be set as the description
    /// for the symbol.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript symbol.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateSymbol(
        env: JSVM_Env,
        description: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API searches in the global registry for an existing symbol with the given description.
    /// If the symbol already exists it will be returned, otherwise a new symbol will be created in the registry.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `utf8description` - UTF-8 C string representing the text to be used as the description for the symbol.
    ///
    /// * `length` - The length of the description string in bytes, or JSVM_AUTO_LENGTH if it is null-terminated.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript symbol.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_SymbolFor(
        env: JSVM_Env,
        utf8description: *const ::core::ffi::c_char,
        length: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a JavaScript TypedArray object over an existing ArrayBuffer. TypedArray
    /// objects provide an array-like view over an underlying data buffer where each element has the
    /// same underlying binary scalar datatype.It's required that (length * size_of_element) + byte_offset should
    /// be <= the size in bytes of the array passed in. If not, a RangeError exception is raised.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `type` - Scalar datatype of the elements within the TypedArray.
    ///
    /// * `length` - Number of elements in the TypedArray.
    ///
    /// * `arraybuffer` - ArrayBuffer underlying the typed array.
    ///
    /// * `byteOffset` - The byte offset within the ArrayBuffer from which to start projecting the TypedArray.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript TypedArray
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateTypedarray(
        env: JSVM_Env,
        type_: JSVM_TypedarrayType,
        length: usize,
        arraybuffer: JSVM_Value,
        byteOffset: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a JavaScript DataView object over an existing ArrayBuffer. DataView
    /// objects provide an array-like view over an underlying data buffer, but one which allows items
    /// of different size and type in the ArrayBuffer.It is required that byte_length + byte_offset is
    /// less than or equal to the size in bytes of the array passed in. If not, a RangeError exception
    /// is raised.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `length` - Number of elements in the DataView.
    ///
    /// * `arraybuffer` - ArrayBuffer underlying the DataView.
    ///
    /// * `byteOffset` - The byte offset within the ArrayBuffer from which to start projecting the DataView.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript DataView.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateDataview(
        env: JSVM_Env,
        length: usize,
        arraybuffer: JSVM_Value,
        byteOffset: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API is used to convert from the C int32_t type to the JavaScript number type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Integer value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateInt32(env: JSVM_Env, value: i32, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API is used to convert from the C uint32_t type to the JavaScript number type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Unsigned integer value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateUint32(env: JSVM_Env, value: u32, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API is used to convert from the C int64_t type to the JavaScript number type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Integer value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateInt64(env: JSVM_Env, value: i64, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API is used to convert from the C double type to the JavaScript number type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Double-precision value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateDouble(env: JSVM_Env, value: f64, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API converts the C int64_t type to the JavaScript BigInt type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Integer value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript BigInt.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateBigintInt64(
        env: JSVM_Env,
        value: i64,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API converts the C uint64_t type to the JavaScript BigInt type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - Unsigned integer value to be represented in JavaScript.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript BigInt.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateBigintUint64(
        env: JSVM_Env,
        value: u64,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API converts an array of unsigned 64-bit words into a single BigInt value.
    /// The resulting BigInt is calculated as (–1)sign_bit (words[0] × (264)0 + words[1] × (264)1 + …)
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `signBit` - Determines if the resulting BigInt will be positive or negative.
    ///
    /// * `wordCount` - The length of the words array.
    ///
    /// * `words` - An array of uint64_t little-endian 64-bit words.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript BigInt.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateBigintWords(
        env: JSVM_Env,
        signBit: ::core::ffi::c_int,
        wordCount: usize,
        words: *const u64,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a JavaScript string value from an ISO-8859-1-encoded C
    /// string. The native string is copied.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `str` - Character buffer representing an ISO-8859-1-encoded string.
    ///
    /// * `length` - The length of the string in bytes, or JSVM_AUTO_LENGTH if it is null-terminated.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript string.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateStringLatin1(
        env: JSVM_Env,
        str_: *const ::core::ffi::c_char,
        length: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a JavaScript string value from a UTF16-LE-encoded C
    /// string. The native string is copied.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `str` - Character buffer representing a UTF16-LE-encoded string.
    ///
    /// * `length` - The length of the string in two-byte code units, or JSVM_AUTO_LENGTH
    /// if it is null-terminated.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript string.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateStringUtf16(
        env: JSVM_Env,
        str_: *const char16_t,
        length: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API creates a JavaScript string value from a UTF8-encoded C
    /// string. The native string is copied.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `str` - Character buffer representing a UTF8-encoded string.
    ///
    /// * `length` - The length of the string in bytes, or JSVM_AUTO_LENGTH if it is null-terminated.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript string.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateStringUtf8(
        env: JSVM_Env,
        str_: *const ::core::ffi::c_char,
        length: usize,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns the length of an array.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing the JavaScript Array whose length is being queried.
    ///
    /// * `result` - uint32 representing length of the array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetArrayLength(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut u32,
    ) -> JSVM_Status;
    /// This API is used to retrieve the underlying data buffer of an ArrayBuffer and its length.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `arraybuffer` - JSVM_Value representing the ArrayBuffer being queried.
    ///
    /// * `data` - The underlying data buffer of the ArrayBuffer. If byte_length is 0, this may be NULL
    /// or any other pointer value.
    ///
    /// * `byteLength` - Length in bytes of the underlying data buffer.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetArraybufferInfo(
        env: JSVM_Env,
        arraybuffer: JSVM_Value,
        data: *mut *mut ::core::ffi::c_void,
        byteLength: *mut usize,
    ) -> JSVM_Status;
    /// This API returns the length of an array.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - JSVM_Value representing JavaScript Object whose prototype to return. This returns
    /// the equivalent of Object.getPrototypeOf (which is not the same as the function's prototype property).
    ///
    /// * `result` - JSVM_Value representing prototype of the given object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetPrototype(
        env: JSVM_Env,
        object: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns various properties of a typed array.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `typedarray` - JSVM_Value representing the TypedArray whose properties to query.
    ///
    /// * `type` - Scalar datatype of the elements within the TypedArray.
    ///
    /// * `length` - The number of elements in the TypedArray.
    ///
    /// * `data` - The data buffer underlying the TypedArray adjusted by the byte_offset value so that it
    /// points to the first element in the TypedArray. If the length of the array is 0, this may be NULL or
    /// any other pointer value.
    ///
    /// * `arraybuffer` - The ArrayBuffer underlying the TypedArray.
    ///
    /// * `byteOffset` - The byte offset within the underlying native array at which the first element of
    /// the arrays is located. The value for the data parameter has already been adjusted so that data points
    /// to the first element in the array. Therefore, the first byte of the native array would be at data - byte_offset.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetTypedarrayInfo(
        env: JSVM_Env,
        typedarray: JSVM_Value,
        type_: *mut JSVM_TypedarrayType,
        length: *mut usize,
        data: *mut *mut ::core::ffi::c_void,
        arraybuffer: *mut JSVM_Value,
        byteOffset: *mut usize,
    ) -> JSVM_Status;
    /// Any of the out parameters may be NULL if that property is unneeded.
    /// This API returns various properties of a DataView.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `dataview` - JSVM_Value representing the DataView whose properties to query.
    ///
    /// * `bytelength` - Number of bytes in the DataView.
    ///
    /// * `data` - The data buffer underlying the DataView.
    /// If byte_length is 0, this may be NULL or any other pointer value.
    ///
    /// * `arraybuffer` - ArrayBuffer underlying the DataView.
    ///
    /// * `byteOffset` - The byte offset within the data buffer from which to start projecting the DataView.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetDataviewInfo(
        env: JSVM_Env,
        dataview: JSVM_Value,
        bytelength: *mut usize,
        data: *mut *mut ::core::ffi::c_void,
        arraybuffer: *mut JSVM_Value,
        byteOffset: *mut usize,
    ) -> JSVM_Status;
    /// Returns JSVM_OK if the function executed successfully. If a non-date JSVM_Value is
    /// passed in it returns JSVM_date_expected.This API returns the C double
    /// primitive of time value for the given JavaScript Date.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing a JavaScript Date.
    ///
    /// * `result` - Time value as a double represented as milliseconds
    /// since midnight at the beginning of 01 January, 1970 UTC.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_DATE_EXPECTED`] If a non-date JSVM_Value is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetDateValue(env: JSVM_Env, value: JSVM_Value, result: *mut f64) -> JSVM_Status;
    /// This API returns the C boolean primitive equivalent of the given JavaScript Boolean.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript Boolean.
    ///
    /// * `result` - C boolean primitive equivalent of the given JavaScript Boolean.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_BOOLEAN_EXPECTED`]If a non-boolean JSVM_Value is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueBool(env: JSVM_Env, value: JSVM_Value, result: *mut bool)
        -> JSVM_Status;
    /// This API returns the C double primitive equivalent of the given JavaScript number.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript number.
    ///
    /// * `result` - C double primitive equivalent of the given JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_NUMBER_EXPECTED`] If a non-number JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueDouble(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut f64,
    ) -> JSVM_Status;
    /// This API returns the C int64_t primitive equivalent of the given JavaScript BigInt.
    /// If needed it will truncate the value, setting lossless to false.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript BigInt.
    ///
    /// * `result` - C int64_t primitive equivalent of the given JavaScript BigInt.
    ///
    /// * `lossless` - Indicates whether the BigInt value was converted losslessly.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_BIGINT_EXPECTED`] If a non-BigInt is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueBigintInt64(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut i64,
        lossless: *mut bool,
    ) -> JSVM_Status;
    /// This API returns the C uint64_t primitive equivalent of the given JavaScript BigInt.
    /// If needed it will truncate the value, setting lossless to false.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript BigInt.
    ///
    /// * `result` - C uint64_t primitive equivalent of the given JavaScript BigInt.
    ///
    /// * `lossless` - Indicates whether the BigInt value was converted losslessly.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_BIGINT_EXPECTED`] If a non-BigInt is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueBigintUint64(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut u64,
        lossless: *mut bool,
    ) -> JSVM_Status;
    /// This API converts a single BigInt value into a sign bit, 64-bit little-endian array, and the number
    /// of elements in the array. signBit and words may be both set to NULL, in order to get only wordCount.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript BigInt.
    ///
    /// * `signBit` - Integer representing if the JavaScript BigInt is positive or negative.
    ///
    /// * `wordCount` - Must be initialized to the length of the words array. Upon return, it will be set to
    /// the actual number of words that would be needed to store this BigInt.
    ///
    /// * `words` - Pointer to a pre-allocated 64-bit word array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueBigintWords(
        env: JSVM_Env,
        value: JSVM_Value,
        signBit: *mut ::core::ffi::c_int,
        wordCount: *mut usize,
        words: *mut u64,
    ) -> JSVM_Status;
    /// This API retrieves the external data pointer that was previously passed to OH_JSVM_CreateExternal().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript external value.
    ///
    /// * `result` - Pointer to the data wrapped by the JavaScript external value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] If a non-external JSVM_Value is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueExternal(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This API returns the C int32 primitive equivalent of the given JavaScript number.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript number.
    ///
    /// * `result` - C int32 primitive equivalent of the given JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_NUMBER_EXPECTED`] If a non-number JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueInt32(env: JSVM_Env, value: JSVM_Value, result: *mut i32)
        -> JSVM_Status;
    /// This API returns the C int64 primitive equivalent of the given JavaScript number.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript number.
    ///
    /// * `result` - C int64 primitive equivalent of the given JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_NUMBER_EXPECTED`] If a non-number JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueInt64(env: JSVM_Env, value: JSVM_Value, result: *mut i64)
        -> JSVM_Status;
    /// This API returns the ISO-8859-1-encoded string corresponding the value passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript string.
    ///
    /// * `buf` - Buffer to write the ISO-8859-1-encoded string into. If NULL is passed in, the
    /// length of the string in bytes and excluding the null terminator is returned in result.
    ///
    /// * `bufsize` - Size of the destination buffer. When this value is insufficient, the returned string
    /// is truncated and null-terminated.
    ///
    /// * `result` - Number of bytes copied into the buffer, excluding the null terminator.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_STRING_EXPECTED`] If a non-string JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueStringLatin1(
        env: JSVM_Env,
        value: JSVM_Value,
        buf: *mut ::core::ffi::c_char,
        bufsize: usize,
        result: *mut usize,
    ) -> JSVM_Status;
    /// This API returns the UTF8-encoded string corresponding the value passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript string.
    ///
    /// * `buf` - Buffer to write the UTF8-encoded string into. If NULL is passed in, the length
    /// of the string in bytes and excluding the null terminator is returned in result.
    ///
    /// * `bufsize` - Size of the destination buffer. When this value is insufficient, the returned
    /// string is truncated and null-terminated.
    ///
    /// * `result` - Number of bytes copied into the buffer, excluding the null terminator.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_STRING_EXPECTED`] If a non-string JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueStringUtf8(
        env: JSVM_Env,
        value: JSVM_Value,
        buf: *mut ::core::ffi::c_char,
        bufsize: usize,
        result: *mut usize,
    ) -> JSVM_Status;
    /// This API returns the UTF16-encoded string corresponding the value passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript string.
    ///
    /// * `buf` - Buffer to write the UTF16-LE-encoded string into. If NULL is passed in,
    /// the length of the string in 2-byte code units and excluding the null terminator is returned.
    ///
    /// * `bufsize` - Size of the destination buffer. When this value is insufficient,
    /// the returned string is truncated and null-terminated.
    ///
    /// * `result` - Number of 2-byte code units copied into the buffer, excluding the null terminator.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_STRING_EXPECTED`] If a non-string JSVM_Value is passed in.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueStringUtf16(
        env: JSVM_Env,
        value: JSVM_Value,
        buf: *mut char16_t,
        bufsize: usize,
        result: *mut usize,
    ) -> JSVM_Status;
    /// This API returns the C primitive equivalent of the given JSVM_Value as a uint32_t.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - JSVM_Value representing JavaScript number.
    ///
    /// * `result` - C primitive equivalent of the given JSVM_Value as a uint32_t.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_NUMBER_EXPECTED`] If a non-number JSVM_Value is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetValueUint32(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut u32,
    ) -> JSVM_Status;
    /// This API is used to return the JavaScript singleton object that is used to represent the given boolean value.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The value of the boolean to retrieve.
    ///
    /// * `result` - JSVM_Value representing JavaScript Boolean singleton to retrieve.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetBoolean(env: JSVM_Env, value: bool, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the global object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - JSVM_Value representing JavaScript global object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetGlobal(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the null object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - JSVM_Value representing JavaScript null object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetNull(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Undefined object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - JSVM_Value representing JavaScript Undefined value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetUndefined(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API implements the abstract operation ToBoolean()
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to coerce.
    ///
    /// * `result` - JSVM_Value representing the coerced JavaScript Boolean.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CoerceToBool(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API implements the abstract operation ToNumber() as defined. This
    /// function potentially runs JS code if the passed-in value is an object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to coerce.
    ///
    /// * `result` - JSVM_Value representing the coerced JavaScript number.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CoerceToNumber(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API implements the abstract operation ToObject().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to coerce.
    ///
    /// * `result` - JSVM_Value representing the coerced JavaScript Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CoerceToObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API implements the abstract operation ToString().This
    /// function potentially runs JS code if the passed-in value is an object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to coerce.
    ///
    /// * `result` - JSVM_Value representing the coerced JavaScript string.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CoerceToString(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API represents behavior similar to invoking the typeof Operator
    /// on the object as defined. However, there are some differences:It has support
    /// for detecting an External value.It detects null as a separate type, while
    /// ECMAScript typeof would detect object.If value has a type that is invalid,
    /// an error is returned.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value whose type to query.
    ///
    /// * `result` - The type of the JavaScript value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Typeof(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_ValueType,
    ) -> JSVM_Status;
    /// This API represents invoking the instanceof Operator on the object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The JavaScript value to check.
    ///
    /// * `constructor` - The JavaScript function object of the constructor function
    /// to check against.
    ///
    /// * `result` - Boolean that is set to true if object instanceof constructor is true.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Instanceof(
        env: JSVM_Env,
        object: JSVM_Value,
        constructor: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API represents invoking the IsArray operation on the object
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given object is an array.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsArray(env: JSVM_Env, value: JSVM_Value, result: *mut bool) -> JSVM_Status;
    /// This API checks if the Object passed in is an array buffer.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given object is an ArrayBuffer.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsArraybuffer(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the Object passed in is a date.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isDate` - Whether the given JSVM_Value represents a JavaScript Date object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsDate(env: JSVM_Env, value: JSVM_Value, isDate: *mut bool) -> JSVM_Status;
    /// This API checks if the Object passed in is a typed array.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given JSVM_Value represents a TypedArray.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsTypedarray(env: JSVM_Env, value: JSVM_Value, result: *mut bool)
        -> JSVM_Status;
    /// This API checks if the Object passed in is a DataView.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given JSVM_Value represents a DataView.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsDataview(env: JSVM_Env, value: JSVM_Value, result: *mut bool) -> JSVM_Status;
    /// This API represents the invocation of the Strict Equality algorithm.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `lhs` - The JavaScript value to check.
    ///
    /// * `rhs` - The JavaScript value to check against.
    ///
    /// * `result` - Whether the two JSVM_Value objects are equal.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_StrictEquals(
        env: JSVM_Env,
        lhs: JSVM_Value,
        rhs: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API represents the invocation of the Relaxed Equality algorithm.
    /// Returns true as long as the values are equal, regardless of type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `lhs` - The JavaScript value to check.
    ///
    /// * `rhs` - The JavaScript value to check against.
    ///
    /// * `result` - Whether the two JSVM_Value objects are relaxed equal.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_Equals(
        env: JSVM_Env,
        lhs: JSVM_Value,
        rhs: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API represents the invocation of the ArrayBuffer detach operation.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `arraybuffer` - The JavaScript ArrayBuffer to be detached.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_DETACHABLE_ARRAYBUFFER_EXPECTED`] If a non-detachable ArrayBuffer is passed in it.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DetachArraybuffer(env: JSVM_Env, arraybuffer: JSVM_Value) -> JSVM_Status;
    /// This API represents the invocation of the ArrayBuffer IsDetachedBuffer operation.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript ArrayBuffer to be checked.
    ///
    /// * `result` - Whether the arraybuffer is detached.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsDetachedArraybuffer(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API returns the names of the enumerable properties of object as an array of
    /// strings. The properties of object whose key is a symbol will not be included.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the properties.
    ///
    /// * `result` - A JSVM_Value representing an array of JavaScript values that represent
    /// the property names of the object. The API can be used to iterate over result using
    /// OH_JSVM_GetArrayLength and OH_JSVM_GetElement.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetPropertyNames(
        env: JSVM_Env,
        object: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns an array containing the names of the available properties
    /// of this object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the properties.
    ///
    /// * `keyMode` - Whether to retrieve prototype properties as well.
    ///
    /// * `keyFilter` - Which properties to retrieve (enumerable/readable/writable).
    ///
    /// * `keyConversion` - Whether to convert numbered property keys to strings.
    ///
    /// * `result` - A JSVM_Value representing an array of JavaScript values
    /// that represent the property names of the object. OH_JSVM_GetArrayLength and
    /// OH_JSVM_GetElement can be used to iterate over result.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetAllPropertyNames(
        env: JSVM_Env,
        object: JSVM_Value,
        keyMode: JSVM_KeyCollectionMode,
        keyFilter: JSVM_KeyFilter,
        keyConversion: JSVM_KeyConversion,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API set a property on the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object on which to set the property.
    ///
    /// * `key` - The name of the property to set.
    ///
    /// * `value` - The property value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_SetProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Value,
        value: JSVM_Value,
    ) -> JSVM_Status;
    /// This API gets the requested property from the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the property.
    ///
    /// * `key` - The name of the property to retrieve.
    ///
    /// * `result` - The value of the property.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API checks if the Object passed in has the named property.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `key` - The name of the property whose existence to check.
    ///
    /// * `result` - Whether the property exists on the object or not.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_HasProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API attempts to delete the key own property from object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `key` - The name of the property to delete.
    ///
    /// * `result` - Whether the property deletion succeeded or not. result
    /// can optionally be ignored by passing NULL.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DeleteProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the Object passed in has the named own property.
    /// key must be a string or a symbol, or an error will be thrown. JSVM-API will
    /// not perform any conversion between data types.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `key` - The name of the own property whose existence to check.
    ///
    /// * `result` - Whether the own property exists on the object or not.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_HasOwnProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This method is equivalent to calling OH_JSVM_SetProperty with
    /// a JSVM_Value created from the string passed in as utf8name.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object on which to set the property.
    ///
    /// * `utf8name` - The name of the property to set.
    ///
    /// * `value` - The property value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_SetNamedProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        utf8name: *const ::core::ffi::c_char,
        value: JSVM_Value,
    ) -> JSVM_Status;
    /// This method is equivalent to calling OH_JSVM_SetProperty with
    /// a JSVM_Value created from the string passed in as utf8name.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the property.
    ///
    /// * `utf8name` - The name of the property to get.
    ///
    /// * `result` - The value of the property.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetNamedProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        utf8name: *const ::core::ffi::c_char,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This method is equivalent to calling OH_JSVM_SetProperty with
    /// a JSVM_Value created from the string passed in as utf8name.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `utf8name` - The name of the property whose existence to check.
    ///
    /// * `result` - Whether the property exists on the object or not.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_HasNamedProperty(
        env: JSVM_Env,
        object: JSVM_Value,
        utf8name: *const ::core::ffi::c_char,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API sets an element on the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to set the properties.
    ///
    /// * `index` - The index of the property to set.
    ///
    /// * `value` - The property value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_SetElement(
        env: JSVM_Env,
        object: JSVM_Value,
        index: u32,
        value: JSVM_Value,
    ) -> JSVM_Status;
    /// This API gets the element at the requested index.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the property.
    ///
    /// * `index` - The index of the property to get.
    ///
    /// * `result` - The value of the property.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetElement(
        env: JSVM_Env,
        object: JSVM_Value,
        index: u32,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns if the Object passed in has an element
    /// at the requested index.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `index` - The index of the property whose existence to check.
    ///
    /// * `result` - Whether the property exists on the object or not.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_HasElement(
        env: JSVM_Env,
        object: JSVM_Value,
        index: u32,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API attempts to delete the specified index from object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `index` - The index of the property to delete.
    ///
    /// * `result` - Whether the element deletion succeeded or not. result
    /// can optionally be ignored by passing NULL.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DeleteElement(
        env: JSVM_Env,
        object: JSVM_Value,
        index: u32,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This method allows the efficient definition of multiple properties
    /// on a given object. The properties are defined using property descriptors.
    /// Given an array of such property descriptors, this API will set the properties
    /// on the object one at a time, as defined by DefineOwnProperty().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the properties.
    ///
    /// * `propertyCount` - The number of elements in the properties array.
    ///
    /// * `properties` - The array of property descriptors.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DefineProperties(
        env: JSVM_Env,
        object: JSVM_Value,
        propertyCount: usize,
        properties: *const JSVM_PropertyDescriptor,
    ) -> JSVM_Status;
    /// This method freezes a given object. This prevents new properties
    /// from being added to it, existing properties from being removed, prevents
    /// changing the enumerability, configurability, or writability of existing
    /// properties, and prevents the values of existing properties from being changed.
    /// It also prevents the object's prototype from being changed.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to freeze.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ObjectFreeze(env: JSVM_Env, object: JSVM_Value) -> JSVM_Status;
    /// This method seals a given object. This prevents new properties
    /// from being added to it, as well as marking all existing properties as non-configurable.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to seal.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ObjectSeal(env: JSVM_Env, object: JSVM_Value) -> JSVM_Status;
    /// This method allows a JavaScript function object to be called from
    /// a native add-on. This is the primary mechanism of calling back from the
    /// add-on's native code into JavaScript.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `recv` - The this value passed to the called function.
    ///
    /// * `func` - JSVM_Value representing the JavaScript function to be invoked.
    ///
    /// * `argc` - The count of elements in the argv array.
    ///
    /// * `argv` - Array of JSVM_values representing JavaScript values passed in as arguments to the function.
    ///
    /// * `result` - JSVM_Value representing the JavaScript object returned.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CallFunction(
        env: JSVM_Env,
        recv: JSVM_Value,
        func: JSVM_Value,
        argc: usize,
        argv: *const JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API allows an add-on author to create a function object in native
    /// code. This is the primary mechanism to allow calling into the add-on's native
    /// code from JavaScript.The newly created function is not automatically visible
    /// from script after this call. Instead, a property must be explicitly set on any
    /// object that is visible to JavaScript, in order for the function to be accessible
    /// from script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `utf8name` - Optional name of the function encoded as UTF8. This is visible
    /// within JavaScript as the new function object's name property.
    ///
    /// * `length` - The length of the utf8name in bytes, or JSVM_AUTO_LENGTH if it
    /// is null-terminated.
    ///
    /// * `cb` - The native function which should be called when this function
    /// object is invoked and data. JSVM_Callback provides more details.
    ///
    /// * `result` - JSVM_Value representing the JavaScript function object for the newly
    /// created function.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateFunction(
        env: JSVM_Env,
        utf8name: *const ::core::ffi::c_char,
        length: usize,
        cb: JSVM_Callback,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This method is used within a callback function to retrieve details about
    /// the call like the arguments and the this pointer from a given callback info.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `cbinfo` - The callback info passed into the callback function.
    ///
    /// * `argc` - Specifies the length of the provided argv array and receives the
    /// actual count of arguments. argc can optionally be ignored by passing NULL.
    ///
    /// * `argv` - C array of JSVM_values to which the arguments will be copied. If
    /// there are more arguments than the provided count, only the requested number of
    /// arguments are copied. If there are fewer arguments provided than claimed, the
    /// rest of argv is filled with JSVM_Value values that represent undefined. argv
    /// can optionally be ignored by passing NULL.
    ///
    /// * `thisArg` - Receives the JavaScript this argument for the call. thisArg
    /// can optionally be ignored by passing NULL.
    ///
    /// * `data` - Receives the data pointer for the callback. data can optionally
    /// be ignored by passing NULL.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetCbInfo(
        env: JSVM_Env,
        cbinfo: JSVM_CallbackInfo,
        argc: *mut usize,
        argv: *mut JSVM_Value,
        thisArg: *mut JSVM_Value,
        data: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This API returns the new.target of the constructor call. If the
    /// current callback is not a constructor call, the result is NULL.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `cbinfo` - The callback info passed into the callback function.
    ///
    /// * `result` - The new.target of the constructor call.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetNewTarget(
        env: JSVM_Env,
        cbinfo: JSVM_CallbackInfo,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// his method is used to instantiate a new JavaScript value using
    /// a given JSVM_Value that represents the constructor for the object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `constructor` - JSVM_Value representing the JavaScript function to be invoked as a constructor.
    ///
    /// * `argc` - The count of elements in the argv array.
    ///
    /// * `argv` - Array of JavaScript values as JSVM_Value representing the arguments to
    /// the constructor. If argc is zero this parameter may be omitted by passing in NULL.
    ///
    /// * `result` - JSVM_Value representing the JavaScript object returned, which
    /// in this case is the constructed object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_NewInstance(
        env: JSVM_Env,
        constructor: JSVM_Value,
        argc: usize,
        argv: *const JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// When wrapping a C++ class, the C++ constructor callback passed via constructor
    /// should be a static method on the class that calls the actual class constructor, then
    /// wraps the new C++ instance in a JavaScript object, and returns the wrapper object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `utf8name` - Name of the JavaScript constructor function. For clarity, it is
    /// recommended to use the C++ class name when wrapping a C++ class.
    ///
    /// * `length` - The length of the utf8name in bytes, or JSVM_AUTO_LENGTH if it
    /// is null-terminated.
    ///
    /// * `constructor` - Struct include callback function that handles constructing instances of the class.
    /// When wrapping a C++ class, this method must be a static member with the JSVM_Callback.callback
    /// signature. A C++ class constructor cannot be used.
    /// Include Optional data to be passed to the constructor callback as the data
    /// property of the callback info. JSVM_Callback provides more details.
    ///
    /// * `propertyCount` - Number of items in the properties array argument.
    ///
    /// * `properties` - Array of property descriptors describing static and instance data
    /// properties, accessors, and methods on the class See JSVM_PropertyDescriptor.
    ///
    /// * `result` - A JSVM_Value representing the constructor function for the class.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_DefineClass(
        env: JSVM_Env,
        utf8name: *const ::core::ffi::c_char,
        length: usize,
        constructor: JSVM_Callback,
        propertyCount: usize,
        properties: *const JSVM_PropertyDescriptor,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// Wraps a native instance in a JavaScript object. The native instance can
    /// be retrieved later using OH_JSVM_Unwrap().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsObject` - The JavaScript object that will be the wrapper for the native object.
    ///
    /// * `nativeObject` - The native instance that will be wrapped in the JavaScript object.
    ///
    /// * `finalizeCb` - Optional native callback that can be used to free the native instance
    /// when the JavaScript object has been garbage-collected.
    ///
    /// * `finalizeHint` - Optional contextual hint that is passed to the finalize callback.
    /// properties, accessors, and methods on the class See JSVM_PropertyDescriptor.
    ///
    /// * `result` - Optional reference to the wrapped object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Wrap(
        env: JSVM_Env,
        jsObject: JSVM_Value,
        nativeObject: *mut ::core::ffi::c_void,
        finalizeCb: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
        result: *mut JSVM_Ref,
    ) -> JSVM_Status;
    /// When JavaScript code invokes a method or property accessor on the class, the corresponding
    /// JSVM_Callback is invoked. If the callback is for an instance method or accessor, then the this
    /// argument to the callback is the wrapper object; the wrapped C++ instance that is the target of
    /// the call can be obtained then by calling OH_JSVM_Unwrap() on the wrapper object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsObject` - The object associated with the native instance.
    ///
    /// * `result` - Pointer to the wrapped native instance.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_Unwrap(
        env: JSVM_Env,
        jsObject: JSVM_Value,
        result: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// Retrieves a native instance that was previously wrapped in the JavaScript object jsObject
    /// using OH_JSVM_Wrap() and removes the wrapping. If a finalize callback was associated with the wrapping,
    /// it will no longer be called when the JavaScript object becomes garbage-collected.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsObject` - The object associated with the native instance.
    ///
    /// * `result` - Pointer to the wrapped native instance.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_RemoveWrap(
        env: JSVM_Env,
        jsObject: JSVM_Value,
        result: *mut *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// Associates the value of the typeTag pointer with the JavaScript object or external.
    /// OH_JSVM_CheckObjectTypeTag() can then be used to compare the tag that was attached to the
    /// object with one owned by the addon to ensure that the object has the right type.
    /// If the object already has an associated type tag, this API will return JSVM_INVALID_ARG.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript object or external to be marked.
    ///
    /// * `typeTag` - The tag with which the object is to be marked.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] If the object already has an associated type tag.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_TypeTagObject(
        env: JSVM_Env,
        value: JSVM_Value,
        typeTag: *const JSVM_TypeTag,
    ) -> JSVM_Status;
    /// Compares the pointer given as typeTag with any that can be found on js object.
    /// If no tag is found on js object or, if a tag is found but it does not match typeTag,
    /// then result is set to false. If a tag is found and it matches typeTag, then result is set to true.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript object or external whose type tag to examine.
    ///
    /// * `typeTag` - The tag with which to compare any tag found on the object.
    ///
    /// * `result` - Whether the type tag given matched the type tag on the object. false is also returned
    /// if no type tag was found on the object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CheckObjectTypeTag(
        env: JSVM_Env,
        value: JSVM_Value,
        typeTag: *const JSVM_TypeTag,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API can be called multiple times on a single JavaScript object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsObject` - The JavaScript object to which the native data will be attached.
    ///
    /// * `finalizeData` - Optional data to be passed to finalizeCb.
    ///
    /// * `finalizeCb` - Native callback that will be used to free the native data when the
    /// JavaScript object has been garbage-collected. JSVM_Finalize provides more details.
    ///
    /// * `finalizeHint` - Optional contextual hint that is passed to the finalize callback.
    ///
    /// * `result` - Optional reference to the JavaScript object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_AddFinalizer(
        env: JSVM_Env,
        jsObject: JSVM_Value,
        finalizeData: *mut ::core::ffi::c_void,
        finalizeCb: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
        result: *mut JSVM_Ref,
    ) -> JSVM_Status;
    /// This API returns the highest JSVM-API version supported by the JSVM runtime.
    ///
    /// JSVM-API is planned to be additive such that newer releases of JSVM may support additional
    /// API functions. In order to allow an addon to use a newer function when running with versions
    /// of JSVM that support it, while providing fallback behavior when running with JSVM
    /// versions that don't support it.
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The highest version of JSVM-API supported.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetVersion(env: JSVM_Env, result: *mut u32) -> JSVM_Status;
    /// Return information of the VM.
    ///
    /// # Arguments
    ///
    /// * `result` - The information of the VM.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_GetVMInfo(result: *mut JSVM_VMInfo) -> JSVM_Status;
    /// This function gives V8 an indication of the amount of externally
    /// allocated memory that is kept alive by JavaScript objects (i.e. a JavaScript
    /// object that points to its own memory allocated by a native addon). Registering
    /// externally allocated memory will trigger global garbage collections more often
    /// than it would otherwise.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `changeInBytes` - The change in externally allocated memory that is kept
    /// alive by JavaScript objects.
    ///
    /// * `result` - The adjusted value
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_AdjustExternalMemory(
        env: JSVM_Env,
        changeInBytes: i64,
        result: *mut i64,
    ) -> JSVM_Status;
    /// This function notifies the VM that the system is running low on memory
    /// and optionally triggers a garbage collection.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `level` - The memory pressure level set to the current VM.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_MemoryPressureNotification(
        env: JSVM_Env,
        level: JSVM_MemoryPressureLevel,
    ) -> JSVM_Status;
    /// This API creates a deferred object and a JavaScript promise.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `deferred` - A newly created deferred object which can later be
    /// passed to OH_JSVM_ResolveDeferred() or OH_JSVM_RejectDeferred() to resolve
    /// resp. reject the associated promise.
    ///
    /// * `promise` - The JavaScript promise associated with the deferred object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreatePromise(
        env: JSVM_Env,
        deferred: *mut JSVM_Deferred,
        promise: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API resolves a JavaScript promise by way of the deferred object with
    /// which it is associated. Thus, it can only be used to resolve JavaScript promises
    /// for which the corresponding deferred object is available. This effectively means
    /// that the promise must have been created using OH_JSVM_CreatePromise() and the deferred
    /// object returned from that call must have been retained in order to be passed to this API.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `deferred` - The deferred object whose associated promise to resolve.
    ///
    /// * `resolution` - The value with which to resolve the promise.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_ResolveDeferred(
        env: JSVM_Env,
        deferred: JSVM_Deferred,
        resolution: JSVM_Value,
    ) -> JSVM_Status;
    /// This API rejects a JavaScript promise by way of the deferred object with
    /// which it is associated. Thus, it can only be used to reject JavaScript promises
    /// for which the corresponding deferred object is available. This effectively means
    /// that the promise must have been created using OH_JSVM_CreatePromise() and the deferred
    /// object returned from that call must have been retained in order to be passed to this API.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `deferred` - The deferred object whose associated promise to resolve.
    ///
    /// * `rejection` - The value with which to reject the promise.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_RejectDeferred(
        env: JSVM_Env,
        deferred: JSVM_Deferred,
        rejection: JSVM_Value,
    ) -> JSVM_Status;
    /// This API return indicating whether promise is a native promise object.
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The value to examine
    ///
    /// * `isPromise` - Flag indicating whether promise is a native promise object
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_IsPromise(env: JSVM_Env, value: JSVM_Value, isPromise: *mut bool)
        -> JSVM_Status;
    /// This API register a resolution/rejection handler with a promise.
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `promise` - The promise to be handled.
    ///
    /// * `onFulfilled` - The function to be invoked if promise is resolved.
    ///
    /// * `onRejected` - The function to be invoked if promise is rejected.
    ///
    /// * `result` - Another promise returned from promise then/catch method.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code.
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the arguments are invalid.
    ///
    /// [`JSVM_INVALID_TYPE`] if the arguments are invalid Javascript type.
    ///
    /// [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    /// [`JSVM_GENERIC_FAILURE`] if the API failed.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_PromiseRegisterHandler(
        env: JSVM_Env,
        promise: JSVM_Value,
        onFulfilled: JSVM_Value,
        onRejected: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API parses a JSON string and returns it as value if successful.
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsonString` - The string to parse.
    ///
    /// * `result` - The parse value if successful.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_JsonParse(
        env: JSVM_Env,
        jsonString: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API stringifies the object and returns it as string if successful.
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `jsonObject` - The object to stringify.
    ///
    /// * `result` - The string if successfully stringified.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_JsonStringify(
        env: JSVM_Env,
        jsonObject: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API create the startup snapshot of the VM.
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `contextCount` - The object to stringify.
    ///
    /// * `contexts` - The array of contexts to add to the snapshot.
    ///
    /// * `blobData` - The snapshot data.
    ///
    /// * `blobSize` - The size of snapshot data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 11
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_JSVM_CreateSnapshot(
        vm: JSVM_VM,
        contextCount: usize,
        contexts: *const JSVM_Env,
        blobData: *mut *const ::core::ffi::c_char,
        blobSize: *mut usize,
    ) -> JSVM_Status;
    /// This function returns a set of statistics data of the heap of the VM.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM whose heap statistics are returned.
    ///
    /// * `result` - The heap statistics data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] in all cases.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_GetHeapStatistics(vm: JSVM_VM, result: *mut JSVM_HeapStatistics) -> JSVM_Status;
    /// This function creates and starts a CPU profiler.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM to start CPU profiler for.
    ///
    /// * `result` - The pointer to the CPU profiler.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] in all cases.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_StartCpuProfiler(vm: JSVM_VM, result: *mut JSVM_CpuProfiler) -> JSVM_Status;
    /// This function stops the CPU profiler and output to the stream.
    ///
    /// # Arguments
    ///
    /// * `vm` - THe VM to start CPU profiler for.
    ///
    /// * `profiler` - The CPU profiler to stop.
    ///
    /// * `stream` - The output stream callback for receiving the data.
    ///
    /// * `streamData` - Optional data to be passed to the stream callback.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] in all cases.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_StopCpuProfiler(
        vm: JSVM_VM,
        profiler: JSVM_CpuProfiler,
        stream: JSVM_OutputStream,
        streamData: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This funciton takes the current heap snapshot and output to the stream.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM whose heap snapshot is taken.
    ///
    /// * `stream` - The output stream callback for receiving the data.
    ///
    /// * `streamData` - Optional data to be passed to the stream callback.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] in all cases.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_TakeHeapSnapshot(
        vm: JSVM_VM,
        stream: JSVM_OutputStream,
        streamData: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// This functiong activates insepctor on host and port.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `host` - The host to listen to for inspector connections.
    ///
    /// * `port` - The port to listen to for inspector connections.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_OpenInspector(
        env: JSVM_Env,
        host: *const ::core::ffi::c_char,
        port: u16,
    ) -> JSVM_Status;
    /// This function attempts to close all remaining inspector connections.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CloseInspector(env: JSVM_Env) -> JSVM_Status;
    /// This function will block until a client (existing or connected later)
    /// has sent Runtime.runIfWaitingForDebugger command.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `breakNextLine` - Whether break on the next line of JavaScript code.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_WaitForDebugger(env: JSVM_Env, breakNextLine: bool) -> JSVM_Status;
    /// Define a JavaScript class with given class name, constructor, properties, callback handlers for
    /// property operations including get, set, delete, enum etc., and call as function callback.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `utf8name` - Name of the JavaScript constructor function. For clarity, it is
    /// recommended to use the C++ class name when wrapping a C++ class.
    ///
    /// * `length` - The length of the utf8name in bytes, or JSVM_AUTO_LENGTH if it
    /// is null-terminated.
    ///
    /// * `constructor` - Struct include callback function that handles constructing instances of the class.
    /// When wrapping a C++ class, this method must be a static member with the JSVM_Callback.callback
    /// signature. A C++ class constructor cannot be used.
    /// Include Optional data to be passed to the constructor callback as the data
    /// property of the callback info. JSVM_Callback provides more details.
    ///
    /// * `propertyCount` - Number of items in the properties array argument.
    ///
    /// * `properties` - Array of property descriptors describing static and instance data
    /// properties, accessors, and methods on the class See JSVM_PropertyDescriptor.
    ///
    /// * `propertyHandlerCfg` - The instance object triggers the corresponding callback function.
    ///
    /// * `callAsFunctionCallback` - Calling an instance object as a function will trigger this callback.
    ///
    /// * `result` - A JSVM_Value representing the constructor function for the class.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_DefineClassWithPropertyHandler(
        env: JSVM_Env,
        utf8name: *const ::core::ffi::c_char,
        length: usize,
        constructor: JSVM_Callback,
        propertyCount: usize,
        properties: *const JSVM_PropertyDescriptor,
        propertyHandlerCfg: JSVM_PropertyHandlerCfg,
        callAsFunctionCallback: JSVM_Callback,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// Determines whether the current thread holds the lock for the specified environment.
    /// Only threads that hold locks can use the environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `isLocked` - Flag indicating whether the current thread holds the lock for the specified environment.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsLocked(env: JSVM_Env, isLocked: *mut bool) -> JSVM_Status;
    /// Acquire the lock for the specified environment. Only threads that hold locks can use the environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_AcquireLock(env: JSVM_Env) -> JSVM_Status;
    /// Release the lock for the specified environment. Only threads that hold locks can use the environment.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_ReleaseLock(env: JSVM_Env) -> JSVM_Status;
    /// Starts the running of the task queue inside the VM.
    /// This task queue can be executed by an external event loop.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance on which to start the task queue.
    ///
    /// * `result` - Whether the task queue was successfully started.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_PumpMessageLoop(vm: JSVM_VM, result: *mut bool) -> JSVM_Status;
    /// Check to see if there are any microtasks waiting in the queue, and if there are, execute them.
    ///
    /// # Arguments
    ///
    /// * `vm` - The VM instance on which to check microtasks.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_PerformMicrotaskCheckpoint(vm: JSVM_VM) -> JSVM_Status;
    /// This API checks if the value passed in is callable.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isCallable` - Whether the given value is callable.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] If the function executed successfully.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsCallable(
        env: JSVM_Env,
        value: JSVM_Value,
        isCallable: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is undefined.
    /// This equals to `value === undefined` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isUndefined` - Whether the given value is Undefined.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsUndefined(
        env: JSVM_Env,
        value: JSVM_Value,
        isUndefined: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is a null object.
    /// This equals to `value === null` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isNull` - Whether the given value is Null.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsNull(env: JSVM_Env, value: JSVM_Value, isNull: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is either a null or an undefined object.
    /// This is equivalent to `value == null` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isNullOrUndefined` - Whether the given value is Null or Undefined.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsNullOrUndefined(
        env: JSVM_Env,
        value: JSVM_Value,
        isNullOrUndefined: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is a boolean.
    /// This equals to `typeof value === 'boolean'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isBoolean` - Whether the given value is Boolean.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsBoolean(env: JSVM_Env, value: JSVM_Value, isBoolean: *mut bool)
        -> JSVM_Status;
    /// This API checks if the value passed in is a number.
    /// This equals to `typeof value === 'number'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isNumber` - Whether the given value is Number.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsNumber(env: JSVM_Env, value: JSVM_Value, isNumber: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is a string.
    /// This equals to `typeof value === 'string'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isString` - Whether the given value is String.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsString(env: JSVM_Env, value: JSVM_Value, isString: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is a symbol.
    /// This equals to `typeof value === 'symbol'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isSymbol` - Whether the given value is Symbol.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsSymbol(env: JSVM_Env, value: JSVM_Value, isSymbol: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is a function.
    /// This equals to `typeof value === 'function'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isFunction` - Whether the given value is Function.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsFunction(
        env: JSVM_Env,
        value: JSVM_Value,
        isFunction: *mut bool,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is an object.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isObject` - Whether the given value is Object.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsObject(env: JSVM_Env, value: JSVM_Value, isObject: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is a bigInt.
    /// This equals to `typeof value === 'bigint'` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The VM instance on which to check microtasks.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isBigInt` - Whether the given value is BigInt.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM funtions result code.
    /// [`JSVM_OK`] This API will not trigger any exception.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsBigInt(env: JSVM_Env, value: JSVM_Value, isBigInt: *mut bool) -> JSVM_Status;
    /// This API returns a JSVM-API value corresponding to a JavaScript Map type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Map.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateMap(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API checks if the value passed in is a Map.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isMap` - Whether the given value is Map.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsMap(env: JSVM_Env, value: JSVM_Value, isMap: *mut bool) -> JSVM_Status;
    /// This API returns a JSVM-API value corresponding to a JavaScript Set type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript Set.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateSet(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API checks if the value passed in is a Set.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isSet` - Whether the given value is Set.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsSet(env: JSVM_Env, value: JSVM_Value, isSet: *mut bool) -> JSVM_Status;
    /// This function compiles a string of JavaScript code with the compile options
    /// and returns the compiled script.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the JSVM-API call is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script to be compiled.
    ///
    /// * `optionCount` - length of option array.
    ///
    /// * `options` - Compile options to be passed.
    ///
    /// * `result` - The compiled script.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    /// [`JSVM_STRING_EXPECTED`] If there are parameters passed in that are not of type string.
    ///
    /// [`JSVM_GENERIC_FAILURE`] If there is an unknown reason causing execution failure.
    ///
    /// [`JSVM_PENDING_EXCEPTION`] If a JS exception occurs during the execution process.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CompileScriptWithOptions(
        env: JSVM_Env,
        script: JSVM_Value,
        optionCount: usize,
        options: *mut JSVM_CompileOptions,
        result: *mut JSVM_Script,
    ) -> JSVM_Status;
    /// This API implements the abstract operation ToBigInt().
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to coerce.
    ///
    /// * `result` - JSVM_Value representing the coerced JavaScript BigInt.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    /// [`JSVM_BIGINT_EXPECTED`] If the JavaScript value fails to coerce.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CoerceToBigInt(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API checks if the value passed in is a regExp.
    /// This equals to `value instanceof RegExp` in JS.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is RegExp.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsRegExp(env: JSVM_Env, value: JSVM_Value, result: *mut bool) -> JSVM_Status;
    /// This API checks if the value passed in is a constructor.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `isConstructor` - Whether the given value is Constructor.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsConstructor(
        env: JSVM_Env,
        value: JSVM_Value,
        isConstructor: *mut bool,
    ) -> JSVM_Status;
    /// This API returns the JavaScript value of the regular expression
    /// corresponding to the input.
    /// The interface may throw an exception.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript string to convert to a regular expression.
    ///
    /// * `flags` - Regular expression flag bits.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript RegExp.
    ///
    /// # Returns
    ///
    /// * Only returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    /// [`JSVM_STRING_EXPECTED`] If the value of 'value' is not a string.
    ///
    /// [`JSVM_GENERIC_FAILURE`] If create RegExp failed.
    ///
    /// [`JSVM_PENDING_EXCEPTION`] If the API throws an exception during runtime.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateRegExp(
        env: JSVM_Env,
        value: JSVM_Value,
        flags: JSVM_RegExpFlags,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns the Object prototype.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - JSVM_Value representing JavaScript Object whose prototype to return. This returns
    /// the equivalent of Object.getPrototypeOf (which is not the same as the function's prototype property).
    ///
    /// * `result` - JSVM_Value representing prototype of the given object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_ObjectGetPrototypeOf(
        env: JSVM_Env,
        object: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API set the prototype on the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object on which to set the prototype.
    ///
    /// * `prototype` - The prototype value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_ObjectSetPrototypeOf(
        env: JSVM_Env,
        object: JSVM_Value,
        prototype: JSVM_Value,
    ) -> JSVM_Status;
    /// Creates a function with a given script as its body.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `funcName` - A string containing the function's name. Pass NULL to create an anonymous function.
    ///
    /// * `length` - The length of the funcName in bytes, or JSVM_AUTO_LENGTH if it
    /// is null-terminated.
    ///
    /// * `argc` - The count of elements in the argv array.
    ///
    /// * `argv` - Array of JSVM_Values representing JavaScript strings passed in as arguments to the function.
    ///
    /// * `script` - A JavaScript string containing the script to use as the function's body.
    ///
    /// * `result` - JSVM_Value representing the JavaScript function object for the newly
    /// created function.
    ///
    /// # Returns
    ///
    /// * Returns JSVM function's result code.
    /// [`JSVM_OK`] If the API succeeded.
    /// [`JSVM_INVALID_ARG`] If the input parameter is invalid.
    ///
    /// [`JSVM_GENERIC_FAILURE`] If the input script fails to be compiled.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateFunctionWithScript(
        env: JSVM_Env,
        funcName: *const ::core::ffi::c_char,
        length: usize,
        argc: usize,
        argv: *const JSVM_Value,
        script: JSVM_Value,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This function keep persistently save a JSVM_Script and extend its lifecycle
    /// beyond the current scope.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script to be retained.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the script is empty or already retained.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_RetainScript(env: JSVM_Env, script: JSVM_Script) -> JSVM_Status;
    /// This function release the script retained by OH_JSVM_RetainScript
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `script` - A JavaScript string containing the script to be retained.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code
    /// [`JSVM_OK`] if the API succeeded.
    ///
    /// [`JSVM_INVALID_ARG`] if the script is empty or not retained.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_ReleaseScript(env: JSVM_Env, script: JSVM_Script) -> JSVM_Status;
    /// This function activates insepctor with pid and alias it.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `pid` - A process id to identify the inspector connection.
    ///
    /// * `name` - An alias for the inspector that under a specific pid.
    /// default name is jsvm if a nullptr is passed in.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_OpenInspectorWithName(
        env: JSVM_Env,
        pid: ::core::ffi::c_int,
        name: *const ::core::ffi::c_char,
    ) -> JSVM_Status;
    /// Compile WebAssembly bytecode into a WebAssembly module.
    /// If WebAssembly cache provided, deserialization will be performed.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `wasmBytecode` - WebAssembly bytecode.
    ///
    /// * `wasmBytecodeLength` - WebAssembly bytecode length in byte.
    ///
    /// * `cacheData` - Optional WebAssembly cache.
    ///
    /// * `cacheDataLength` - Optional WebAssembly cache length in byte.
    ///
    /// * `cacheRejected` - Output parameter representing whether the provided cacheData is rejected.
    ///
    /// * `wasmModule` - Output parameter representing compiled WebAssembly module.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if any of env, wasmBytecode is NULL, or data length is invalid.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] if compile failed.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    /// Returns [`JSVM_JIT_MODE_EXPECTED`] if run in jitless mode.
    ///
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CompileWasmModule(
        env: JSVM_Env,
        wasmBytecode: *const u8,
        wasmBytecodeLength: usize,
        cacheData: *const u8,
        cacheDataLength: usize,
        cacheRejected: *mut bool,
        wasmModule: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// Compile the function with the specified index in the WebAssembly module
    /// into the specified optimization level.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `wasmModule` - The WebAssembly module to which the function to compiled belongs.
    ///
    /// * `functionIndex` - The index of the function to be compiled, should never be out of range.
    ///
    /// * `optLevel` - Optimization level the function will be compiled with.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if env is NULL, or wasmModule is NULL or is not a WebAssembly module.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] if functionIndex out of range or compile failed.
    ///
    /// Returns [`JSVM_PENDING_EXCEPTION`] if an exception occurs.
    ///
    /// Returns [`JSVM_JIT_MODE_EXPECTED`] if run in jitless mode.
    ///
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CompileWasmFunction(
        env: JSVM_Env,
        wasmModule: JSVM_Value,
        functionIndex: u32,
        optLevel: JSVM_WasmOptLevel,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a WebAssembly module.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a WebAssembly module.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if any of the input arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_IsWasmModuleObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// Create cache for compiled WebAssembly module.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `wasmModule` - The compiled WebAssembly module.
    ///
    /// * `data` - Output parameter representing generated WebAssembly module cache.
    ///
    /// * `length` - Output parameter representing byte length of generated WebAssembly module cache.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if any of the input arguments is NULL.
    ///
    /// Returns [`JSVM_GENERIC_FAILURE`] if create wasm cache failed.
    ///
    /// Returns [`JSVM_JIT_MODE_EXPECTED`] if run in jitless mode.
    ///
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_CreateWasmCache(
        env: JSVM_Env,
        wasmModule: JSVM_Value,
        data: *mut *const u8,
        length: *mut usize,
    ) -> JSVM_Status;
    /// Release cache data with specified cache type.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `cacheData` - The cache data to be released, double free is undefined behaviors.
    ///
    /// * `cacheType` - The type of cache data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// Returns [`JSVM_OK`] if the function executed successfully.
    ///
    /// Returns [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL or cacheType is illegal.
    ///
    ///
    ///
    /// Available since API-level: 12
    #[cfg(feature = "api-12")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-12")))]
    pub fn OH_JSVM_ReleaseCache(
        env: JSVM_Env,
        cacheData: *const u8,
        cacheType: JSVM_CacheType,
    ) -> JSVM_Status;
    /// This API creates an external JavaScript string value from an ISO-8859-1-encoded C
    /// string. The native string is copied when failed to create external string.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `str` - Character buffer representing an ISO-8859-1-encoded string.
    ///
    /// * `length` - The length of the string in bytes, or JSVM_AUTO_LENGTH if it is null-terminated.
    ///
    /// * `finalizeCallback` - Optional callback to call when the external value is being collected.
    /// JSVM_Finalize provides more details.
    ///
    /// * `finalizeHint` - Optional hint to pass to the finalize callback during collection.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript external string.
    ///
    /// * `copied` - flag indicate whether the external string is successfully created,
    /// true for faild to create external ones and fall back to non-external strings, false for success.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if one of env, str and copied is NULL.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_CreateExternalStringLatin1(
        env: JSVM_Env,
        str_: *mut ::core::ffi::c_char,
        length: usize,
        finalizeCallback: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
        result: *mut JSVM_Value,
        copied: *mut bool,
    ) -> JSVM_Status;
    /// This API creates an external JavaScript string value from an UTF16-LE-encoded C
    /// string. The native string is copied when failed to create external string.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `str` - Character buffer representing an UTF16-LE-encoded string.
    ///
    /// * `length` - The length of the string in bytes, or JSVM_AUTO_LENGTH if it is null-terminated.
    ///
    /// * `finalizeCallback` - Optional callback to call when the external value is being collected.
    /// JSVM_Finalize provides more details.
    ///
    /// * `finalizeHint` - Optional hint to pass to the finalize callback during collection.
    ///
    /// * `result` - A JSVM_Value representing a JavaScript external string.
    ///
    /// * `copied` - flag indicate whether the external string is successfully created,
    /// true for faild to create external ones and fall back to non-external strings, false for success.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if one of env, str and copied is NULL.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_CreateExternalStringUtf16(
        env: JSVM_Env,
        str_: *mut char16_t,
        length: usize,
        finalizeCallback: JSVM_Finalize,
        finalizeHint: *mut ::core::ffi::c_void,
        result: *mut JSVM_Value,
        copied: *mut bool,
    ) -> JSVM_Status;
    /// This API creates a JavaScript private key.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `description` - Optional JSVM_Value which refers to a JavaScript string to be set as the description
    /// for the private key.
    ///
    /// * `result` - A JSVM_Data representing a JavaScript private key.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if env or result is NULL.
    ///
    /// [`JSVM_STRING_EXPECTED`] if the description is not a string.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_CreatePrivate(
        env: JSVM_Env,
        description: JSVM_Value,
        result: *mut JSVM_Data,
    ) -> JSVM_Status;
    /// This API set a private property on the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object on which to set the private property.
    ///
    /// * `key` - The private key of the property.
    ///
    /// * `value` - The private property value.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the arguments is NULL or the key is not a private key.
    ///
    /// [`JSVM_OBJECT_EXPECTED`] object passed in is not a real object.
    ///
    /// [`JSVM_GENERIC_FAILURE`] if failed to set the private key but no exception is pending.
    ///
    /// [`JSVM_PENDING_EXCPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_SetPrivate(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Data,
        value: JSVM_Value,
    ) -> JSVM_Status;
    /// This API gets the requested private property from the Object passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object from which to retrieve the private property.
    ///
    /// * `key` - The private key of the property.
    ///
    /// * `result` - The value of the private property.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the arguments is NULL or the key is not a private key.
    ///
    /// [`JSVM_OBJECT_EXPECTED`] object passed in is not a real object.
    ///
    /// [`JSVM_GENERIC_FAILURE`] if failed to get the private key but no exception is pending.
    ///
    /// [`JSVM_PENDING_EXCPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetPrivate(
        env: JSVM_Env,
        object: JSVM_Value,
        key: JSVM_Data,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API attempts to delete the property of the private key from object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `object` - The object to query.
    ///
    /// * `key` - The private key of the property to delete.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the arguments is NULL or the key is not a private key.
    ///
    /// [`JSVM_OBJECT_EXPECTED`] object passed in is not a real object.
    ///
    /// [`JSVM_GENERIC_FAILURE`] if failed to delete the private key but no exception is pending.
    ///
    /// [`JSVM_PENDING_EXCPTION`] if an exception occurs.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_DeletePrivate(env: JSVM_Env, object: JSVM_Value, key: JSVM_Data) -> JSVM_Status;
    /// This API creates a new reference with the specified reference count to the data passed in.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `data` - The JSVM_Data for which a reference is being created.
    ///
    /// * `initialRefcount` - Initial reference count for the new reference.
    ///
    /// * `result` - JSVM_Ref pointing to the new reference.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any parameter is null or the value of initialRefcount is 0.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_CreateDataReference(
        env: JSVM_Env,
        data: JSVM_Data,
        initialRefcount: u32,
        result: *mut JSVM_Ref,
    ) -> JSVM_Status;
    /// If still valid, this API returns the JSVM_Data representing the
    /// JavaScript data associated with the JSVM_Ref. Otherwise, result will be NULL.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `ref` - The JSVM_Ref for which the corresponding value is being requested.
    ///
    /// * `result` - The JSVM_Data referenced by the JSVM_Ref.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any parameter is null or the ref is not a reference to JSVM_Data.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetReferenceData(
        env: JSVM_Env,
        ref_: JSVM_Ref,
        result: *mut JSVM_Data,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a BigInt Object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a BigInt Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsBigIntObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a Boolean Object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a Boolean Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsBooleanObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a String Object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a String Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsStringObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a Number Object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a Number Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsNumberObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// Check whether the given JSVM_Value is a Symbol Object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `value` - The JavaScript value to check.
    ///
    /// * `result` - Whether the given value is a Symbol Object.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_IsSymbolObject(
        env: JSVM_Env,
        value: JSVM_Value,
        result: *mut bool,
    ) -> JSVM_Status;
    /// This API returns the Symbol.asyncIterator of Well-Known Symbols.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.asyncIterator of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolAsyncIterator(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.hasInstance of Well-Known Symbols.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.hasInstance of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolHasInstance(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.isConcatSpreadable of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.isConcatSpreadable of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolIsConcatSpreadable(
        env: JSVM_Env,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// This API returns the Symbol.match of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.match of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolMatch(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.replace of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.replace of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolReplace(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.search of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.search of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolSearch(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.split of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.split of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolSplit(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.toPrimitive of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.toPrimitive of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolToPrimitive(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.unscopables of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.unscopables of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolUnscopables(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.toStringTag of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.toStringTag of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolToStringTag(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// This API returns the Symbol.iterator of Well-Known Symbols
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `result` - The Symbol.iterator of Well-Known Symbols.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_GetSymbolIterator(env: JSVM_Env, result: *mut JSVM_Value) -> JSVM_Status;
    /// Trace start with specified categories for all JSVM VM.(Non-thread-safe)
    ///
    /// # Arguments
    ///
    /// * `count` - The count of trace categories.
    ///
    /// * `categories` - Select internal trace events for tracing by categories.
    ///
    /// * `tag` - User-defined tag of trace data.
    ///
    /// * `eventsCount` - Number of trace events.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if categories or count is illegal.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_TraceStart(
        count: usize,
        categories: *const JSVM_TraceCategory,
        tag: *const ::core::ffi::c_char,
        eventsCount: usize,
    ) -> JSVM_Status;
    /// Trace stop for specified categories for all JSVM VM.(Non-thread-safe)
    ///
    /// # Arguments
    ///
    /// * `stream` - The output stream callback for receiving the data.
    ///
    /// * `streamData` - Data passed to the stream callback.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if stream or streamData is NULL
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_TraceStop(
        stream: JSVM_OutputStream,
        streamData: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// Set Handler For OOM Error. If this function is invoked repeatedly,
    /// only the last time takes effect. When handler is null, the previous setting is canceled.
    ///
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `handler` - The handler for OOM Error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if vm is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_SetHandlerForOOMError(
        vm: JSVM_VM,
        handler: JSVM_HandlerForOOMError,
    ) -> JSVM_Status;
    /// This API is used to enable/disable the given debug option for a certain JSVM_Env.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `debugOption` - The debug option to be changed.
    ///
    /// * `isEnabled` - Whether to enable or disable the debug option.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if env is NULL.
    ///
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_JSVM_SetDebugOption(
        env: JSVM_Env,
        debugOption: JSVM_DebugOption,
        isEnabled: bool,
    ) -> JSVM_Status;
    /// Set Handler For Fatal Error. If this function is invoked repeatedly,
    /// only the last time takes effect. When handler is null, the previous setting is canceled.
    ///
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `handler` - The handler for Fatal Error.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if vm is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_SetHandlerForFatalError(
        vm: JSVM_VM,
        handler: JSVM_HandlerForFatalError,
    ) -> JSVM_Status;
    /// Set Handler For Promise Reject. If this function is invoked repeatedly,
    /// only the last time takes effect. When handler is null, the previous setting is canceled.
    ///
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `handler` - The handler for Promise Reject.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if vm is NULL.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_SetHandlerForPromiseReject(
        vm: JSVM_VM,
        handler: JSVM_HandlerForPromiseReject,
    ) -> JSVM_Status;
    /// When wrapping a C++ class, the C++ constructor callback passed via constructor
    /// should be a static method on the class that calls the actual class constructor, then
    /// wraps the new C++ instance in a JavaScript object according to the different Options
    /// passed in, and returns the wrapper object.
    ///
    /// # Arguments
    ///
    /// * `env` - The environment that the API is invoked under.
    ///
    /// * `utf8name` - Name of the JavaScript constructor function. For clarity, it is
    /// recommended to use the C++ class name when wrapping a C++ class.
    ///
    /// * `length` - The length of the utf8name in bytes, or JSVM_AUTO_LENGTH if it
    /// is null-terminated.
    ///
    /// * `constructor` - Struct include callback function that handles constructing instances of the class.
    /// When wrapping a C++ class, this method must be a static member with the JSVM_Callback.callback
    /// signature. A C++ class constructor cannot be used.
    /// Include Optional data to be passed to the constructor callback as the data
    /// property of the callback info. JSVM_Callback provides more details.
    ///
    /// * `propertyCount` - Number of items in the properties array argument.
    ///
    /// * `properties` - Array of property descriptors describing static and instance data
    /// properties, accessors, and methods on the class See JSVM_PropertyDescriptor.
    ///
    /// * `parentClass` - The parent-class of the currently defined class.
    ///
    /// * `option_count` - Number of items in an option array argument.
    ///
    /// * `options` - DefineClass options to be passed.
    ///
    /// * `result` - A JSVM_Value representing the constructor function for the class.
    ///
    /// # Returns
    ///
    /// * Returns JSVM functions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if any of the pointer arguments is NULL.
    ///
    /// [`JSVM_GENERIC_FAILURE`] if the input utf8name | constructor | properties is invalid.
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_DefineClassWithOptions(
        env: JSVM_Env,
        utf8name: *const ::core::ffi::c_char,
        length: usize,
        constructor: JSVM_Callback,
        propertyCount: usize,
        properties: *const JSVM_PropertyDescriptor,
        parentClass: JSVM_Value,
        option_count: usize,
        options: *mut JSVM_DefineClassOptions,
        result: *mut JSVM_Value,
    ) -> JSVM_Status;
    /// Add VM GC Callback.
    ///
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `triggerTime` - The timing of GC callback trigger.
    ///
    /// * `handler` - When Trigger gc, the callback function will be called.
    ///
    /// * `gcType` - The type of gc.
    ///
    /// * `userData` - The native pointer data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if the vm or the handler is NULL or the handler has been added before.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_AddHandlerForGC(
        vm: JSVM_VM,
        triggerTime: JSVM_CBTriggerTimeForGC,
        handler: JSVM_HandlerForGC,
        gcType: JSVM_GCType,
        userData: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
    /// Remove VM GC Callback.
    ///
    /// # Arguments
    ///
    /// * `vm` - The environment that the API is invoked under.
    ///
    /// * `triggerTime` - The timing of GC callback trigger.
    ///
    /// * `handler` - When Trigger gc, the callback function will be called.
    ///
    /// * `userData` - The native pointer data.
    ///
    /// # Returns
    ///
    /// * Returns JSVM funtions result code.
    /// [`JSVM_OK`] if the function executed successfully.
    ///
    /// [`JSVM_INVALID_ARG`] if the vm or the handler is NULL, or the handler has been removed,
    /// or the handler has never been added.
    ///
    ///
    ///
    /// Available since API-level: 18
    #[cfg(feature = "api-18")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-18")))]
    pub fn OH_JSVM_RemoveHandlerForGC(
        vm: JSVM_VM,
        triggerTime: JSVM_CBTriggerTimeForGC,
        handler: JSVM_HandlerForGC,
        userData: *mut ::core::ffi::c_void,
    ) -> JSVM_Status;
}
