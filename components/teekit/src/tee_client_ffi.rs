// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
pub enum sem_t {}

#[repr(C)]
pub struct __BindgenUnionField<T>(::core::marker::PhantomData<T>);
impl<T> __BindgenUnionField<T> {
    #[inline]
    pub const fn new() -> Self {
        __BindgenUnionField(::core::marker::PhantomData)
    }
    #[inline]
    pub unsafe fn as_ref(&self) -> &T {
        ::core::mem::transmute(self)
    }
    #[inline]
    pub unsafe fn as_mut(&mut self) -> &mut T {
        ::core::mem::transmute(self)
    }
}
impl<T> ::core::default::Default for __BindgenUnionField<T> {
    #[inline]
    fn default() -> Self {
        Self::new()
    }
}
impl<T> ::core::clone::Clone for __BindgenUnionField<T> {
    #[inline]
    fn clone(&self) -> Self {
        *self
    }
}
impl<T> ::core::marker::Copy for __BindgenUnionField<T> {}
impl<T> ::core::fmt::Debug for __BindgenUnionField<T> {
    fn fmt(&self, fmt: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        fmt.write_str("__BindgenUnionField")
    }
}
impl<T> ::core::hash::Hash for __BindgenUnionField<T> {
    fn hash<H: ::core::hash::Hasher>(&self, _state: &mut H) {}
}
impl<T> ::core::cmp::PartialEq for __BindgenUnionField<T> {
    fn eq(&self, _other: &__BindgenUnionField<T>) -> bool {
        true
    }
}
impl<T> ::core::cmp::Eq for __BindgenUnionField<T> {}
pub const TEEC_PARAM_NUM: u32 = 4;
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl TEEC_ReturnCode {
    /// The operation is successful.
    pub const TEEC_SUCCESS: TEEC_ReturnCode = TEEC_ReturnCode(0);
    /// Invalid command. The command is not supported by the TA.
    pub const TEEC_ERROR_INVALID_CMD: TEEC_ReturnCode = TEEC_ReturnCode(1);
    /// The TA does not exist.
    pub const TEEC_ERROR_SERVICE_NOT_EXIST: TEEC_ReturnCode = TEEC_ReturnCode(2);
    /// The session between the CA and TA does not exist.
    pub const TEEC_ERROR_SESSION_NOT_EXIST: TEEC_ReturnCode = TEEC_ReturnCode(3);
    /// The number of connections to the TA has reached the limit.
    pub const TEEC_ERROR_SESSION_MAXIMUM: TEEC_ReturnCode = TEEC_ReturnCode(4);
    /// The TA to be registered already exists.
    pub const TEEC_ERROR_REGISTER_EXIST_SERVICE: TEEC_ReturnCode = TEEC_ReturnCode(5);
    /// Secure OS framework error.
    pub const TEEC_ERROR_TAGET_DEAD_FATAL: TEEC_ReturnCode = TEEC_ReturnCode(6);
    /// Failed to read the file.
    pub const TEEC_ERROR_READ_DATA: TEEC_ReturnCode = TEEC_ReturnCode(7);
    /// Failed to write the file.
    pub const TEEC_ERROR_WRITE_DATA: TEEC_ReturnCode = TEEC_ReturnCode(8);
    /// Failed to truncate the file.
    pub const TEEC_ERROR_TRUNCATE_OBJECT: TEEC_ReturnCode = TEEC_ReturnCode(9);
    /// Failed to seek data.
    pub const TEEC_ERROR_SEEK_DATA: TEEC_ReturnCode = TEEC_ReturnCode(10);
    /// File synchronization error.
    pub const TEEC_ERROR_FSYNC_DATA: TEEC_ReturnCode = TEEC_ReturnCode(11);
    /// Failed to rename the file.
    pub const TEEC_ERROR_RENAME_OBJECT: TEEC_ReturnCode = TEEC_ReturnCode(12);
    /// Failed to load the TA when opening a session.
    pub const TEEC_ERROR_TRUSTED_APP_LOAD_ERROR: TEEC_ReturnCode = TEEC_ReturnCode(13);
    /// Failed to initialize the TA.
    pub const TEEC_ERROR_GENERIC: TEEC_ReturnCode = TEEC_ReturnCode(4294901760);
    /// Permission verification failed. Permission verification is performed before a TEE or session is opened or
    /// a command is sent.
    pub const TEEC_ERROR_ACCESS_DENIED: TEEC_ReturnCode = TEEC_ReturnCode(4294901761);
    /// The operation is canceled. This error code is returned when you operate the parameter with
    /// the cancallation flag.
    pub const TEEC_ERROR_CANCEL: TEEC_ReturnCode = TEEC_ReturnCode(4294901762);
    /// Concurrent access causes permission conflict. Concurrent access to files in the trusted storage
    /// service may cause this error.
    pub const TEEC_ERROR_ACCESS_CONFLICT: TEEC_ReturnCode = TEEC_ReturnCode(4294901763);
    /// Too much data is passed in the requested operation for the TA to parse.
    pub const TEEC_ERROR_EXCESS_DATA: TEEC_ReturnCode = TEEC_ReturnCode(4294901764);
    /// Incorrect data format. The TA failed to parse the parameters sent from the CA.
    pub const TEEC_ERROR_BAD_FORMAT: TEEC_ReturnCode = TEEC_ReturnCode(4294901765);
    /// Invalid parameter. The input parameter is null or invalid.
    pub const TEEC_ERROR_BAD_PARAMETERS: TEEC_ReturnCode = TEEC_ReturnCode(4294901766);
    /// The operation in the current state is invalid. This error code is returned if the trusted storage service is not
    /// initialized when a trusted storage service operation is requested.
    pub const TEEC_ERROR_BAD_STATE: TEEC_ReturnCode = TEEC_ReturnCode(4294901767);
    /// The requested data is not found.
    pub const TEEC_ERROR_ITEM_NOT_FOUND: TEEC_ReturnCode = TEEC_ReturnCode(4294901768);
    /// The requested operation has not been implemented yet. This error code is returned when
    /// <b>TEEC_RequestCancellation</b> is called.
    pub const TEEC_ERROR_NOT_IMPLEMENTED: TEEC_ReturnCode = TEEC_ReturnCode(4294901769);
    /// The requested operation is valid but is not supported in this implementation. This error code is returned
    /// when certain algorithms of the secure encryption and decryption service, such as DSA, are requested.
    pub const TEEC_ERROR_NOT_SUPPORTED: TEEC_ReturnCode = TEEC_ReturnCode(4294901770);
    /// Expected data for the requested operation is not found.
    pub const TEEC_ERROR_NO_DATA: TEEC_ReturnCode = TEEC_ReturnCode(4294901771);
    /// The available system resources are insufficient.
    pub const TEEC_ERROR_OUT_OF_MEMORY: TEEC_ReturnCode = TEEC_ReturnCode(4294901772);
    /// The system is busy. Some resources are exclusively used by the system.
    pub const TEEC_ERROR_BUSY: TEEC_ReturnCode = TEEC_ReturnCode(4294901773);
    /// Communication between an application in the REE and a TA failed.
    pub const TEEC_ERROR_COMMUNICATION: TEEC_ReturnCode = TEEC_ReturnCode(4294901774);
    /// A security fault is detected in the TEE.
    pub const TEEC_ERROR_SECURITY: TEEC_ReturnCode = TEEC_ReturnCode(4294901775);
    /// The supplied buffer is too short for the output generated.
    /// This error may occur when `TEEC_MEMREF_TEMP_OUTPUT` is used.
    pub const TEEC_ERROR_SHORT_BUFFER: TEEC_ReturnCode = TEEC_ReturnCode(4294901776);
    /// MAC value check error.
    pub const TEEC_ERROR_MAC_INVALID: TEEC_ReturnCode = TEEC_ReturnCode(4294914161);
    /// The TA crashed.
    pub const TEEC_ERROR_TARGET_DEAD: TEEC_ReturnCode = TEEC_ReturnCode(4294914084);
    /// Common error.
    pub const TEEC_FAIL: TEEC_ReturnCode = TEEC_ReturnCode(4294922242);
}
#[repr(transparent)]
/// Defines the error codes returned.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct TEEC_ReturnCode(pub ::core::ffi::c_uint);
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl TEEC_ReturnCodeOrigin {
    /// The error code indicates an error originated from the client API.
    pub const TEEC_ORIGIN_API: TEEC_ReturnCodeOrigin = TEEC_ReturnCodeOrigin(1);
    /// The error code indicates an error originated from the communication between the REE and TEE.
    pub const TEEC_ORIGIN_COMMS: TEEC_ReturnCodeOrigin = TEEC_ReturnCodeOrigin(2);
    /// The error code indicates an error originated within the TEE code.
    pub const TEEC_ORIGIN_TEE: TEEC_ReturnCodeOrigin = TEEC_ReturnCodeOrigin(3);
    /// The error code indicates an error originated within the TA code.
    pub const TEEC_ORIGIN_TRUSTED_APP: TEEC_ReturnCodeOrigin = TEEC_ReturnCodeOrigin(4);
}
#[repr(transparent)]
/// Defines the sources of the error codes returned.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct TEEC_ReturnCodeOrigin(pub ::core::ffi::c_uint);
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl TEEC_SharedMemCtl {
    /// The shared memory can carry data from CAs to TAs.
    pub const TEEC_MEM_INPUT: TEEC_SharedMemCtl = TEEC_SharedMemCtl(1);
    /// The shared memory can carry data from TAs to CAs.
    pub const TEEC_MEM_OUTPUT: TEEC_SharedMemCtl = TEEC_SharedMemCtl(2);
    /// The shared memory can carry data transmitted between CAs and TAs.
    pub const TEEC_MEM_INOUT: TEEC_SharedMemCtl = TEEC_SharedMemCtl(3);
}
#[repr(transparent)]
/// Defines the identifiers of the shared memory.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct TEEC_SharedMemCtl(pub ::core::ffi::c_uint);
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl TEEC_ParamType {
    /// The parameter is not used.
    pub const TEEC_NONE: TEEC_ParamType = TEEC_ParamType(0);
    /// The parameter is a `TEEC_Value` tagged as input. Data flows from a CA to a TA.
    pub const TEEC_VALUE_INPUT: TEEC_ParamType = TEEC_ParamType(1);
    /// The parameter is a `TEEC_Value` tagged as output. Data flows from a TA to a CA.
    pub const TEEC_VALUE_OUTPUT: TEEC_ParamType = TEEC_ParamType(2);
    /// The parameter is a `TEEC_Value` tagged as both input and output.
    pub const TEEC_VALUE_INOUT: TEEC_ParamType = TEEC_ParamType(3);
    /// The parameter is a `TEEC_TempMemoryReference` tagged as input. Data flows from a CA to a TA.
    pub const TEEC_MEMREF_TEMP_INPUT: TEEC_ParamType = TEEC_ParamType(5);
    /// The parameter is a `TEEC_TempMemoryReference` tagged as output. Data flows from a TA to a CA.
    pub const TEEC_MEMREF_TEMP_OUTPUT: TEEC_ParamType = TEEC_ParamType(6);
    /// The parameter is a `TEEC_TempMemoryReference` tagged as both input and output.
    /// Data is transmitted between a TA and a CA.
    pub const TEEC_MEMREF_TEMP_INOUT: TEEC_ParamType = TEEC_ParamType(7);
    /// The parameter is a `TEEC_IonReference` tagged as input. Data flows from a CA to a TA
    pub const TEEC_ION_INPUT: TEEC_ParamType = TEEC_ParamType(8);
    /// The parameter is a `TEEC_IonSglistReference` tagged as input. Data flows from a CA to a TA
    pub const TEEC_ION_SGLIST_INPUT: TEEC_ParamType = TEEC_ParamType(9);
    /// The parameter is a `TEEC_RegisteredMemoryReference` that refers to the entire memory block.
    /// The data flow is the same as that of `TEEC_SharedMemCtl`.
    pub const TEEC_MEMREF_WHOLE: TEEC_ParamType = TEEC_ParamType(12);
    /// The parameter is a `TEEC_RegisteredMemoryReference` tagged as input. Data flows from a CA to a TA.
    pub const TEEC_MEMREF_PARTIAL_INPUT: TEEC_ParamType = TEEC_ParamType(13);
    /// The parameter is a `TEEC_RegisteredMemoryReference` tagged as output. Data flows from a TA to a CA.
    pub const TEEC_MEMREF_PARTIAL_OUTPUT: TEEC_ParamType = TEEC_ParamType(14);
    /// The parameter is a `TEEC_RegisteredMemoryReference` tagged as both input and output.
    /// Data is transmitted between a TA and a CA.
    pub const TEEC_MEMREF_PARTIAL_INOUT: TEEC_ParamType = TEEC_ParamType(15);
}
#[repr(transparent)]
/// Defines the parameter types.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct TEEC_ParamType(pub ::core::ffi::c_uint);
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
impl TEEC_LoginMethod {
    /// No login data is provided.
    pub const TEEC_LOGIN_PUBLIC: TEEC_LoginMethod = TEEC_LoginMethod(0);
    /// The login data about the user running the CA process is provided.
    pub const TEEC_LOGIN_USER: TEEC_LoginMethod = TEEC_LoginMethod(1);
    /// The login data about the group running the CA process is provided.
    pub const TEEC_LOGIN_GROUP: TEEC_LoginMethod = TEEC_LoginMethod(2);
    /// The login data about the running CA is provided.
    pub const TEEC_LOGIN_APPLICATION: TEEC_LoginMethod = TEEC_LoginMethod(4);
    /// The login data about the user running the CA process and about the CA are provided.
    pub const TEEC_LOGIN_USER_APPLICATION: TEEC_LoginMethod = TEEC_LoginMethod(5);
    /// The login data about the group running the CA process and about the CA are provided.
    pub const TEEC_LOGIN_GROUP_APPLICATION: TEEC_LoginMethod = TEEC_LoginMethod(6);
    /// Login method reserved for TEEOS.
    pub const TEEC_LOGIN_IDENTIFY: TEEC_LoginMethod = TEEC_LoginMethod(7);
}
#[repr(transparent)]
/// Defines the login methods.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct TEEC_LoginMethod(pub ::core::ffi::c_uint);
/// Defines the linked list type.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct ListNode {
    /// Pointer to the next node in the list.
    pub next: *mut ListNode,
    /// Pointer to the previous node in the list.
    pub prev: *mut ListNode,
}
/// Defines the return values.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
pub use self::TEEC_ReturnCode as TEEC_Result;
/// Defines the universally unique identifier (UUID) as defined in RFC4122 [2].
/// The UUIDs are used to identify TAs.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_UUID {
    /// The low part of the time field.
    pub timeLow: u32,
    /// The middle part of the time field.
    pub timeMid: u16,
    /// The high part of the time field and version information.
    pub timeHiAndVersion: u16,
    /// The clock sequence and node field (8 bytes).
    pub clockSeqAndNode: [u8; 8usize],
}
/// Defines the context, a logical connection between a CA and a TEE.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
pub struct TEEC_Context {
    /// File descriptor for the TA.
    pub fd: i32,
    /// Path to the Trusted Application (TA).
    pub ta_path: *mut u8,
    /// Linked list of sessions associated with the context.
    pub session_list: ListNode,
    /// Linked list of shared memory regions associated with the context.
    pub shrd_mem_list: ListNode,
    pub __bindgen_anon_1: TEEC_Context__bindgen_ty_1,
}
/// Union for either shared buffer or implementation data.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
pub struct TEEC_Context__bindgen_ty_1 {
    pub share_buffer: __BindgenUnionField<TEEC_Context__bindgen_ty_1__bindgen_ty_1>,
    /// Implementation-specific data.
    pub imp: __BindgenUnionField<u64>,
    pub bindgen_union_field: [u64; 3usize],
}
/// Shared buffer used for data exchange and synchronization.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
pub struct TEEC_Context__bindgen_ty_1__bindgen_ty_1 {
    /// Pointer to the shared buffer.
    pub buffer: *mut ::core::ffi::c_void,
    /// Semaphore for synchronization of the shared buffer.
    pub buffer_barrier: sem_t,
}
/// Defines the session between a CA and a TA.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEEC_Session {
    /// Session ID for the session.
    pub session_id: u32,
    /// UUID representing the service associated with the session.
    pub service_id: TEEC_UUID,
    /// The number of operations associated with the session.
    pub ops_cnt: u32,
    pub __bindgen_anon_1: TEEC_Session__bindgen_ty_1,
    /// Pointer to the TEEC context associated with the session.
    pub context: *mut TEEC_Context,
}
/// Union for either a linked list head or implementation-specific data.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub union TEEC_Session__bindgen_ty_1 {
    /// Linked list head for session-related data.
    pub head: ListNode,
    /// Implementation-specific data.
    pub imp: u64,
}
/// Defines a shared memory block, which can be registered or allocated.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEEC_SharedMemory {
    /// Pointer to the shared memory buffer.
    pub buffer: *mut ::core::ffi::c_void,
    /// Size of the shared memory buffer.
    pub size: u32,
    /// Flags associated with the shared memory.
    pub flags: u32,
    /// The number of operations associated with the shared memory.
    pub ops_cnt: u32,
    /// Indicates whether the memory has been allocated.
    pub is_allocated: bool,
    pub __bindgen_anon_1: TEEC_SharedMemory__bindgen_ty_1,
    /// Pointer to the TEEC context associated with the shared memory.
    pub context: *mut TEEC_Context,
}
/// Union for either a linked list head or implementation-specific data.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub union TEEC_SharedMemory__bindgen_ty_1 {
    /// Linked list head for shared memory-related data.
    pub head: ListNode,
    /// Implementation-specific data.
    pub imp: *mut ::core::ffi::c_void,
}
/// Defines a pointer to a temporary buffer.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_TempMemoryReference {
    /// Pointer to the temporary memory buffer.
    pub buffer: *mut ::core::ffi::c_void,
    /// Size of the temporary memory buffer.
    pub size: u32,
}
/// Defines a pointer to the shared memory that is registered or allocated.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_RegisteredMemoryReference {
    /// Pointer to the parent shared memory.
    pub parent: *mut TEEC_SharedMemory,
    /// Size of the registered memory reference.
    pub size: u32,
    /// Offset within the parent shared memory.
    pub offset: u32,
}
/// Describes a parameter that carries small raw data passed by <b>value</b>.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_Value {
    /// The first value.
    pub a: u32,
    /// The second value.
    pub b: u32,
}
/// Describes the size and handle of the ION memory.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_IonReference {
    /// File descriptor for the shared ion memory.
    pub ion_share_fd: ::core::ffi::c_int,
    /// Size of the ion memory.
    pub ion_size: u32,
}
/// Defines a parameter of `TEEC_Operation`.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub union TEEC_Parameter {
    /// Temporary memory reference.
    pub tmpref: TEEC_TempMemoryReference,
    /// Registered memory reference.
    pub memref: TEEC_RegisteredMemoryReference,
    /// A value containing two 32-bit values.
    pub value: TEEC_Value,
    /// Ion memory reference.
    pub ionref: TEEC_IonReference,
}
/// Defines the parameters for the Trusted User Interface (TUI).
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct TEEC_TUI_Parameter {
    /// TUI event type.
    pub event_type: u32,
    /// Return value, is keycode if TUI event is getKeycode.
    pub value: u32,
    /// Notch size of the screen for the TUI.
    pub notch: u32,
    /// Width of the foldable screen for the TUI.
    pub width: u32,
    /// Height of the foldable screen for the TUI.
    pub height: u32,
    /// State of the foldable screen for the TUI.
    pub fold_state: u32,
    /// One state of the folded screen for the TUI.
    pub display_state: u32,
    /// Real width of the mobile device for the TUI.
    pub phy_width: u32,
    /// Real height of the mobile device for the TUI.
    pub phy_height: u32,
}
/// Defines the parameters for opening a session or sending a command.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEEC_Operation {
    /// The value 0 means to cancel the command, and other values mean to execute the command.
    pub started: u32,
    /// Use `TEEC_PARAM_TYPES` to create this parameter.
    pub paramTypes: u32,
    /// Array of parameters for the operation.
    pub params: [TEEC_Parameter; 4usize],
    /// Pointer to the session associated with the operation.
    pub session: *mut TEEC_Session,
    /// Flag indicating whether the operation is canceled.
    pub cancel_flag: bool,
}
extern "C" {
    /// Initializes a TEE.
    ///
    /// The TEE must be initialized before a session is open or commands are sent.
    /// After the initialization, a connection is set up between the CA and the TEE.
    ///
    /// # Arguments
    ///
    /// * `name` - [IN] Indicates the pointer to the TEE path.
    ///
    /// * `context` - [IN/OUT] Indicates the context pointer, which is the handle of the TEE.
    ///
    ///
    /// # Returns
    ///
    /// * Returns `TEEC_SUCCESS` if the TEE is successfully initialized.
    /// Returns `TEEC_ERROR_BAD_PARAMETERS` if <b>name</b> is incorrect or <b>context</b> is null.
    /// Returns `TEEC_ERROR_GENERIC` if the available system resources are insufficient.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_InitializeContext(
        name: *const ::core::ffi::c_char,
        context: *mut TEEC_Context,
    ) -> TEEC_Result;
    /// Closes the TEE.
    ///
    /// After the TEE is closed, the CA is disconnected from the TEE.
    ///
    /// # Arguments
    ///
    /// * `context` - [IN/OUT] Indicates the pointer to the TEE that is successfully initialized.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_FinalizeContext(context: *mut TEEC_Context);
    /// Opens a session.
    ///
    /// This function is used to set up a connection between the CA and the TA of the specified UUID in the specified TEE
    /// context. The data to be transferred is contained in <b>operation</b>.
    /// If a session is opened successfully, <b>session</b> is returned providing a description of the connection.
    /// If the session fails to open, <b>returnOrigin</b> is returned indicating the cause of the failure.
    ///
    /// # Arguments
    ///
    /// * `context` - [IN/OUT] Indicates the pointer to the TEE that is successfully initialized.
    ///
    /// * `session` - [OUT] Indicates the pointer to the session. The value cannot be null.
    ///
    /// * `destination` - [IN] Indicates the pointer to the UUID of the target TA. Each TA has a unique UUID.
    ///
    /// * `connectionMethod` - [IN] Indicates the connection method. For details, see [`TEEC_LoginMethod`].
    ///
    /// * `connectionData` - [IN] Indicates the pointer to the connection data, which varies with the connection mode.
    /// If the connection mode is `TEEC_LOGIN_PUBLIC`, `TEEC_LOGIN_USER`,
    /// `TEEC_LOGIN_USER_APPLICATION`, or `TEEC_LOGIN_GROUP_APPLICATION`, the connection data must be null.
    /// If the connection mode is `TEEC_LOGIN_GROUP` or `TEEC_LOGIN_GROUP_APPLICATION`,
    /// the connection data must point to data of the uint32_t type, which indicates the target group user to be connected
    /// by the CA.
    ///
    /// * `operation` - [IN/OUT] Indicates the pointer to the data to be transmitted between the CA and TA.
    ///
    /// * `returnOrigin` - [IN/OUT] Indicates the pointer to the error source.
    /// For details, see `TEEC_ReturnCodeOrigin`.
    ///
    ///
    /// # Returns
    ///
    /// * Returns `TEEC_SUCCESS` if the session is open successfully.
    /// Returns `TEEC_ERROR_BAD_PARAMETERS` if <b>context</b>, <b>session</b>, or <b>destination</b> is null.
    /// Returns `TEEC_ERROR_ACCESS_DENIED` if the access request is denied.
    /// Returns `TEEC_ERROR_OUT_OF_MEMORY` if the available system resources are insufficient.
    /// Returns `TEEC_ERROR_TRUSTED_APP_LOAD_ERROR` if the TA failed to be loaded.
    /// For details about other return values, see `TEEC_ReturnCode`.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_OpenSession(
        context: *mut TEEC_Context,
        session: *mut TEEC_Session,
        destination: *const TEEC_UUID,
        connectionMethod: u32,
        connectionData: *const ::core::ffi::c_void,
        operation: *mut TEEC_Operation,
        returnOrigin: *mut u32,
    ) -> TEEC_Result;
    /// Closes a session.
    ///
    /// After the session is closed, the CA is disconnected from the TA.
    ///
    /// # Arguments
    ///
    /// * `session` - [IN/OUT] Indicates the pointer to the session to close.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_CloseSession(session: *mut TEEC_Session);
    /// Sends a command to a TA.
    ///
    /// The CA sends the command ID to the TA through the specified session.
    ///
    /// # Arguments
    ///
    /// * `session` - [IN/OUT] Indicates the pointer to the session opened.
    ///
    /// * `commandID` - [IN] Indicates the command ID supported by the TA. It is defined by the TA.
    ///
    /// * `operation` - [IN/OUT] Indicates the pointer to the data to be sent from the CA to the TA.
    ///
    /// * `returnOrigin` - [IN/OUT] Indicates the pointer to the error source.
    /// For details, see `TEEC_ReturnCodeOrigin`.
    ///
    ///
    /// # Returns
    ///
    /// * Returns `TEEC_SUCCESS` if the command is sent successfully.
    /// Returns `TEEC_ERROR_BAD_PARAMETERS` if <b>session</b> is null or
    /// <b>operation</b> is in incorrect format.
    /// Returns `TEEC_ERROR_ACCESS_DENIED` if the access request is denied.
    /// Returns `TEEC_ERROR_OUT_OF_MEMORY` if the available system resources are insufficient.
    /// For details about other return values, see `TEEC_ReturnCode`.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_InvokeCommand(
        session: *mut TEEC_Session,
        commandID: u32,
        operation: *mut TEEC_Operation,
        returnOrigin: *mut u32,
    ) -> TEEC_Result;
    /// Registers shared memory in the specified TEE context.
    ///
    /// The registered shared memory can implement zero-copy.
    /// The zero-copy function, however, also requires support by the operating system.
    /// At present, zero-copy cannot be implemented in this manner.
    ///
    /// # Arguments
    ///
    /// * `context` - [IN/OUT] Indicates the pointer to the TEE that is successfully initialized.
    ///
    /// * `sharedMem` - [IN/OUT] Indicates the pointer to the shared memory.
    /// The pointed shared memory cannot be null and the size cannot be 0.
    ///
    ///
    /// # Returns
    ///
    /// * Returns `TEEC_SUCCESS` if the operation is successful.
    /// Returns `TEEC_ERROR_BAD_PARAMETERS` if <b>context</b> or <b>sharedMem</b> is null or
    /// the pointed memory is empty.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_RegisterSharedMemory(
        context: *mut TEEC_Context,
        sharedMem: *mut TEEC_SharedMemory,
    ) -> TEEC_Result;
    /// Requests shared memory in the specified TEE context.
    ///
    /// The shared memory can be used to implement zero-copy during data transmission between the REE and TEE.
    /// The zero-copy function, however, also requires support by the operating system.
    /// At present, zero-copy cannot be implemented in this manner.
    ///
    /// If the <b>size</b> field of the input parameter <b>sharedMem</b> is set to <b>0</b>, <b>TEEC_SUCCESS</b>
    /// will be returned but the shared memory cannot be used because this memory has neither an address nor size.
    /// # Arguments
    ///
    /// * `context` - [IN/OUT] Indicates the pointer to the TEE that is successfully initialized.
    ///
    /// * `sharedMem` - [IN/OUT] Indicates the pointer to the shared memory. The size of the shared memory cannot be 0.
    ///
    ///
    /// # Returns
    ///
    /// * Returns `TEEC_SUCCESS` if the operation is successful.
    /// Returns `TEEC_ERROR_BAD_PARAMETERS` if <b>context</b> or <b>sharedMem</b> is null.
    /// Returns `TEEC_ERROR_OUT_OF_MEMORY` if the available system resources are insufficient.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_AllocateSharedMemory(
        context: *mut TEEC_Context,
        sharedMem: *mut TEEC_SharedMemory,
    ) -> TEEC_Result;
    /// Releases the shared memory registered or acquired.
    ///
    /// If the shared memory is acquired by using `TEEC_AllocateSharedMemory`,
    /// the memory released will be reclaimed. If the shared memory is acquired by using `TEEC_RegisterSharedMemory`,
    /// the local memory released will not be reclaimed.
    /// # Arguments
    ///
    /// * `sharedMem` - [IN/OUT] Indicates the pointer to the shared memory to release.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_ReleaseSharedMemory(sharedMem: *mut TEEC_SharedMemory);
    /// Cancels an operation.
    ///
    /// This operation is only used to send a cancel message. Whether to perform the cancel operation is
    /// determined by the TEE or TA.
    /// At present, the cancel operation does not take effect.
    /// # Arguments
    ///
    /// * `operation` - [IN/OUT] Indicates the pointer to the data to be sent from the CA to the TA.
    ///
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn TEEC_RequestCancellation(operation: *mut TEEC_Operation);
}
